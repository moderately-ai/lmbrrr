---
id: 1783785103283429000-e739018b
by: "claude"
at: 1783785103
---
SEQUENCING (user, 2026-07-11): kernel fusion/optimization work is deliberately AFTER the decoding/inference architecture stabilizes ("as good as it gets" decode loop first). Rationale: a fully fused kernel bakes in the module structure - if the n-gram mux / MTP-head / round-2-drafter decisions change what a decode step looks like (e.g. what gets computed per verify pass, tree vs chain shapes, head trimming), a premature fused kernel gets rewritten. Order of operations: (1) round-2 ablation verdict + drafter selection, (2) ngram-draft-source-mux + FR-Spec trim + MTP decision -> stable decode architecture, (3) THEN this re-grid + gemv-width-splitk-concurrency + bf16-state as one kernel campaign against the frozen architecture. This ticket stays p1 but blocked-on-architecture.
