---
id: 1783892352842416000-13691982
by: "claude-main"
at: 1783892352
---
REVISED 2026-07-12 (stale numbers corrected; profile evidence attached): the 2.8ms/19GB/s/520-600 tok/s figures predate the falsification — honest baseline: deltanet_fused_decode is 29.6% of the decode forward per lmbrrr profile (18 of 24 layers; whole block ~0.9-1.4ms of the 5.51ms in-loop greedy step). Root constraint verified by reading the kernel: v1 decode grid = heads(16) TGs and a TG cannot span GPU cores, so the kernel caps at 16 of ~40 cores REGARDLESS of threads/TG; the per-head RMSNorm (all dv columns in one TG) is what pins the single-dispatch geometry there. DESIGN (settled): 2-dispatch decode — (A) new gated_delta_v2_decode_bf16 = fused prep+core at l=1 in the v2 transposed-state geometry, grid (dv/4, b*heads)=512 TGs, all reductions simd-scope (l2norm via per-lane partials + simd_sum; zero threadgroup barriers), redundant per-lane conv/gates (negligible), no kn/qn/vc device round-trip, emits o_pre + state_out + conv_out; (B) existing gated_delta_v2_epilogue at l=1 (16 TGs, tiny). This is v2-at-l=1 minus one dispatch and minus staging traffic — the form that lost to v1 was the 3-dispatch one. BONUS: decode joins chunks on the transposed layout, deleting the per-round v1<->v2 state transposes. Prize: block ~0.9-1.4 -> ~0.5-0.7ms (~8-12% of the greedy floor). Batched variant preserved (grid scales by b*heads). Gates: stub oracle, 59 tests, drafter parity, greedy bench A/B per protocol.
