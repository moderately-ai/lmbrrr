---
id: 1783793820635243000-0b73560b
by: "claude"
at: 1783793820
---
PARTIAL DELIVERY + diagnosis correction (2026-07-11, commits: fork 3781e79f, lmbrrr d96da76).

Shipped: v2 kernel family in MLX gated_delta geometry — 512 TGs (one simdgroup per head/value-column, dk split across 32 lanes in registers), transposed [heads, dv, dk] state, unified decode/chunk T-loop whose per-step deltas ARE the WY pseudo-values (captures come out of the loop for free). Three dispatches (prep/core/epilogue) because group RMSNorm needs the head's full dv row. Routing is hybrid: chunks l=2..12 (chain + tree segments) on v2; decode stays v1. Gates all green: greedy + dspark outputs identical to v1 through 13 rollbacks, tree-check main rows bitwise (0.0) vs the v2 chain, 49/49 tests. Measured: verify chunks -2.5% (l=2) to -9.1% (l=12); dspark math end-to-end +4.4% (153.9->160.7 same-session).

DIAGNOSIS CORRECTION — the ticket's "2.8 ms of the 4.25 ms token" figure is FALSE. It came from host-side profiler attribution, which absorbs queue backpressure from upstream ops. Subtractive measurement (fused vs unfused sweep deltas) puts the v1 fused decode kernel at ~0.9 ms/token TOTAL across 18 layers. Consequences: (a) v2's 3-dispatch structure costs ~0.8 ms, wiping out the core's decode-side win — v2 decode measured +0.8 ms SLOWER, hence the hybrid; (b) the decode-side upside of any regrid is bounded by ~0.9 ms (~+15% tok/s at 235), not the projected 235->520-600 — that projection is dead; (c) most of the l=1->2 verify intercept belongs to the quantized mc matmul path + SDPA/dense mode switches, NOT DeltaNet (v2 chunks moved the l=2 step only -2.5%) — see gemv-width-splitk-concurrency.

Remaining scope if decode gains are still wanted: fold prep into the core with per-TG redundant conv/norm (one dispatch + epilogue), eliminating the staging round-trip; expected net vs v1 decode is small (<0.5 ms). Lower priority than the gemv/mc work by the corrected numbers. Also: the spec-round cost model predates the chunk-cost change (2.5-9%) — refit before the next scheduler-sensitive benchmark session.
