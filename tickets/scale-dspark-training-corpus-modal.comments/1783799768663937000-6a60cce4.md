---
id: 1783799768663937000-6a60cce4
by: "claude"
at: 1783799768
---
Spec-Bench re-baseline + scheduler follow-through (2026-07-11, commits e79c46b/135409a). Vendored the standard Spec-Bench question set (480 items; the benchmark FR-Spec/SpecVocab/EvoSpec all report on) with a fixed calibration/validation split and a suite harness (evals/run_spec_suite.py) - hand-written prompts retired. On the held-out split, scheduled: B beats r1 strongly on math_reasoning (168 vs 144) and coding (156 vs 131) but regressed on summarization/writing/qa - root cause NOT calibration (balanced refit did not fix it; context-dependent draft cost hypothesis falsified with data - draft ms flat 22->872 prompt tokens) but a STRUCTURAL scheduler gap: width is chosen with draft cost sunk, so a strong drafter keeps width>0 and the old width-0-gated skip-hysteresis never engages on weak classes. Fixed with rate-based hysteresis evidence (round counts toward skip unless realized rate incl. draft beat greedy pace): r1 mean 130.3->155.2 (+19%), B recovers weak classes. Current standings: r1r 155.2 vs b3 152.6 mean - near-tie; B strictly better tau everywhere and decisively better on math/coding. RECOMMENDATION: B becomes the primary drafter for the campaign (its tau advantage converts to throughput as verify costs drop - at today's costs w=2 speculation needs ~3 committed tokens to beat greedy; post-gemv break-even ~2). Follow-up filed on the scheduler: deficit-weighted/EWMA hysteresis (marginal rounds currently count like total misses, over-suppressing B: coding tau 2.56->1.75).
