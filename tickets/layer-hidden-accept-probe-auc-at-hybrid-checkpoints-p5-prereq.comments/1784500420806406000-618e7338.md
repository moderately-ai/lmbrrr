---
id: 1784500420806406000-618e7338
by: "agent-program"
at: 1784500420
---
## MEASURED (2026-07-19, M3) — simple checkpoint features FAIL 0.85 AUC bar

### Instrumentation
`LMBRRR_ACCEPT_PROBE=1` on `gguf spec`: during verify captures layers **15/31/47/63** (+ drafter taps), emits per-draft-pos rows:
`L{15,31,47,63}_{rms,mean,maxabs}`, `conf`, `exact`, `prefix_ok`, `on_accept_prefix`, `pos`, `round`.

### Data
424 rows pooled: exact prose + exact code + m1 prose (N=128 each).  
prefix_rate=0.63, exact_rate=0.78.

### Single-feature AUC (best orientation)

| feature | prefix AUC | exact AUC |
|---|---:|---:|
| **conf** (drafter) | **0.786** | **0.809** |
| L63_maxabs | 0.607 | 0.614 |
| L15_maxabs | 0.626 | 0.601 |
| L31_rms | 0.592 | 0.563 |
| other L*_rms/mean | 0.50–0.62 | 0.50–0.63 |

**BEST = conf 0.79 < 0.85 kill bar → FAIL.**

### Conf threshold (on_accept_prefix)
| thr | prec | rec |
|---|---:|---:|
| 1.0 | 0.79 | 0.79 |
| 1.5 | 0.84 | 0.66 |
| 2.0 | 0.88 | 0.56 |

### Interpretation
1. **Mid-stack hidden RMS/mean/maxabs are weak accept predictors** (~0.55–0.63). Early-exit verify that only looks at layer-norm stats will not clear the gate.
2. **Drafter conf is the strongest available signal (0.79)** but still short of 0.85, and live `--skip-low-conf` already showed trajectory damage at thr=1.0 despite decent thr precision.
3. P5 early-exit needs either: (a) richer features (learned linear/MLP on full/partial hiddens, not 3 scalars), or (b) accept-predictor trained end-to-end on trajectories with audit — not a free scalar gate.
4. Layer-checkpoint early-exit as scoped in the program is **not unparked** by this probe.

### Kill criterion
AUC ≥ 0.85: **FAIL** (best 0.79 conf / 0.63 layer).  
Ticket DONE (probe shipped + measured). P5 remains blocked on a stronger model.
