---
id: 1783762421008849000-a5aa0602
by: "claude"
at: 1783762421
---
CLOSED BELOW MEASUREMENT THRESHOLD (2026-07-11). Serialized-vs-pipelined probe on the fastest operating point (q4k-full-text, 234 tok/s): one-giant-CB (serialized = host+gpu) 4.82 ms/token vs pipelined (max(host,gpu)) 4.27 ms -> host-side encode + dispatch-layer constants are bounded at ~0.55 ms/token (~13%) and fully hidden by command-buffer pipelining. Shaving allocator-scan/fence-map/encoder-lock constants buys zero wall-clock until GPU time falls to ~0.5 ms/token (single-stream ~800+ tok/s, i.e. after another ~8x of kernel-side wins) or dispatch counts rise dramatically. Reopen if either happens; the probe (CANDLE_METAL_COMPUTE_PER_BUFFER=4096 A/B) is the cheap re-check.
