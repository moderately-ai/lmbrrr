---
id: 1784553744920602000-a81104a1
by: "agent-program"
at: 1784553744
---
## KILL by bound (2026-07-19) — not worth fork implement

### Prior kernel bound (probe_nofold, already on metallib)
Entire fold epilogue (d load + rowsum + fma) ≤ **~7%** of mm2d wall (0.513 vs ~0.55 ms).

### int2b deletes only part of that
int2b encodes {−1,0,+1} directly → delete **P−rowsum** staging/barrier/fma. Still need **software d·P** scale (ue8m0 hardware scale is separate fullk ticket). So realized kernel win **≪ 7%**, likely ~3–4% of mm2d if lucky.

### e2e ceiling from profile (verify_width=5, taps on)
| component | % of step |
|---|---:|
| mlp (hosts mm2d) | 56.2 |
| deltanet_fused | 24.1 |
| rest | ~20 |

Optimistic: 0.04 × 0.56 of verify × 0.84 of round ≈ **~1.9% tok/s** if 100% realized. Kill bar is **spec +1%**. Eng cost = candle fork repack + int2b metallib + parity tests on M3 beta toolchain.

### Decision
**KILL implement** — EV at the noise floor of soft-adapt wins, bound already published, campaign capital better on structure (accept/drafter). Does not refute that int2b is "correct algebra"; only that M3 OP won't feel it.

Exactness E. Regime: M3 mm2d m≤8 instruction-bound.
