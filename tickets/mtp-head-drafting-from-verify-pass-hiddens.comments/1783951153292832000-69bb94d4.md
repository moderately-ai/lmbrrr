---
id: 1783951153292832000-69bb94d4
at: 1783951153
---
CORRECTION + IMPLEMENTATION NOTES (2026-07-13, deep agent pass that PARSED THE SAFETENSORS HEADERS — supersedes the previous comment's claim). MiniCPM-V-4.6 does NOT ship an MTP head: zero mtp.* tensors in openbmb/MiniCPM-V-4.6 and -Thinking (779 tensors each, full name dump checked); the config's mtp_num_hidden_layers=1 / mtp_use_dedicated_embeddings=false are vestigial Qwen3.5 inheritance (transformers' Qwen3_5ForCausalLM even drops mtp.* on load). The head EXISTS in Qwen/Qwen3.5-0.8B — the architecturally-identical base our tower was finetuned from (hidden 1024, 24 layers, same comb/dims/eos) — as 15 root-level mtp.* tensors with NO vocab-sized tensor (embeddings + lm_head tied to the target's). SO: shape-compatible TRANSPLANT is possible; acceptance on the finetuned VLM tower is unmeasured — that measurement is now Phase 1 of this ticket. If transplant acceptance is degraded but nonzero, Phase 2 = distill the head on MiniCPM outputs (Modal credits; init from Qwen's head; cheap next to a full drafter round).\n\nIMPLEMENTATION (from mlx-lm PR #1456 + vLLM qwen3_5_mtp.py, agreeing exactly):\n- TENSORS (all bf16): mtp.fc.weight [1024,2048] no bias; mtp.pre_fc_norm_embedding/hidden [1024]; mtp.norm [1024]; one full-attention decoder layer clone: q_proj [4096,1024] (=[q;gate], attn_output_gate: out = o_proj(attn * sigmoid(gate)), o_proj in 2048), k/v_proj [512,1024], q/k per-head-dim RMSNorm [256], gate/up [3584,1024], down [1024,3584].\n- TRAP: raw Qwen checkpoints store ZERO-CENTRED RMSNorm weights (kernel computes (1+w)*xhat) — shift ALL mtp norm weights +1.0 at load if our kernel is standard RMSNorm (mlx-lm sanitize does; our loader must match whichever convention our Qwen35RmsNorm uses — CHECK, we loaded the same-family tower so we already handle this somewhere).\n- FORWARD (one step): e = rms(embed(next_token), pre_fc_norm_embedding); h = rms(POST-FINAL-NORM trunk hidden, pre_fc_norm_hidden) — yes the hidden is normed twice, trained that way; x = fc(cat([e, h])) — EMBEDDING FIRST; x -> the full-attn decoder layer with ITS OWN plain KV cache (no GDN state — trivially trimmable), partial RoPE dims 0-63 theta 1e7; logits = rms(x, mtp.norm) @ embed.T (our 248094-row embedding).\n- MULTI-STEP (head trained for recursion, vendor says 'multi-steps'): catch-up batched step over all (hidden, successor-token) pairs the MTP cache hasn't seen; then depth 2..k single steps feeding the head's OWN post-norm hidden + its drafted token. Vendor operating points: k=2 (vLLM), steps 3/draft 4 (SGLang), k=4 profitable on Apple silicon (mlx-lm).\n- VERIFY/ROLLBACK: slots into OUR existing per-round rollback machinery as just another draft source (our verify already handles hybrid-state rollback); the MTP KV cache trims by rejected count each round. mlx-lm's losslessness bar for reference: bit-exact tail-independence of caches after rollback + e2e text equal to greedy modulo <=1-bf16-ULP top-2 ties.\n- EXPECTED (family numbers, greedy): depth-1 acceptance 83-100% (M5 Max, 122B), ~80% (27B M4 Pro), SGLang mean accept ~3.2/4 — worth racing round-4 DSpark (drafter-checkpoint-ab-protocol) even from a transplanted head.\nPHASING: 1) load Qwen3.5-0.8B mtp.* + wire single-step drafting behind a flag, measure transplant acceptance on 6-class suite (cheap, answers viability); 2) if 40-80%: distillation pass on Modal (user sign-off with round-5); 3) full A/B vs DSpark round-4 per protocol. Effort: phase 1 is days (one attention layer + fc + norms, all patterns exist in qwen35.rs).
