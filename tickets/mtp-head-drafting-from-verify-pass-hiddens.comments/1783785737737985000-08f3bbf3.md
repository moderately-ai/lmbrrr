---
id: 1783785737737985000-08f3bbf3
by: "claude"
at: 1783785737
---
SCOPE (from the 2026-07-11 frontier survey + user P1.3 discussion). Train 1-2 MTP heads that predict the +1/+2 token from the TARGET's final hidden state at the last accepted position - the tensor the lm_head already consumed in the verify pass - so drafting costs one extra projection folded into verification instead of the DSpark block rollout (~5 ms/round, ~30% of the round at tau 2.66). DeepSeek-V3 production reference: 85-90% acceptance on +1. Relationship to DSpark (NOT orthogonal - attacks its cost line, not its tau line): (a) supplement mode - MTP pre-extends the next anchor before the block runs (~+0.8 tau for one matmul); (b) replacement mode per-class - where DSpark tau ~1.3 (prose), a free +1 at 85% beats paying 5 ms for mostly-rejected drafts; scheduler EV gate decides per round. Training: ~$20-60 Modal on the round-2 quantized-target traces (already generated); the checkpoint ships NO native MTP weights (audit-minicpm-mtp-weights + re-verified 2026-07-11). Head cost note: 248k-vocab output projection per head - batch heads through the multi-column kernels + apply FR-Spec trim (frspec-drafter-vocab-trim composes). DECISION GATE: pick supplement vs replacement vs skip after (1) the round-2 warm/fresh ablation verdict and (2) the DSpark paper re-read agent's findings on whether the paper ablated MTP-style drafting - UPDATE THIS TICKET with that audit's conclusions before starting work.
