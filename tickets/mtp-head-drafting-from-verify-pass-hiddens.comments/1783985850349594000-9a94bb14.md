---
id: 1783985850349594000-9a94bb14
at: 1783985850
---
M3 RELEASE DEPTH SWEEP (vec-eval, math prompt, 128 tokens, q4k-full-text target): depth1 111.7 tok/s (tau 1.53, p1 0.54), depth2 110.3 (tau 1.92, p1 0.59), depth3 85.1, depth4 69.3 — vs greedy 182.6 and DSpark gamma-6 ~142 on this prompt. MTP loses on M3 NOT on drafting cost (2.9-6ms/128tok total) but on the verify slope: verify(2)=9.8ms vs greedy 5.5 means even a perfect depth-1 drafter caps out under greedy. Operating point is depth 1-2. Break-even (with gemv-width-splitk-concurrency flattening verify(2) to ~6.5ms and ~1ms draft): ~204 tok/s at the CURRENT untuned tau 1.53 — spec beats greedy again IF split-k lands. Distillation (phase 2, user sign-off) pushes p1 0.54 -> ~0.85 for more headroom. OPEN ITEM before any ship: depth-2 ran exact-prefix 45/128 (depths 1/3/4 exact 128/128) — presumed benign chunk-shape tie-flip (the documented ulp class) but needs the stub-oracle-style top-2 margin check at the divergence position; add top-k capture to the mtp runner or run the invariance oracle across depths.
