---
id: 1783947159955442000-1464fe3b
at: 1783947159
---
PRIORITY EVIDENCE + EVAL PROTOCOL (2026-07-13 research pass): the checkpoint SHIPS a vendor-trained MTP head — config mtp_num_hidden_layers=1, mtp_use_dedicated_embeddings=false, Qwen3-Next-style: fc([norm(embed); norm(hidden)]) -> one full-attention decoder layer -> SHARED lm_head; weights are inside model.safetensors (official GGUFs DROP these tensors; we load from safetensors so we have them). This is a free, vendor-trained drafter to benchmark our round-4 DSpark drafter AGAINST — the single most obvious missing comparison arm. Ecosystem context: llama.cpp merged generic MTP (#22673) but a documented report shows MTP spec is a NET LOSS on Metal there (-11% even at 100% acceptance — their batch-N verify overhead); mlx-lm PR #1456 loads the same-style head with replay-based GDN rollback and measures 1.36-2.11x on M5 Max — so the head is good and the loss is llama.cpp's Metal verify path, which OUR stack does not share (our verify economics are already positive). EVAL once implemented: drafter-checkpoint-ab-protocol recipe, arms = round-4 DSpark vs MTP-head drafting (depth 1..3), report tau + acceptance distribution + tok/s per class; also feeds round-5 training decisions (if the vendor MTP head wins anywhere, its predictions are distillation targets). Implementation reference: mlx-lm PR #1456's head wiring + their <=1 bf16 ULP batched-vs-sequential losslessness criterion for hybrid-state rollback tests.
