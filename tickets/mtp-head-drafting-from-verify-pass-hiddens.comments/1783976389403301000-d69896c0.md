---
id: 1783976389403301000-d69896c0
at: 1783976389
---
BLOCKING QUESTION RESOLVED (2026-07-13; read vLLM qwen3_5_mtp.py @ main and mlx-lm PR #1456 @ d742b7ee incl. its test_qwen3_5_mtp.py — they agree exactly). ATTENTION CONTEXT: the MTP layer keeps its OWN persistent plain KV cache, fully separate from the trunk's — it never attends over trunk KV ('the MTP layer only attends within its own cache'). Protocol: (1) CATCH-UP: after a trunk forward over positions p..p+S-1, one batched mtp_step over the S-1 pairs (post-final-norm hidden at position i, token at i+1) appends S-1 rows to the MTP cache — mtp_step(hidden[:, :-1], tokens[:, 1:]); (2) RECURSION depth k>1: feed the head's OWN post-norm hidden + its drafted token, one pair per step; (3) ROLLBACK: plain cache.trim(rejected_count) — no hybrid-state machinery needed for the head itself; (4) ROPE POSITIONS: use the MTP cache offset (count of pairs fed) directly — uniformly absolute-1, and the shift cancels in q.k because attention is cache-internal (mlx doc comment states this explicitly); (5) the layer is a STANDARD full-attention decoder layer (packed [q;gate] sigmoid-gated output, per-head-dim qk norms, partial rope) = our qwen35.rs FullAttention verbatim, and logits go through the target's own tied-embedding lm_head. vLLM corroborates structurally (Qwen3_5DecoderLayer('full_attention'), fc(cat[norm(embed), norm(hidden)]), shared lm_head, separate draft-model KV). PHASE 1 IS UNBLOCKED: remaining work is (a) pull Qwen/Qwen3.5-0.8B mtp.* tensors + verify sub-tensor names against DecoderLayer's self_attn.*/mlp.* paths, (b) MtpHead in qwen35.rs (zero-centred norm loading per the wiring-validation comment), (c) capture_final_norm hook, (d) --drafter-mtp depth<=4 behind a flag with --schedule off, (e) transplant-acceptance measurement on the 6-class suite.
