---
id: 1784047220513469000-713164a0
at: 1784047220
---
DISTILLED-HEAD SWEEP (2026-07-14, M3, macOS 27, greedy floor 197.6). Distillation run mtp-distill-r1 (H100, 2 epochs, 40k convs, regen-r4-400k): holdout top-1 0.3829 (vendor) -> 0.7595. ON-DEVICE: p1 acceptance 0.54 (vendor) -> 0.724 (distilled) — the alignment hypothesis fully CONFIRMED; acceptance is no longer the constraint. Depth sweep (tides, 384 tok): d1 116.9 tok/s (tau 1.71), d2 111.7 (2.06), d3 102.2 (2.32) — all FAR below greedy 197.6. ROUND DECOMPOSITION (d1): 17.17ms/round = draft 3.52 + verify 9.45 + other(host/sync) 4.20. THE BLOCKER IS VERIFY ECONOMICS, NOT ACCEPTANCE: chunk-2 verify costs 9.45ms = 1.87 greedy tokens (5.06ms each), so even PERFECT depth-1 acceptance (tau=2.0) with zero draft/host cost only breaks even; d3 needs round < 11.7ms but verify-chunk4 alone is 12.5ms. Verify is compute-bound (~20% ALU eff at m=2-4, per the verify-curve dossier) when it should be near-bandwidth-bound (weights read once for all rows) — the fixable root is the m-in-[2,8] quantized GEMM path (parked gemv-width-splitk-concurrency lane), plus the 4.2ms/round host overhead and 3.5ms draft cost in this phase-1 synchronous harness (both shrinkable but insufficient alone). Lossless check: single divergence at pos 119, top2_margin 0.0, tie_flip_class true — the documented sub-noise tie-flip, benign. VERDICT: spec lane blocked on verify-width kernel efficiency; greedy (197.6) remains the product path until that kernel lane reopens. The distillation asset is banked: acceptance side is solved whenever verify economics flip.
