---
id: 1783984901129186000-21e73715
at: 1783984901
---
PHASE 1 IMPLEMENTED AND VIABLE (commit after cb58219 series; --drafter-mtp + --mtp-depth). M4 correctness smoke, q4k-full-text target, depth 3, math prompt, 64 tokens: committed stream EXACTLY equals the greedy stream (64/64 prefix — losslessness held on first contact), mean accepted length 2.03/round, position-1 conditional acceptance 48.4%, full-depth-3 acceptance 8/31 rounds. That is an UNTUNED transplant onto the VLM-finetuned tower (ticket's viability bands said 40-80% -> distillation candidate; we are mid-band with far better economics than DSpark: ~3 one-layer forwards per round vs the ~10ms block propose). Loop design proved out: catch-up produces draft_1 for free; recursion pairs truncate every round; verify hiddens ARE the next catch-up input (cache holds only true-hidden committed pairs — lossless by construction). NEXT: M3 release measurement — depth sweep 1..4 vs greedy vs DSpark round-4 on the suite classes (queued behind chain-eval); the steep verify slope (verify_ms[2]=9.8, [4]=12.83 vs greedy 5.5) is now the shared wall for ALL spec lanes — gemv-width-splitk-concurrency is the lever that moves it. If M3 standings justify: phase 2 distillation proposal (user sign-off) to push position-1 past 80%.
