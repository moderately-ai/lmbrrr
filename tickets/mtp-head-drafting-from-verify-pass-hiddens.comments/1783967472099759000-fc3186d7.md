---
id: 1783967472099759000-fc3186d7
at: 1783967472
---
WIRING VALIDATED against our code (2026-07-13; full report in session log). GREEN LIGHTS: (1) NORM VERDICT — load every mtp.* norm through Qwen35RmsNorm::new(..., zero_centered=TRUE, ...) (qwen35.rs:187-196 applies the +1 shift at load; the trunk's full-attn layers already do this); NEVER dspark.rs's RmsNorm (deliberately unshifted — silent corruption). (2) The trunk's FullAttention (qwen35.rs:535-805) already implements the packed [q;gate] + sigmoid-gated output and per-head-dim q/k norms — DecoderLayer::new(LayerType::FullAttention, ...) constructs the entire MTP layer clone; implement MtpHead INSIDE qwen35.rs (building blocks are private). MUST share the trunk's Arc<RotaryEmbedding> (partial 0.25) — dspark.rs's Rotary ignores the partial factor (garbage trap). (3) HIDDENS: post-final-norm hidden is computed but discarded (qwen35.rs:2389-2398); smallest addition = capture_final_norm flag + take_final_norm_capture, mirroring the existing device-capture pattern; only ONE target hidden per round is needed (recursion feeds on the MTP's own norm output). (4) CONFIDENCE STORY, phase 1: fixed depth d<=4 with --schedule OFF (identity STS would make the scheduler arbitrary), but LOG softmax-margin pseudo-confidences per step so a Platt sts.json can be fitted offline for phase 2 — the scheduler plumbing is already generic. (5) Load via a separate VarBuilder from the Qwen3.5-0.8B file (the DsparkDrafter::load pattern).\nBLOCKING RISK before any code: the 15 tensors do not disclose the MTP layer's ATTENTION CONTEXT semantics — own fresh per-round KV seeded by the anchor hidden (EAGLE-style, assumed) vs attending over trunk KV. Read the reference implementation (mlx-lm qwen3_5.py MTPModule) for the ground truth first; also verify the mtp.* sub-tensor names against DecoderLayer's hardcoded self_attn.*/mlp.* paths (safetensors header dump).
