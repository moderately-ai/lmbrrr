---
id: 1783986119513234000-89e8d8ee
at: 1783986119
---
Depth-2 divergence RESOLVED: margin capture added to the runner (committed top-k trajectories + a divergence report field). At the divergence position (45) the top-2 margin is 0.125 — far inside the 0.75 documented noise bound: chunk-shape tie-flip (depth-2 chunks are length 3, re-rolling ulp ties vs other depths), NOT a rollback defect. MTP drafting is lossless modulo the same sub-noise tie class as every other verified lane.
