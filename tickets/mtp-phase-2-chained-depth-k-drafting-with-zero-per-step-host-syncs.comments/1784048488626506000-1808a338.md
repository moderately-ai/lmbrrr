---
id: 1784048488626506000-1808a338
at: 1784048488
---
Today each draft step is mtp_step -> argmax_token -> BLOCKING readback + device.synchronize() (dspark.rs draft loop) — a full host round-trip per drafted token. Replace with one command stream per round: GPU-side argmax (fused_head machinery, u32 out) feeds the next step's embedding lookup via on-device index_select; depth-k drafting = k dependent dispatch groups in one CB, no host involvement until the round's single structural sync. Precedent: drafter-chain-persistent-kernel (done) for the markov drafter; the MTP variant chains embed -> 1 decoder layer -> fc -> norm -> sliced-head argmax. With the FR-Spec slice, target draft ~0.5-0.8ms for depth 1-3 (vs 3.5-6.6 today). Gate: drafted ids byte-identical to the synchronous path.
