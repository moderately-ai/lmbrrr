---
id: 1783914840346636000-672747cd
at: 1783914840
---
SLICE 2 LANDED (lmbrrr 71b5fc0, fork ff134429): fused SwiGLU in the MLP. candle-nn ops::swiglu now dispatches a fused CustomOp1 (swiglu_bf16/f16/f32, one thread/output element, reads packed gate_up directly) — validates the framework path alongside slice 1's direct wrapper. Mlp::forward uses it for Silu; -24 dispatches/step (silu unary gone, gate*up folded in). GATES: nextest 59/59; fork swiglu unit test bit-exact f32 (CPU+Metal); stub oracle 0.375<0.75 + invariant; tree-check PASSED; greedy identical to slice 1. CUMULATIVE (slices 1+2, rotated A/B vs pre-fusion wave2): +4.0% long / +4.4% medium steady greedy (NON-OVERLAPPING), model-forward host +22-24%; deployed floor ~354 long / ~346 medium (was ~341/331). In-loop refit greedy 3.647->3.616 (barely moves — no-draft rounds host-cost-dominated; fusion win is the tight bench loop not scheduled economics), verify l=1/l=4 cheaper; cost-model-fusion.json shipped. HONEST SIZING CONFIRMED: exact fusion is ~+2%/slice against the 74.58%-mv floor; two slices = +4%. Remaining slices (cross-layer input_layernorm -24, sigmoid-gate -6, rope/qk strided -12/-18) each ~+1.5%, diminishing. The big jump stays the 32k head (fixed-32k-target-head-decision, user).
