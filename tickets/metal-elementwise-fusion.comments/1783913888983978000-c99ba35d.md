---
id: 1783913888983978000-c99ba35d
at: 1783913888
---
SLICE 1 LANDED (lmbrrr b6735f8, fork 1e1801bf): fused residual-add + post-attention rmsnorm. rms_norm_add kernel (ggml kernel_rms_norm_fuse analogue, one TG/row, two-level reduction) + direct (Tensor,Tensor) wrapper src/fused_norm.rs (CustomOp is single-output; the 2 outputs have independent lifetimes + both need offset-0, so the fused_deltanet direct-wrapper pattern is optimal, not the stacked-CustomOp3 workaround). Wired at DecoderLayer post-attention site = -24 dispatches/step (1/layer x 24). GATES (margin, not bitwise — first non-bit-preserving change): nextest 59/59; stub oracle 0.375 < 0.75 (TIGHTER than wave-3's 0.5) + corrupt-invariant; tree-check main 0.00000; greedy text bit-identical to wave-3 (no l=1 flips); drafter tie-flips at tok 26 = the documented DIVERGENT-CONTENT confound (present on the bit-preserving wave-3 binary too; tau unchanged). MEASURED (rotated A/B, 3 reps): steady +1.3% long (non-overlapping) / +1.9% medium; decode_model HOST wall +20.1/20.2% — the dispatch removal is real but SHADOWED by the 74.58%-mv GPU floor (exactly the trace's prediction). Calibration tau stable (mean +0.077, no collapse). VERDICT: exact +1.5% e2e/slice confirms fusion is a real-but-small lever against the mv-dominated floor; ships. Remaining slices: SwiGLU (CustomOp2, -24), cross-layer input_layernorm add (-24, needs layer-return restructure), sigmoid-gate (-6), rope/qk-norm strided-input (-12/-18). Each ~+1.5%; full set ~+4-5% e2e. The big jump remains the 32k head (fixed-32k-target-head-decision, user).
