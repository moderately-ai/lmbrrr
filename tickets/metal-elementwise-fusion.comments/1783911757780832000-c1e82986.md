---
id: 1783911757780832000-c1e82986
at: 1783911757
---
RE-SCOPED + PROMOTED p3->p2 with post-wave census + trace + fusion prior-art (2026-07-12). This is now the top usable GPU-side lever (mv floor closed; see simdgroup-matrix ticket). CENSUS (verified, src receipts): greedy step = ~389 dispatches; the elementwise soup = 181 of them (rmsnorm 61, badd 48, bmul 30, silu 24, sigmoid 6, rope 12) but only ~8.4% of GPU TIME (trace pipeline-stats: rmsnorm 6.70 + bmul 1.27 + badd 0.15 + silu 0.14 + sigmoid 0.13). The count>>time gap IS the barrier-drain teeth around the tiny dispatches — wave-1's empirical calibration (removed ~180 same-class dispatches, got +12-15% e2e, far above their ~9% time share) sizes this slice at ~+5-8% e2e. SLICE (ranked by census savings, do in order): (1) residual-add + rmsnorm fold = -48 dispatches, the single highest value — every (residual+hidden) feeds the next rmsnorm (qwen35.rs:2174->2184, 2196->2156, ->2361). REFERENCE: ggml-metal kernel_rms_norm_fuse_impl (ggml-metal.metal:3058-3117, templated 'short F': F==2 mul, F==3 mul+add) — fold the residual add into the norm's load pass (read x+resid once, one reduction, one write). Keep the _4 float4 + scalar-tail split (ggml #16220) so odd inner dims vectorize. (2) SwiGLU silu*mul = -24; REFERENCE kernel_swiglu (:1484, silu(gate)*up one pass). (3) sigmoid*mul DeltaNet/attn gate = -6; no ref exists in ggml/MLX, write the trivial swiglu analogue with sigmoid. (4) rope-contiguous + qk-norm-contiguous strided-input variants = -12/-18 movement (make rope_partial/rms_norm accept strided input, drop the .contiguous()). NOTHING to borrow wholesale: candle #3467 lazy-backend is a capture scaffold with zero fusions implemented (stalled, conflicting); MLX mx.compile can't cross the norm boundary; MLX ships no residual-fused norm. Hand-write, static (we know our block structure — no graph matcher needed). GATES: NOT bit-preserving (F32 intermediate rounding) -> margin oracle + tree-check + text-compare-advisory, package bench + in-loop refit after. WATCH upstream #3485 (fused quantized SwiGLU gate/up) + #16143 (ggml fused RMS_NORM+MUL+SWIGLU for Qwen3Next, our arch class).
