---
id: 1784074185530727000-3bdd11df
at: 1784074185
---
FIRST GREEDY CROSSINGS — honest decode-only, suite-measured (2026-07-14 night; receipts /tmp/suite-mtpfr.log + artifacts/suite-mtpfr-*, M3, validation split, 3q x 2 reps).

FUSED ROLLBACK RECONSTRUCTION SHIPPED (fork 614c45af kernel gated_delta_v2_reconstruct_{f32,bf16}; lmbrrr fused_deltanet::gated_delta_v2_reconstruct + qwen35 wiring, reference chain under LMBRRR_UNFUSED_RECONSTRUCT=1): one dispatch per layer replaces the 8-10-op f32 chain. GATES: stub invariance oracle PASS; fused-vs-unfused text divergence = single tie-flip (pos 119, margin 0.125 <= 0.75 bound, the documented sub-noise class). EFFECT (tides d3, warm): draft bucket 5.26 -> 2.89 ms/round, other 0.52 -> 0.40 — the reconstruction GPU time had been draining into the NEXT round draft window, exactly as the shader-profile attribution predicted. d3 dev arm 129.9 -> 169.2 tok/s.

SUITE (mtpd3fr = distilled MTP d3 + real 32k slice + chained draft + mm2d splitk verify + fused recon): 6-class MEAN 170.4 tok/s = 86 pct of greedy 197.6 (evening arc: 137.5 -> 145.2 -> 170.4). CROSSINGS: math 212.2 / 204.9 / 222.5 (ALL THREE questions beat greedy; class mean 213.2 = +7.9 pct), translation q164 218.2, qa q326 205.2; d5 math q404 223.3 (+13 pct). Class means: math 213.2, coding 184.3, translation 175.0, qa 162.0, writing 148.6, summarization 139.1. d5 still loses the mean to d3.

REMAINING to lift the MEAN over greedy: (1) r2 distillation (in flight, holdout already 0.7555 at step 4000 vs r1 final 0.7595 — tracking well ahead per-step); (2) weak-class floor via scheduler/chain-handoff (wall 3a: writing/summarization/qa at tau 1.8-2.1 should exit to greedy, making 197.6 the per-class floor — that ALONE puts the effective mean near greedy with math/coding upside on top); (3) draft chain dispatch collapse (~1ms of the remaining 2.9ms draft); (4) fused m-row verify argmax (~0.3ms). SHIP BAR unchanged: mean >= +2 pct with no class regressing — with the greedy floor in place the bar becomes reachable this week.
