---
id: 1784072971152662000-145f00b7
at: 1784072971
---
ATTRIBUTION COMPLETE (2026-07-14 night, follow-up to the round decomposition): the bmul_f32 block (~20 pct of the round with copy_f32/badd_f32/gemm_nn_f32) is ROLLBACK STATE RECONSTRUCTION — qwen35.rs reconstruct_capture_state, invoked per GDN layer (x18) on every partial-accept round (40-77 pct of rounds at p1 0.57-0.73): per layer it does f32 broadcast_sub+exp over gcs, broadcast_mul of kc by rel, two contiguous copies, an f32 GEMM (delta^T x wk), and broadcast_mul of s0 [heads, 256, 256] by exp(gcs_j) — ~0.5M-element strided f32 elementwise per layer per rollback. The fused-norm path is NOT the source (rms_norm already routes to the fused kernel; the f32 chain at qwen35.rs:252 is the LMBRRR_UNFUSED_RMSNORM debug path only).

FUSION TARGET (metal-elementwise-fusion p1 scope, benefits BOTH spec vehicles): one Metal kernel per layer computing S = delta^T-x-(kc*exp(gcs_j - gcs)) + exp(gcs_j)*S0 in a single dispatch (small K-loop GEMM over prefix<=8 rows + decay FMA on the state tile — the prefix is tiny, the state is 8x256x256, this is a bandwidth-shaped kernel, expected ~5-10x over the current 6-8-op f32 chain). Secondary: conv-window narrow/pad copies ride along. Estimated recovery: ~1.5-2.5 ms/round at current acceptance; shrinks as p1 rises (r2).
