---
id: 1783945199436210000-fac81eeb
at: 1783945199
---
EXECUTOR PROCEDURE — remaining slices (rms_norm_add, swiglu, rope_partial are SHIPPED at pin ff134429; +4% bench). Remaining dispatch-count prizes per the dossier census (docs/research/metal-decode-utilization.md): input_layernorm cross-layer fusion (-24 dispatches/step), DeltaNet sigmoid-gate into decode kernel (-6), rope/qk strided handling (-12/-18).

PER-SLICE NOTES:
- input_layernorm cross-layer: fold layer L+1's input RMSNorm into layer L's tail rms_norm_add (three outputs: residual, normed-for-L-tail, normed-for-L+1-head) OR reuse the existing rms_norm_add by restructuring the layer loop so the residual-add + next-norm pair always goes through the fused op. PREFER the restructure — no new kernel. Edit site: src/qwen35.rs layer loop + Qwen35RmsNorm::forward_add.
- sigmoid gate: gated_delta_v2 decode kernel (fork candle-metal-kernels/src/metal_src/gated_delta_v2.metal) — apply sigmoid to the gate inside the decode kernel epilogue instead of a separate unary dispatch. Bit-preservation check: sigmoid on bf16 in-kernel vs candle's unary sigmoid may differ in rounding — run the text gate; if text diverges, gate on stub-oracle margin (dev <= 0.75) + quality ladder instead and say so in the ticket.
- rope/qk strided: rope_partial currently requires contiguous input (offset0 sanitize in src/fused_norm.rs pattern); the q/k views after the qkvz split are strided, costing copy dispatches. Extend the rope_partial kernel (fork reduce.metal) with row-stride params to read strided q/k directly. Bit-preserving (same arithmetic, different addressing).

GATES for every slice (order): fork edit -> commit -> push tomsanbear -> bump 4 rev= pins in lmbrrr/Cargo.toml -> cargo build --release -> cargo nextest run (59) -> REBUILD (nextest clobbers the metal binary) -> stub oracle (dspark-run tides prompt, invariance true, dev <= 0.75) -> tree-check -> text byte-identical vs previous binary (except sigmoid slice, see above).
MEASUREMENT: slices are individually below bench noise (~1-2 dispatches percent); land all three, then ONE rotated bench A/B vs the pre-slice binary (palindrome, medium+long, per eval-protocol-ambient-control) + validation refresh if >= +1%. Record dispatch-count deltas from the meter (metal-gpu-timestamp-meter) or a gputrace dispatch census as the primary receipt, tok/s as secondary.
