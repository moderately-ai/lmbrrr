---
id: 1783991617514262000-cb239473
at: 1783991617
---
SLICE 1 SHIPPED (cross-layer input-norm fusion, target model): each layer's input norm rides the previous layer's MLP residual add, final norm rides the last tail — ~24 serial dispatches/token off the rmsnorm census (61 -> ~37); MTP head folded into the same structure. M3 two-binary rotated A/B vs the parent commit: paired median +1.62%, mean +1.59%, ALL 6 rounds positive (1.33-1.71 band, non-overlapping). Battery green; ppl quality guard CLEAN and marginally better (deployed-vs-ref relative gap +10.7% vs +11.5% baseline, KLD 0.2527 vs 0.2565, top1 0.7595 vs 0.7557); goldens re-blessed same-pin with margin justification. Greedy floor now ~191 tok/s steady long-profile (182.6 session start -> 186.1 argmax -> 191.0). NEXT SLICES: sigmoid-gate fusion (out*sigmoid(gate), 2 dispatches x 6 attn layers -> 1, bit-identical variant by rounding sigmoid to bf16 pre-multiply) — building now; then strided rope/qk copies.
