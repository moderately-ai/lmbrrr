---
id: 1783894636125823000-28804cf8
by: "claude-main"
at: 1783894636
---
FALSIFIED at the micro gate (fork 00adb831, metal_benchmarks qmv-soa, quiet-ish load 2.4, warmed, MIN_DUR 1.5s): SoA plane-split (16B headers | 128B quants, perfect 128B cache-line alignment, dense header streaming) vs AoS 144B blocks — lm_head 119.0 -> 123.4 GB/s (+3.7%), dn_qkvz/mlp/attn/down +1-2%; outputs BITWISE-IDENTICAL on all shapes (clean experiment: same arithmetic, same schedule, layout-only change). AoS control reproduced the historical 119 GB/s exactly (rig consistent). CONCLUSION: the q4_K mv kernel is NOT layout-bound. With maxTotalThreads=1024 (no register cap), 62k TGs at lm_head (no occupancy cap), and now layout eliminated, the limiter is integer-dequant ISSUE RATE (~40 int ops per 8 weights) — the original int-pipe diagnosis was right and no data-layout change fixes arithmetic. ABSOLUTE-TIME CHECK that reframes the campaign: q4_K AoS is already the FASTEST head option per token (142MB @ 1.16ms) vs q8_0 (336MB @ 262 GB/s = 1.28ms) vs dense bf16 (1.42ms) — the q8_0 fallback is falsified a priori on absolute time (and was already measured -1..-6% in-stream earlier). CAMPAIGN CONSEQUENCE (recorded, not buried): Jump 2 (floor 280-300 via faster GEMV) is dead as designed; the surviving floor lever is reading FEWER weights — certified-subvocab-head is promoted to the keystone (head ~1.2ms of the 5.09ms realized greedy step; a 32k certified sub-vocab could cut ~1ms = +25% floor). Closing this ticket as falsified.
