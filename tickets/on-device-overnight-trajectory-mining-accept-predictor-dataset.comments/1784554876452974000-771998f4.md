---
id: 1784554876452974000-771998f4
by: "agent-program"
at: 1784554876
---
## PIPELINE SHIPPED + first harvest (2026-07-19)

### Artifacts
- `evals/trajectory/harvest.sh` — multi-class ORACLE_LOG + ACCEPT_PROBE runs
- `evals/trajectory/train_probe.py` — AUC report + conf+pos grid
- Oracle rows enriched: mean_conf, drafts, round_margin, used_pld/recycle

### First harvest (M3, soft default, N=64, 11 prompts)
| metric | value |
|---|---:|
| probe rows | 656 |
| oracle rounds | 164 |
| **oracle conf AUC** | **0.868** |
| **probe conf AUC** | **0.891** |
| conf+w·pos best | **0.896** (w=-0.3) |
| L15/31/47 RMS AUC | ~0.51–0.52 |
| L63 RMS AUC | 0.35 (anti) |

### Kill bar 0.85
**PASS on conf** (clears). **FAIL on layer RMS** (still dead).

### What this does NOT prove
- Does **not** revive `--skip-low-conf` (causal trajectory shift already REFUTED live).
- Does **not** unpark layer-depth early-exit (needs mid-stack head, not drafter conf).
- **Does** justify conf-based accept policy (already default soft adapt) and richer conf→margin maps.
- SPRINTER still needs features available **during** target forward, not only drafter conf.

### Next
Overnight harvest `reps>=20` on M3 idle; train non-linear probe; only then revisit P5 with mid-forward features.
