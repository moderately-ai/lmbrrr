---
id: 1783911796258995000-c95b0637
at: 1783911796
---
STAYS p3, now with a LANDMINE (2026-07-12 trace + upstream review). DEMOTION EVIDENCE: the post-wave trace at CPB=100 shows NO boundary hole — Active Cores stays saturated across all 3 command-buffer boundaries; the ~0.6-0.7ms hole the dossier diagnosed is GONE (the CPB flip claimed the whole prize). So the encoder-start all-fence wait costs us ~nothing now. DIAGNOSIS CONFIRMED against upstream source anyway: commands.rs command_encoder()/blit walk the ENTIRE prev_ce_outputs map and wait on every distinct prior fence, ignoring buffer identity — the per-buffer data to do first-touch-per-buffer exists and is simply unused at wait time (upstream #3532's 'minimum required fences' claim does not match its own code). LANDMINE: open upstream issue #3705 — deterministic missed-sync corruption in quantized generation at KV depth 8192, in EXACTLY this fence machinery. Our stack doesn't reach depth 8192 (128 new tokens, short prompts) so it's not biting us, but ANY fence change must be read against #3705 first and gated with the full sync battery (oracle + tree-check + barrier-probe, own slice, never bundled). VERDICT: opportunistic-only; do not spend a slice here until something makes the boundary waits matter again. If touched, fix #3705's root cause in the same pass.
