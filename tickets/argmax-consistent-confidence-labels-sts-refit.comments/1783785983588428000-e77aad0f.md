---
id: 1783785983588428000-e77aad0f
by: "claude"
at: 1783785983
---
FROM THE FULL PAPER RE-READ + FULL CODE READ (2026-07-11). CONFIRMED at code level (DeepSpec deepspec/modeling/dspark/loss.py, read in full): confidence-head training labels are accept_rate_3d = 1 - 0.5*L1(p_draft, p_target) - the analytic acceptance rate under T=1.0 REJECTION SAMPLING (paper Eq 8). Our verifier accepts on EXACT ARGMAX match - a different event - so the head is calibrated to the wrong question. This mechanistically explains the measured in-loop STS overconfidence (scheduler predicted >3.5 expected tokens/round, realized 2.13) alongside the BF16-vs-quantized fit-distribution gap. FIX (for round-3 training, or a head-only finetune on round-2 checkpoints): add a label mode to compute_dspark_loss - argmax-event labels: hard 1[argmax p_d == argmax p_t] (deterministic given aligned logits already in the cache), optionally smoothed by target_probs[argmax p_d] as the soft variant; select via config flag so the paper-default remains. Independently: STS must be refit on held-out ARGMAX-acceptance outcomes of the DEPLOYED config (the Modal evaluator already emits per-position acceptance labels - use its eval-split records). SECONDARY paper-parity notes from the same read: (1) loss_decay_gamma=4.0 gives w_k=exp(-k/4) vs paper exp(-(k-1)/8) at block 8 - twice-steep front-loading; likely fine at deployed widths <=5, now a recorded deliberate deviation; (2) scheduler is Algorithm 1 (sec 3.2.2), NOT "Appendix A" (Appendix A is the losslessness counterexample) - and under exact-argmax verification the early-stopping break is a throughput heuristic for us, not a correctness requirement: unconstrained admission search is legal (paper only earns that via its async variant). Docs referencing "Appendix-A scheduler" should be corrected when touched.
