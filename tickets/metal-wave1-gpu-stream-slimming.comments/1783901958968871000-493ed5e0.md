---
id: 1783901958968871000-493ed5e0
at: 1783901958
---
C1 LANDED (lmbrrr e3a6622, fork 8559194d): bf16-dst quantized GEMV variants for q4_K/q8_0/q6_K, mv + mc routes. Kernels gain a DT dst template param — F32 accumulation unchanged, store converts via the same static_cast<bfloat>(float) the cast kernel used, so bit-identical by construction. candle-core fwd_mv returns BF16 storage when src1 is BF16 and the variant exists; MixedLinear's to_dtype is now a no-op on those routes. ~100-130 cast_f32_bf16 dispatches/step gone from the stream. DEVIATION FROM TICKET, with receipts: the keep-f32 MixedLinear flag is NOT needed — the pre-build census showed every deployed consumer (target lm_head via MiniCpm forward, drafter lm_head + markov_w2 at session dtype BF16, all body projections) already receives bf16-rounded logits through MixedLinear's output cast; 'heads must stay F32' was based on a wrong premise about the argmax input dtype. F32-activation paths (CPU, --dtype f32 oracles, m>=8 tile-mm route) keep F32 kernels untouched. GATES: nextest 59/59; stub oracle passed (0.5/0.75, corrupt 0/3/5 invariant); tree-check passed (main 0.00000, alt 0.25, rollback 0.33); committed text BYTE-IDENTICAL vs the 93172dc7 binary on stub loop AND round-4 drafter stack. Perf attribution deferred to the package-level post-wave bench per ticket. Next: O1/O2 DeltaNet cat elimination + b+a fused dense GEMV.
