---
id: 1783902817707373000-dd0fa41b
at: 1783902817
---
O1+O2 LANDED (lmbrrr 7eb8eec, fork 7b9b7b7b): v2 prep/decode/prep_tree kernels take b/a via a separate ba buffer and read the raw qkvz GEMV output directly — the 3-way projection cat is gone (54 copy2d_bf16/step). in_proj_b+in_proj_a fused at load into one 2*heads-row dense GEMV whose output is exactly the kernel's [b|a] layout (36 -> 18 tiny GEMVs/step). Dense-protection respected: gates never row-concat into the quantized qkvz weight; apply_quantized_text_artifact rejects a manifest carrying one gate without the other. v1 fallback paths keep the packed layout via a 2-tensor [qkvz|ba] cat. GATES: nextest 59/59; stub oracle 0.5/0.75 invariant; tree-check passed with per-round deltas and tokens IDENTICAL to the C1 run; committed text byte-identical vs the C1 binary on stub and round-4 drafter runs, per-round stats (rounds/rollbacks/histograms) identical. Remaining in wave 1: C4 partial-rotary rope-in-place.
