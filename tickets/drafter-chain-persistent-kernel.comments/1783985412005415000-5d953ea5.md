---
id: 1783985412005415000-5d953ea5
at: 1783985412
---
BUILT AND MEASURED — premise half-falsified, real cause found, fix shipped. (1) Single-dispatch step shipped (fork 3d69e89a): packed 32-bit atomic argmax (scores are exact bf16 -> 16 ordered bits + 15-bit complemented index; works on every Metal GPU — MSL's atomic_ulong has no usable load/store), last-TG epilogue, self-cleaning interleaved scratch, vd<=32768 cap. Bitwise 8/8 arms + 200-round soak; goldens byte-identical across the pin bump. (2) M3 chain-eval VERDICT: fenced chain segment UNCHANGED at 3.43ms with 12 -> 6 dispatches — dispatch-boundary cost is ~FREE here, the boundary model is falsified at the chain level too. With the drain-fixed instrument the backbone reads a tight 6.20ms and fenced-sum ~= in-loop 9.83ms (propose is fully serial, no hidden overlap). (3) REAL CAUSE: execution latency — 573us/step in-situ vs 78us isolated = ~16GB/s effective on a ~150GB/s part; the kernel read q8 quants as 32 SCALAR BYTE LOADS per block with 2 latency-serialized rows/thread (isolated benching hid it by pipelining 64 chains per command buffer — instrument lesson: lone-dispatch latency never shows in deep-pipelined micro benches). (4) FIX SHIPPED (fork f2c7fa82): packed_char4/bfloat4 vector loads with strict sequential addition order (bitwise gate holds) + 128 TGs on the fused shape (one row/thread). vec-eval running on the M3 now — expectation: chain 3.43 -> well under 1ms if latency-bound diagnosis is right; the fenced ladder decides.
