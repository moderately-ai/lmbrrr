---
id: 1784082405003051000-4fe8be2e
at: 1784082405
---
RE-PRICED (2026-07-15, see spec-loop-economics-recovery): the fused MTP pre-fc kernel removed 3 of ~19 chain-step dispatches bit-identically and measured a dead wash in the unfenced production loop — dispatch-count reduction per se is ~free already (concurrent encoder). A persistent-kernel collapse of the chain is justified only by (a) unfenced evidence of real inter-dispatch GPU idle, or (b) removed memory traffic (keeping activations in registers/threadgroup across steps — the actual persistent-kernel win). Note the DSpark markov in-situ numbers (285us vs 78us isolated) came from that drafter's near-empty dispatches and do not transfer to the MTP chain whose dispatches carry real GEMV work. OTA note: neither llama.cpp nor MLX runs serial decode chains in one dispatch; both rely on concurrent encoders + barriers.
