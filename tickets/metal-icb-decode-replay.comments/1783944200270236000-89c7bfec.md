---
id: 1783944200270236000-89c7bfec
at: 1783944200
---
RE-PRICED by the megakernel agent (deep fanout, 2026-07-13) — mostly DOWNWARD, with one upgrade. DOWNWARD: (a) ggerganov prototyped Metal command-buffer replay (llama.cpp #14570) and measured NO improvement — pipelined submission already masks CPU encode; he also concluded MTLIndirectCommandBuffer 'won't cut it' for compute replay (#14482 comment). (b) ICB compute commands are CONCURRENT-only; dependent dispatches still need memoryBarrier = the same full drain — ICBs cut CPU encode, NOT GPU drain. Our wave-2 measurement agrees (host encode already shadowed). (c) Full spin-barrier megakernel is UNSAFE on Apple: Sorensen et al. OOPSLA 2021 + Apple's own OBE-violation test (CAS spin-lock starves on A12/A14; non-preemptive linear scheduler); no forward-progress guarantee exists or is queryable; TDR kills starved kernels. Memory-model half IS solved (Metal 3.2+ device-scope fences, 4.1 acquire/release) — ordering != scheduling. UPGRADE: the GPU-side drain in a dependency chain is REAL, ecosystem-unaddressed, and partially recoverable via STARVATION-FREE patterns (FidelityFX-SPD last-finisher completion counters; SPAA'25 decoupled-fallback — both proven working on Apple silicon). Realistic prize ~0.15-0.3ms/token (~5-11%). NEXT STEP if pursued: the agent's Stage-1 probe — single-threadgroup N-chained-GEMV loop vs N real dispatches (zero forward-progress risk, ~a day) to size the true boundary-drain component before any cross-TG sync is written. Ticket scope updated: 'ICB replay' -> 'Stage-1 drain probe, then starvation-free partial fusion'; priority stays p3.
