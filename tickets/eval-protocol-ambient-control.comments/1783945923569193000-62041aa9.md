---
id: 1783945923569193000-62041aa9
at: 1783945923
---
AMENDMENT 1 — DECISION STATISTIC (the original body said 'non-overlapping ranges', which with 3 medians is too weak to certify +2% claims): the standard test is PAIRED. For suite-based A/Bs: per-question paired deltas (same question, both arms, same round), bootstrap-resample questions 10k times, ship iff the 95% CI of the mean delta excludes 0 AND the point estimate >= +2%. For bench-based A/Bs (no natural pairing): pair by palindrome position (run i of arm A vs adjacent run of arm B), report min/median/max per arm, and require median delta >= +2% with the WORST A-run beating the BEST B-run only for claims made off 3 reps; for anything finer, raise reps to 6 or move to the suite. Sample-size honesty: per-class 3 x reps 3 = 18 questions resolves ~2% effects at best; claims of +1-1.5% (e.g. fused-argmax ship bar) need per-class 5 or reps 5 — the ticket making the claim pays for the reps.\n\nAMENDMENT 2 — SUSTAINED vs BURST (one-time characterization, part of this ticket's deliverable): the memory protocol records throttling 64->48 tok/s under sustained load but standings quote burst numbers. Run 10 minutes of continuous decode (--max-new-tokens 1000 back-to-back runs), plot tok/s per run, record throttle onset time and the sustained plateau. Decide and write into the campaign log WHICH number standings quote (recommend: quote both as 'burst / sustained'). Cross-link: eval-latency-surface step 3 gathers part of this.\n\nAMENDMENT 3 — VERSION LOGGING: every session log (this protocol's RECORD step) must include: macOS build (sw_vers), Xcode/metal toolchain (xcrun metal --version), fork pin sha (Cargo.toml rev=), lmbrrr commit, and for cross-stack evals the llama.cpp commit + mlx/mlx_lm versions. Cross-session comparisons without matching version stamps are void.
