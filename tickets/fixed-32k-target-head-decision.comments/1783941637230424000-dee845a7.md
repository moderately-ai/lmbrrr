---
id: 1783941637230424000-dee845a7
at: 1783941637
---
EXPERIMENT DONE — RECOMMEND AGAINST SHIPPING (2026-07-13). Built --target-head-vocab-size N knob (flag-gated, default off; restrict_lm_head_vocab + argmax remap, greedy path; full 248k ranking = frspec-observed ++ zero-freq tail). Two findings both moved the answer against it:

SPEED: +13.5% steady bench greedy at 32k — NOT the +30-40% the byte-fraction estimate predicted. The estimate conflated head BYTES (41% of device reads) with head TIME (~14% of bench wall). Saturates at 32k (16k/8k give no more — head stops being the bottleneck). N=248094 permutation head reproduces full-head speed (-0.1%) = remap validated. Full sweep: 131k +6.7%, 65k +10.1%, 32k +13.5%, 16k +13.5%, 8k +15.8%.

QUALITY (clean argmax-coverage curve over full-head greedy traces, isolates out-of-vocab from re-quant noise): 32k aggregate 97.7% coverage = 2.3% token error, but CONCENTRATED: math/translation/coding/writing 100%, summarization 93.8% (6.2% err), qa 92.4% (7.6% err). Errors land on HIGH-SALIENCE tokens: proper nouns (Kecil -> dropped, summary reroutes) and rare sub-tokens (peacock -> peafinn). Summarization stuck at ~94% even at 64k (open-vocab names never in any head). Confound resolved: coding's text divergence was 100%-coverage = pure re-quantization tie-flip, NOT out-of-vocab.

VERDICT: +13.5% for visible quality damage on 2/6 classes is a weak trade vs the exact levers still available (fusion +4% banked + more; mc verify-parallelism; adaptive-sync). The exactness commitment is worth more. RECOMMEND: keep the knob as an experimental tool, do NOT default it, do NOT pursue distillation to shave the 2% (not worth compute at +13.5%). META-INSIGHT for the campaign: head only 14% of bench-wall despite 34% of GPU-exec time => bench greedy is host/sampling-bound; the real bench-mode lever is the argmax/readback/device-chain host path, not more kernel work. Decision deferred to user; my rec is against.
