---
id: 1783945217388724000-43a9dc9c
at: 1783945217
---
DECISION PACKAGE (for the user; executor should NOT act without sign-off). Measured this session: --target-head-vocab-size 32768 --target-head-vocab-ranking target/head-vocab-ranking-full.json gives +13.5% bench greedy (saturates by 32k; the byte-fraction estimate overpredicted 2.3x because the bench loop is host/sampling-bound at the margin). Quality: argmax coverage 93.8% summarization / 92.4% qa — dropped tokens include proper nouns; NOT exact, fails our default correctness bar. RECOMMENDATION: reject as a default; keep the flags as an opt-in degraded-latency mode only. Ranking artifact regeneration (if target/ is lost): rebuild via the head-vocab ranking script path documented in the campaign log (observed-frequency ids 62123 ++ zero-freq tail = full 248094 permutation). If accepted despite the rec, required follow-ups: quant-quality ladder on the restricted head + suite validation with per-class means + explicit user-facing flag docs.
