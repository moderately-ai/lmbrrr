---
id: 1784057145307389000-ccfb40ff
at: 1784057145
---
OBSOLETE + QUESTION ANSWERED (2026-07-14, live xctrace-export session, artifacts m3:~/gpu-runs/run-004). The macOS 27 headless xctrace route (record --attach + export --xpath metal-gpu-intervals / gpu-performance-state-intervals) delivers everything this ticket wanted to build, with zero engine code. MEASURED (M3, steady 8.6s window, 1704 tokens, live decode at 197 tok/s): GPU busy = 99.7% of wall (5.043 vs 5.06 ms/token); total inter-CB gap = 0.017 ms/token; the only gaps >20us are the 7 bench-iteration boundaries; GPU perf state = MAXIMUM throughout (single 435us induced-minimum blip). VERDICT: the '1.55ms idle' (dossier) and '~0.6ms scheduling gap' (replay decomposition) were measurement-methodology artifacts — replay busy at pinned perf states is not comparable to live wall. Greedy decode on the M3 is fully kernel-bound at max clocks; with DRAM at 88.6% of peak inside the kernels, remaining greedy headroom is single-digit-percent kernel-interior efficiency only. The parse recipe (interval-union + gap histogram over the exported XML with id/ref resolution) lives at m3:~/gpu-runs/run-004/parse_gaps.py and in the metal-live-gpu-probe skill. Closing: build nothing; the instrument exists in the OS.
