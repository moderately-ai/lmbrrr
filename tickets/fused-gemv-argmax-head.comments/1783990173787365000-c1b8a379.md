---
id: 1783990173787365000-c1b8a379
at: 1783990173
---
BUILT AND GATED (fork d3b12763 + lmbrrr wiring; M3 A/B in flight). Step-1 tie rule recorded: candle's indexed reduction (reduce.metal indexed::operator>) is max value, LOWEST index on exact ties; the deployed head path stores BF16 logits (bf16-direct mv), so fast_argmax compares bf16 values — the fused kernel mirrors nr2sg2's per-row arithmetic byte-for-byte across 8 row tiles/TG, rounds each sum to bf16 before comparing, ties to lowest index. GATE 1 green (fork bench task argmax-head): fused index == production-kernel+host-argmax on 24 varied activations AND manufactured exact ties (winner row duplicated above -> winner holds; duplicated below -> lower copy wins). Depth-1 timing on the M4: fused pair 3.93ms vs mv-alone 4.26ms — faster than the projection alone by skipping the 0.5MB logits write, before counting the argmax dispatch it deletes. GATE 2 green: committed text byte-identical on/off (130/130, tides). Wired behind LMBRRR_FUSED_ARGMAX=1 in the greedy device chain (supports_fused_head_argmax = Metal + q4_K head; head-restriction remap composes unchanged). M3 now running: kernel gate + battery bless/byte-compare + 6-round rotated long-profile A/B; ship default-on at >= +1% non-overlapping per the ticket bar.
