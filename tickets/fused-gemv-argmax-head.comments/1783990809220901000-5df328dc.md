---
id: 1783990809220901000-5df328dc
at: 1783990809
---
SHIPPED DEFAULT-ON. M3 rotated production A/B (6 paired long-profile rounds, arms alternated): paired median +1.19%, mean +1.00%, ALL rounds positive (min +0.21, max +1.80) — clears the >=+1% bar for an exact change. Steady greedy ~188.5 tok/s flag-on vs ~186.1 off in-session. Committed text byte-identical on/off; goldens unchanged (no re-bless — the change is exact). M3 kernel-gate depth-1 note: fused pair 2627us vs mv-alone 2634us — at depth 1 the commit floor dominates both arms; the production win is the deleted argmax dispatch + barrier + 0.5MB logits round-trip, which only the e2e A/B prices. LMBRRR_FUSED_ARGMAX=0 is the opt-out. Remaining ticket scope (spec-verify per-position argmax_tokens variant) stays open as a follow-up; the greedy chain is done.
