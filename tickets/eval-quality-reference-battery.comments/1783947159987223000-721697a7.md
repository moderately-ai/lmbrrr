---
id: 1783947159987223000-721697a7
at: 1783947159
---
METRIC UPGRADE (2026-07-13 research pass): (1) add KL-divergence-vs-F16 alongside ppl as the primary quant gate — Unsloth's 150+ run Qwen3.5 GGUF campaign (unsloth.ai/docs/models/qwen3.5/gguf-benchmarks) standardized on KLD and it is more sensitive than ppl for quant damage; implementation: the lmbrrr ppl command should also emit mean KLD (full-vocab KL of quantized logits vs bf16 logits at each position — both arms already computed in the battery, this is one extra reduction). (2) Their tensor-level finding transfers directly: 4-bit damage concentrates in the GDN-SPECIFIC tensors (ssm_alpha, ssm_beta, attn_gate equivalents — our ba/gate projections) — if our per-linear sensitivity table predates this, cross-check it. (3) Task-eval settings: use OpenBMB's endorsed sampling (temp 0.7, top-p 0.8, top-k 100, repeat-penalty 1.05) for any sampled quality comparison so numbers are ecosystem-comparable; greedy for the agreement battery as specced. (4) config mamba_ssm_dtype=float32 confirms vendors keep GDN state f32 — our f32-state decision matches the vendor; any future state-precision experiment needs a long-generation drift eval (cite this on bf16-recurrent-state-storage if reopened).
