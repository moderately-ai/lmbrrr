---
id: 1783758895013320000-72b43f67
by: "claude"
at: 1783758895
---
RUNNER INTEGRATION LANDED, MEASURED, VERDICT RECORDED (2026-07-11, lmbrrr d2af2a5). --tree wires propose_branching into the spec loop (flattened verify, longest-path acceptance, per-round rollback_tree, winner-path drafter ctx, --tree-band gating on calibrated pos-0 survival). Also fixed a latent rank-0 index_select bug in propose_branching's alt chain - the branch path had never actually executed (parity harness only covered default). MEASURED RESULTS (math, scheduled, quantized target, controls reproduced 146.6/146.7): ungated tree 114.4 tok/s tau 2.048 (35 tree rounds, 5 alt wins +8 tokens, but reconstruct-noise tie flips COST main acceptances - tau net down); band-gated 0.3-0.7: tau_eff 2.133 -> 2.207 (+3.5%, mechanism confirmed working) but 134.5 tok/s vs 146.7 chain. Tides: tree == chain (133.6 vs 132.9, few tree rounds). CONCLUSION: the mechanism note's prediction is CONFIRMED by measurement - host-composed branch restart (+~8ms/tree round: wider verify l=2w+1, per-layer closed-form seeding, unconditional rollback_tree) eats the tau gain; positive EV requires the in-kernel segment restart (fork work: segment-descriptor input to gated_delta_chunk so one dispatch runs both segments with the chunk-end-update-formula restart in registers, ~0.3-0.5ms/branch) AND ideally a cheaper winner install for the full-main-accept case (skip closed-form when accepted==w on main: state_main is already exact - a free 2-3ms on most rounds; TODO'd). Acceptance disposition: draft-tree construction DONE; tree verification DONE (segment decomposition chosen over per-path re-advance, measured, documented); longest-path + exact state DONE (tree-check gate: main bitwise, alt within rollback envelope); tau_eff vs chain reported (target tau_eff>=3.0 NOT met with round-1 drafter - ceiling ~2.8 per mechanism analysis). Ticket stays open on exactly two items: (a) in-kernel segment restart in the fork, (b) exact-state fast path for full-main-accept. Both are prerequisites for tree EV, worth doing after single-command-buffer-decode-forward shrinks the round floor.
