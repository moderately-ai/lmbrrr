---
id: 1783758161529634000-8b8171ba
by: "claude"
at: 1783758161
---
TARGET-SIDE MACHINERY LANDED (2026-07-11, lmbrrr 09041e7). Plan items (1)-(3) model-half done: RotaryEmbedding::apply_with_positions (siblings share positions); TreeForward ctx plumbed through DecoderLayer; FullAttention takes per-token rope positions + ancestor mask (built in Qwen35TextModel::tree_mask); GatedDeltaNet::forward_tree runs the fused chunk kernel per segment with the alternate seeded from the closed-form post-anchor state of the main segment's own capture (2 dispatches + tiny reconstruct per layer, zero weight re-reads); select_tree_state installs the winner from the right capture; TruncatableKvCache::compact_rows + rollback_tree handle winner-path KV (truncate for main, compact for alt). minicpm wrappers: forward_tree_all_logits / rollback_tree. GATE: new tree-check subcommand (permanent) - main-branch rows BITWISE EQUAL to plain chain verify (max delta 0.00000 across w=1/3/5 x 4 rounds - mask, rope, seg1 exact); alt rows max 0.44 and rollback probes (both branches) max 0.25 vs chain references - inside the 0.75 partial-accept envelope, same closed-form-vs-kernel FP class, so NO new noise bound derivation needed. REMAINING: runner integration in dspark_drafter_run (--tree flag: propose_branching alt chain, flattened verify via forward_tree_all_logits, longest-path acceptance vs both branches, rollback_tree commit, oracle + measurement per protocol), scheduler mid-band branch gating (0.3-0.7 pos-0 survival), tau_eff vs chain tau report per prompt class. Cost note for go/no-go: tree at w adds verify l from w+1 to 2w+1; from the current bf16-direct table (4.8/8.5/9.6/10.6/11.8/12.9/14.0/15.2/16.4/17.6/18.6/19.6) w=3: 10.6->14.0ms (+3.4ms/round) buys the runner-up branch; break-even if it adds >= ~0.65 tokens/round vs greedy-q4k 196.9 bar.
