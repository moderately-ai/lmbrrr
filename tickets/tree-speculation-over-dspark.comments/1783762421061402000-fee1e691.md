---
id: 1783762421061402000-fee1e691
by: "claude"
at: 1783762421
---
IN-KERNEL RESTART SEQUENCED AFTER ROUND-2 TRAINING (2026-07-11, quantified). The measured band-gated tree is 136.1 tok/s vs 146.9 chain; host-composed overhead per tree round is ~8-9 ms of which the in-kernel segment restart can eliminate at most ~2-3 ms (the rest is unavoidable wider projections/lm_head rows and the partial-accept installs the fast path already skips). Perfect elimination recovers ~2% -> ~139 tok/s, still below chain with the round-1 drafter's mid-band mass. Tree EV therefore requires tau improvement first (round-2 training, gated on user sign-off; the corpus-scaling doc projects the mid-band getting materially richer). Doing the WY-solve ancestor-masking kernel surgery now would be premature on a path that cannot pay off before round-2 lands. Ticket releases the claim and stays open on exactly: in-kernel segment restart (WY ancestor mask + per-segment decay bases + dual capture emission in gated_delta_chunk.metal), to be picked up WITH round-2's drafter. Mechanism, runner, gates, and fast path are all landed and measured; --tree remains opt-in.
