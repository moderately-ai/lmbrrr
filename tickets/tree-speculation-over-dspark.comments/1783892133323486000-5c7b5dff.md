---
id: 1783892133323486000-5c7b5dff
by: "claude-main"
at: 1783892133
---
K4 LANDED (lmbrrr 013042c, fork b223baeb): in-kernel tree segment restart. forward_tree's v2 path is now ONE fused prep/core/epilogue dispatch per DeltaNet layer — the alternate segment restarts inside the core kernel from the post-anchor register state; state_mid returns as cap_alt.s0, replacing the per-layer host closed-form seed + second kernel call. Gates: tree-check PASSED (main rows bit-exact 0.00000, alt 0.25 / rollback 0.28 vs 0.75 eps), stub oracle 0.25/0.75, 59/59 tests (gate exposed+fixed a latent compact_rows bug: copy() keeps the strided narrow layout, slice_set rejected multi-head KV compaction — alt-win rollback was BROKEN in production shape until now). BREAK-EVEN ON FUSED TABLE (q404 math / q124 coding, 3 reps rotated, moderate load): chain 226.6/181.1; tree-always 176.2/167.8; band 0.3,0.85: 197.5/181.7; band 0.45,0.75: 209.1/175.2. Mid-band tree on coding: tau 2.31->3.10 (+34%) yet tok/s TIES chain — the l=2w+1 chunk premium (+2-3ms) exactly eats the extra ~0.8 tokens/round; math stays negative (position-0 rarely ambiguous). DECISION: tree default stays OFF. The tau machinery works and is now cheap on the kernel side; it becomes EV-positive if/when K6 (q4k-soa-plane-repack) shrinks the chunk intercept. Re-run this break-even after K6.
