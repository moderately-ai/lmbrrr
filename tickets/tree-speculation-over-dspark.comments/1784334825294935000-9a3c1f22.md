---
id: 1784334825294935000-9a3c1f22
by: "claude"
at: 1784334825
---
C1 VIABILITY RE-MEASURED on the CURRENT Bonsai width-4 drafter (2026-07-17, margin-3.0, planar mm2d, M3). The acceptance run-length distribution is PROMPT-DEPENDENT, which decides C1:

- Easy prompt ("Explain quantum computing"): round_accepted ~82% cap-saturated (accept=4) — NO mid-chain dip, C1 has no room (the value is all at positions 5+, i.e. width-7, parked).
- Harder prompt ("Explain relativity + worked example", 64 rounds): accept=0 5%, =1 11%, =2 16%, =3 12%, **=4 (cap) 56%**, mean 3.047. So 44% of rounds reject WITHIN the 4-token chain, rejection peaking at position 3 (16%). Genuine mid-chain uncertainty.

So C1 (confidence-placed branch, NOT root) is NOT refuted for the current drafter — on diverse/hard prompts there IS a per-position dip (modal reject at position 3) to place the ONE branch, partially meeting the parked "unlock = lower pos-0 survival" condition. Two caveats that keep EV borderline (unchanged from the prior root-tree measurements): (1) the l=2w+1 verify premium persists — for Bonsai GQA a tree at m=8 routes DeltaNet to gated_delta_v2 (l>=8), and the F1 occupancy win does NOT reduce the tree cost (tree stays ~1.16x verify: matmuls flat m=1..8, only the DeltaNet chunk scales ~l); (2) the branch only pays when the drafter's #2 token at the dip matches the target argmax AND the round would otherwise have rejected there. NET: rescue ~16% of rounds x ~1-2 tokens vs ~16% verify premium on ALL tree rounds — genuinely marginal, must be MEASURED not projected. C1 BUILD SCOPE: (a) propose exposes per-position top-2 from the drafter's block logits (already computed — the diffusion block gives all-position logits in one forward; the alt continuation after b_k needs either a 2nd drafter forward or the independent per-position #2 as an approximation); (b) Sequoia-DP topology on the measured accept vector = depth-4 main + a 3-node branch at the dip; (c) reuse the BUILT tree machinery (forward_tree_all_logits, tree_mask, rollback_tree, K4 in-kernel restart) generalized from 2-branch-at-root to 1-branch-at-position-k; (d) gate: tree-check bitwise (branch pruned == chain), oracle; (e) A/B tree vs chain on hard+easy prompt classes, adopt only if net-positive. Verdict pending the build+measure.
