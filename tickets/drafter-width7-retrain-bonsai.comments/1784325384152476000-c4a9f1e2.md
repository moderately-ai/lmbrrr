---
id: 1784325384152476000-c4a9f1e2
by: "claude"
at: 1784325384
---
WIDTH-7 LANDED + A/B = CATASTROPHIC REGRESSION, but the confound is a DEPLOY-PATH bug, not the width idea (2026-07-17). Pipeline ran end-to-end: train (6 epochs, loss 2.65->2.30) -> convert_dspark_gguf.py -> M3. **M3 rotated A/B (3 reps): width-7 exact 5.15 tok/s (accept 0.187/round), width-7 margin-3 5.24 (accept 0.21), width-4 margin-3 19.17 (accept 3.448, reproduces the 19.18 baseline = harness sound).** Width-7 accept ~0.19/round = essentially ZERO; margin barely helps (0.187->0.21) -> proposals are UNCORRELATED with the target, not near-misses.

CONFOUND CLEARED via the DeepSpec eval (reads the safetensors checkpoint directly, bypassing my GGUF conversion; UNPACKED target): **gsm8k accept_len 1.71, position-0 accept 0.4274; mt-bench 1.24.** So the drafter is WEAK-BUT-FUNCTIONING in its native format/target; the M3 GGUF path collapses it 1.71 -> 0.19 (~9x). TWO separate problems:
1. TRAINING is UNDERTRAINED: accept_len 1.71 (pos-0 43%) is low (good drafters hit 60-80% pos-0). 38k samples / 6 epochs on a fresh 2-layer drafter is too small (MiniCPM round-4 used 400k for tau 4.41). Even with a fixed deploy path, this drafter would not beat the shipped width-4 -> the retrain needs a BIGGER CORPUS.
2. DEPLOY PATH is BROKEN (the immediate blocker): converts+loads but proposals collapse to a generic token prior (LMBRRR_SPEC_DEBUG shows plausible common tokens 279/13/271/13962 repeating across rounds regardless of context = feature-conditioning fallback). RULED OUT: dropped/unmapped tensors (all 31 mapped, no NOTE), shape/transpose bug (fc [25600,5120]=5taps*5120->5120, markov [256,248320], confidence [5376,1]=5120+256->1 all CORRECT). This is the FIRST end-to-end use of convert_dspark_gguf.py (the shipped width-4 came AS GGUF from prism), so the path is unvalidated.

LEADING HYPOTHESIS (A): TRAIN/DEPLOY TARGET MISMATCH. The drafter conditions on the target's tap hiddens [1,16,31,46,61]; it TRAINED on prism-ml/Ternary-Bonsai-27B-unpacked (dense bf16) hiddens but DEPLOYS against the Q2_0 GGUF hiddens. The unpacked-vs-Q2_0 hidden fidelity was NEVER verified (explicitly OWED in the ops log). If Q2_0's 2-bit error shifts the hiddens beyond the drafter's conditioning tolerance, pos-0 collapses 43%->19% and compounds to accept_len 1.71->0.19. prism's width-4 drafter is robust to Q2_0 (accept 3.45) -> likely trained on the Q2_0-matched target, unlike ours. ALT (B): a value-level conversion corruption the shapes don't expose. DECISIVE NEXT DIAGNOSTIC: on Modal, load the safetensors checkpoint AND the converted GGUF, compare tensor VALUES (rules conversion in/out); AND run the owed unpacked-vs-Q2_0 one-tensor + tap-hidden fidelity check. If conversion is clean + hiddens differ -> confirmed (A) -> retrain with prepare_cache pointed at the DEPLOYMENT-FAITHFUL target (Q2_0-matched), on a bigger corpus. If values differ -> fix the converter.

STATUS: width-7 lever BLOCKED pending (deploy-path fix) + (bigger retrain). The width idea is NOT refuted (verify m=8 is free, cap-truncation demand is real); the retrain EXECUTION has two fixable gaps. Board: width-7 -> blocked, not done. Artifacts kept: /vol checkpoint step_438/step_latest, /vol/models bf16 GGUF, M3 block7-Q8_0.

DEEPER DIAGNOSIS (same session, code-level): conversion FULLY ruled out. Verified the tensor mapping is bidirectionally consistent — converter checkpoint markov_w1->GGUF markov_head_a, and lmbrrr GGUF-load markov_head_a->markov_w1 (dspark.rs:663); same for markov_w2/markov_head_b, and fc. GGUF ne dims reverse to the correct candle logical shapes (markov_w1 [248320,256] for index_select dim-0; if it were transposed the drafter would ERROR on the token-id index, not run). log_snr_bias is Option (None for our log_snr=False drafter, correct no-SNR path). So NO conversion arrangement bug. NARROWED via the LMBRRR_SPEC_DEBUG trace: proposals VARY with the anchor across rounds ([198,248069,271,...] vs [725,25,271,...]) so the MARKOV/anchor conditioning (prev-token->bias) WORKS, but they're generic and repeat patterns ("271,13962,279,279" across rounds 3-5) -> the TAP-FEATURE (context) conditioning specifically is degraded, i.e. the drafter is dominated by its markov prior because the fc(tap-hiddens) contribution is off. That isolates it to the tap-feature path: either (A) the Q2_0 target's tap hiddens differ from the unpacked-target hiddens the fc trained on (the owed fidelity check), or the feature capture/injection differs from DeepSpec. DECISIVE TEST: Modal, run the target forward on BOTH prism-ml/Ternary-Bonsai-27B-unpacked and a Q2_0-dequant of the same, capture layers [1,16,31,46,61], compare hidden magnitudes/cos-sim. If they diverge -> (A) confirmed -> retrain prepare_cache on the DEPLOYMENT-FAITHFUL (Q2_0-matched) target. This is the same class as prism's drafter being robust (likely trained Q2_0-matched) while ours (unpacked-trained) is not.
