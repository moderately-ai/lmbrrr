---
id: 1783950722692143000-a97f4561
at: 1783950722
---
FIRST CENSUS (2026-07-13, directional — load 4.6, iterations 2, medium profile; instrumentation is now ALWAYS-ON in the report JSON, no env flag: readback_wait_seconds field, commit pending). Per-token host budget at steady state (100 tokens, 336 tok/s under load): model encode+queue 1.246 ms | argmax encode 0.005 ms | READBACK WAIT 1.813 ms | callback+bookkeeping ~0 | total decode wall 3.066 ms.\n\nINTERPRETATION (important — do not misread the 1.81): readback_wait measures host time blocked in the every-8 flush, which is dominated by WAITING FOR QUEUED GPU WORK — the GPU is busy for most of it, so it is overlap, not waste. The recoverable prize is the DRAIN TAIL: after the wait returns, the GPU sits idle while the host iterates ids + re-encodes ~100 dispatches to the first commit (~1 ms per flush at CPB=100) = ~0.125 ms/token at N=8 (~3.5%). What the census DOES settle: (a) the host has ~1.8 ms/token of slack — the loop is GPU-bound at steady state, so host-side micro-optimizations (detokenize, bookkeeping) are worthless; (b) encode cost is 1.25 ms/token = the host could NOT keep a 2x faster GPU fed — encode cost becomes the wall on any big kernel win, raising the elementwise-fusion lane's long-term value; (c) ROUTING per the ticket rules: readback bucket >> 0.3 -> deferred-readback proceeds, but its EV is the drain-tail math above, NOT 1.8 ms — expect ~+2-4% from N=32, plus the async SharedEvent path removing the drain entirely. Re-run this census on a QUIET machine for the record (numbers above carry load noise), then close.
