---
id: 1783992862377569000-d85a6f1d
at: 1783992862
---
QUIET CENSUS (M3 referee, long profile, async readback + fused argmax + norm fusion era, 191.6 tok/s steady, 5.20 ms/token wall): decode_model (host in forward calls) 4.200 | readback_wait 0.992 | sampling 0.003 | callback 0.000 | bookkeeping 0.002 ms/token; jitter p50/p95 5.62/5.86 ms. SETTLED: (a) detokenize/sampling/bookkeeping are ~0 — host micro-optimization permanently worthless (routing rule: no detokenize item); (b) with GPU busy ~3.65 ms/token at ~93% of the bandwidth roof (see eval-deltanet-sdpa-roofline), the wall carries ~1.55 ms/token of GPU IDLE — the entire remaining greedy prize (+~40% ceiling, 191 -> ~274); (c) the idle is NOT intrinsic encode cost (wave-2 measured pure encode ~1.25 ms/token < 3.65 GPU busy — the host could keep the GPU fed): decode_model's 4.20 ms/token must contain BLOCKING inside forward calls (pool/lock/backpressure interactions with the async run-ahead), i.e. the attribution question has moved INSIDE the forward call. NEXT INSTRUMENT (routing): metal-gpu-timestamp-meter (p3 -> the natural next census: per-command-buffer busy/idle directly measures the 1.55 ms gap and where it sits), plus an xctrace Time Profiler pass on the M3 for the blocking frames. Census deliverable complete — closing.
