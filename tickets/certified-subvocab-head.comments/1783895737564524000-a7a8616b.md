---
id: 1783895737564524000-a7a8616b
by: "claude-main"
at: 1783895737
---
FALSIFIED — both variants, offline simulation on 844 real decode hiddens (6 classes, layer-23 trace + exact offline final-norm; harness validated: 0.96 argmax agreement vs the production q4k-head trace, logit scale matches). (1) k-means C=2000 Cauchy-Schwarz cluster bounds: rows-scored fraction 0.996 — k-means degenerates on the 248k table (median cluster size 2, one 111k blob), and slack |h|*r ~ 140*0.4 = 56 logits vs real gaps 1-10. No C fixes it: certification needs r < ~0.03, far below row spacing. (2) SVD-Softmax W=128 preview + FEXIPRO per-row tail norms: exact fraction 0.994. Structural cause: decode queries are 91% OUTSIDE the table's top-128 subspace and the table spectrum is nearly flat (row tail-norm/norm median 0.77) — the head runs at cosine ~1e-3, so every norm-product bound is ~50-100x the signal. Bit-exact geometric certification at d=1024 on this tied multilingual table is structurally hopeless, not tuning-limited. WHAT SURVIVES: only the NON-exact option — a fixed FR-Spec-style 32k target-head sub-vocab (would cut ~1.05ms = +20-25% floor) but it changes committed outputs, so per campaign policy it could only ship behind a flag with a quality report; that is a product decision, not an optimization. Closing as falsified; simulation scripts at scratchpad k3_sim.py / k3_svd_sim.py, traces k3-trace-*.json.
