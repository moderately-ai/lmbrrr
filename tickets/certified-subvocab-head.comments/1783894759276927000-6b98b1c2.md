---
id: 1783894759276927000-6b98b1c2
by: "claude-main"
at: 1783894759
---
GATE PASSED (2026-07-12, measured): isolated q4_K lm_head GEMV = 1.16-1.20ms (dispatch-level bench, 142MB @ 119-123 GB/s — and q4_K is already the fastest absolute format: q8_0 1.28ms, bf16 1.42ms, so no format swap competes) + argmax ~0.24ms/round (K4-era oracle report: 11.9ms/50 rounds), vs realized greedy step 5.09ms (r4k5 refit, n=596) — head+argmax ~28% of the step; post-K5 decomposition shows non-model 2.9ms vs model stack 1.4ms. EV at ~18% vocab scored: head 1.2 -> ~0.25ms + <2% fallback amortized ~+0.03 => ~-1.0ms/step = +25% floor (bench 247 -> ~310). Chunks benefit MORE (head read once per l-row chunk) which also re-opens the tree break-even. STATUS: promoted to campaign keystone after q4k-soa-plane-repack falsified (schedule, layout, and format classes all dead — reading fewer weights is the only remaining floor lever). NEXT (offline, before any Metal): k-means C~2000 over the tied embedding + centroid/radius artifact, then bound-tightness simulation on traced decode hiddens to validate the 18%-scored / <2%-fallback claims at d=1024; SVD-Softmax+FEXIPRO fallback design if bounds are loose.
