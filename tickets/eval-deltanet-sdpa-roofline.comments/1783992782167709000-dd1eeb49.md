---
id: 1783992782167709000-dd1eeb49
at: 1783992782
---
PAPER TRAFFIC MODEL (step 1, posted before benching per plan) + VERDICT. Per-token decode traffic on the deployed q4k-full-text config: weights 457.9 MB (deltanet stack 260.1 + attn 54.9 + q4k head 142.9), DeltaNet recurrent+conv state r/w 39.5 MB (18 layers x 2.20 MB: the f32 [16,128,128] state is 1.05 MB/layer each way), sdpa KV 12.3 MB @1k ctx (49.2 @4k, 196.6 @16k) -> TOTAL ~510 MB/token @1k. Bandwidth floor at the M3 Pro's 150 GB/s: 3.40 ms/token = 294 tok/s. Measured kernel-only in-loop greedy: ~3.65 ms -> the aggregate GPU execution runs at ~93% OF THE MACHINE ROOF. VERDICT per this ticket's own gate (>=75% -> close): 'optimize DeltaNet/sdpa kernels' closes PERMANENTLY — no kernel class can be meaningfully off-roof when the weighted aggregate is 93% (per-kernel micro tasks would only re-partition the same total; skipping them, the aggregate is the receipt). COROLLARY that re-ranks the campaign: the greedy wall (5.23 ms at 191 tok/s) carries ~1.6 ms/token of NON-KERNEL time — host/encode/queue gaps — worth up to +40% (191 -> ~274) if fully closed. That is eval-host-loop-budget-census's territory, now the top greedy lever. Secondary note: the model also prices ideal m-flat verify(2) at ~3.7 ms — spec economics remain kernel-compute-bound as established, not bandwidth-bound.
