---
id: 1784552504032401000-882accd8
by: "agent-program"
at: 1784552504
---
## MEASURED (2026-07-19) — already near floor; standalone EV low

### Current rollback cost (M3, Q8_0, prose N=128, 3 reps)
| arm | tps | rollback ms/round | rollback share | partial rounds |
|---|---:|---:|---:|---:|
| **default soft** | 20.17 | **1.97** | 0.9% | 6/29 |
| exact | 15.35 | 4.74 | 2.2% | 21/38 |
| exact + UNFUSED_RECONSTRUCT | 14.33 | **19.84** | 8.4% | — |
| tree N=64 | 14.86 | ~12.1 | 5.0% | — |

### Interpretation
1. **Closed-form fused reconstruct is already shipped** and 4× cheaper than the tensor-op reference (4.7 vs 19.8 ms/rd). Not re-advance.
2. Default OP rollback is **~2 ms/round**. Zeroing it entirely ≤ **~1% tok/s**. Kill bar was "saving < 1 ms/round" → **borderline KILL as default-path lever**.
3. Per-position S emission would trade verify-kernel registers/BW to make rollback a select. Verify is 83% of the round — taxing it to save 2 ms rollback is the wrong trade on the default path.
4. Tree still pays more rollback; if tree is revived with B7, revisit emission as a **tree enabler**, not a chain default win.

### Kill / done-when
Standalone default-path lever: **KILL** (EV ≤1%, reconstruct already optimized).  
Status → done (measured). Do not build emission for chain OP.
