---
id: 1783908384227888000-c6637815
at: 1783908384
---
WAVE 3 SHIPPED (lmbrrr HEAD, fork 933533c8): CPB default flipped 50 -> 100 (the sweep knee, +3-4% steady greedy; sweep receipts in the earlier comment). Variant (c) eager enqueue: BUILT (env CANDLE_METAL_EAGER_ENQUEUE, default off) and FALSIFIED at the new default — rotated A/B wash (+0.1-0.2% medium/long). Variant (b) flush-per-token: skipped with receipts — one buffer per token is the CPB≈500 regime (a token step is ~450-500 encoder acquisitions), and the sweep showed monotone decline past ~150 from encode-ahead starvation; the sweep IS the pacing-granularity decision experiment. Gates green (oracle, tree-check, byte-identical text on stub+drafter). Fresh one-step gputrace at CPB=100 captured to decode-step-5.gputrace for the boundary-hole before/after inspection in Xcode (user-side analysis; the tok/s success metric is already met). Ticket outcome: the accidental-pacer diagnosis was right, the knee is 100, and the eager-pacing family is exhausted.
