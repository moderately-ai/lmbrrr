---
id: 1784500550914512000-3d6294ea
by: "agent-program"
at: 1784500550
---
## RECONFIRM + VERDICT (2026-07-19, M3)

### Isolated bench-gemv (this session)
- Q2_0 GEMV (exact bf16 act): **~104 GB/s** (0.228 ms @ 17408×5120)
- Q2_0 BITPLANE m=1 (int4 acts): **~113–122 GB/s** (0.19–0.21 ms), exact vs its int4 ref
- int4_act_err vs bf16 path: **~0.07–0.37** (not quality-free)

### Algebra (unchanged from B3)
Exact ternary dot from sign planes with **bf16 activations** still requires per-weight select/FMA — same instruction family as the current mv map (extract-c already −30% vs select). The 133 GB/s figure is a **bandwidth ceiling on plane layout with bit-sliced activations**, not a drop-in exact decode kernel.

### Kill criterion
e2e plain +2% **exact**: **cannot ship** without activation quantization.
int4 activation decode would be **Q-class** (PPL gate), not E-class — out of this ticket's exactness.

### Done-when
- Reconfirmed bitplane still beats mv at m=1 in isolation when acts are int4.
- Exact m=1 successor **blocked on activation bit-slicing quality**, not missing code.
- Do not build an "exact bitplane" without a measured int4-activation PPL gate first (new Q ticket if pursued).

Status → done (measured, exact path not viable without Q trade).
