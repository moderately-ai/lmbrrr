---
id: 1783911736609070000-1887aa21
at: 1783911736
---
CLOSING wontdo — the q4_K matvec floor is CONFIRMED CLOSED at THREE independent levels (2026-07-12 post-wave-1 trace + census, one decode step, q4k-full-text, CPB=100): (1) micro-bench — 6 falsifications (row-tile, load-shape, half-accum, u32 unpack, dot(), float4) + SoA layout + q8_0-format fallback, all recorded. (2) buffer-level GPU counters — the lm_head buffer runs Instruction Throughput Limiter 62%, Integer & Complex Limiter 60%, Compute Launch Limiter 39% at 193 GB/s (vs ~400 peak): issue/integer-bound, NOT bandwidth-bound. Kernel occupancy 13-20% while Occupancy Manager wants 49-70% — the deficit is the m=1 single-stream WORKLOAD, not the kernel. (3) line-level shader profiler on kernel_mul_mv_q4_K_impl_t inner loop (5065-5072): the 8 unpack+FMA lines are ~40% of the kernel spread EVENLY at 4-7% each = a throughput wall, no attackable hotspot; already the measured-best u16 four-mask form (comment 5024 records both u32 variants lost 4-5%). q4_K decode reads 0.5 byte and spends ~3 ALU ops/element to unpack+convert+fma; the M4 integer/convert issue rate is the wall. The simdgroup-matrix design was falsified for m<=8 specifically. VERDICT: reading q4_K faster is dead; the floor moves only by reading FEWER weights (32k-head quality lever, ~41% of step bytes = lm_head) or a different numeric format (q8_0 already loses on absolute time). No further mv-kernel work. The one surviving mv-ADJACENT lever is verify-chunk mc column-parallelism, which lives on gemv-width-splitk-concurrency.
