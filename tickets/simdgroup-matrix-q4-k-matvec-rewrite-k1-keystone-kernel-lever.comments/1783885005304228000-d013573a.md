---
id: 1783885005304228000-d013573a
by: "claude-main"
at: 1783885005
---
EVIDENCE (settled by three falsified micro-experiments 2026-07-11/12): the q4_K mv kernel is integer-unpack-pipe-bound, not bandwidth- or FMA-bound. Same 1024x248094 shape: dense BF16 358 GB/s, q8_0 262 GB/s, q4_K 99 GB/s effective. Falsified: row-tile widening, load-shape change, half-accumulate (shifted-nibble variant designed around the 65504 overflow trap — still lost; the unpack pipe, not FMA, is the limiter).

DESIGN: replace per-element nibble unpack + scalar FMA with simdgroup_matrix tiles — unpack each q4_K block once per tile into threadgroup/register storage, then simdgroup_multiply_accumulate against bf16 activations (bf16-direct contract preserved: F32 accumulate, F32 out). Cover kernel_mul_mv_q4_K_{f32,bf16} + _mc variants; keep the 2026-07-12 tail-row bounds guards. mul_mm simdgroup usage in the same file is the in-tree reference for the tile idioms.

GATES: correctness first (logits parity, trajectory-invariance oracle, 58-test suite; quant_quality advisory). ALL perf measurement deferred to the single end-of-fuse pass (2026-07-12 directive): micro-bench target >=2x q4_K GB/s, then verify_table -> in-loop refit -> new cost_model.json -> rotated suite. Downstream re-pricing after: tree + recycling break-evens, K3 sub-vocab 10-min gate.

RISK: if simdgroup tiles cannot beat the unpack pipe, fallback frontier is q8_0-for-head-only (262 GB/s at 2x bytes — paper wash; measure).
