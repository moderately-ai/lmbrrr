---
id: 1783885695063565000-c451e9b2
by: "claude-main"
at: 1783885695
---
K1 attempt #3 LANDED (fork 526e75e1, pin bumped): u32 nibble unpack in mv + mc, mc unpack hoisted out of the column loop. Design revision vs the original ticket framing: simdgroup_matrix tiles DO NOT apply at m<=8 (one live column wastes 7/8 of a tile; at m=1 unpack count is irreducible) — the fix is cheaper unpack per weight, which is what the profile actually indicted. Correctness gates all green (oracle 0.25/0.75, text-identical smoke). NO perf numbers yet — end-of-fuse pass measures micro GB/s (target >=2x), verify_table, in-loop refit, suite.
