---
id: 1783887773589217000-7b15f1d5
by: "claude-main"
at: 1783887773
---
K1 ROUND 2 RESULTS (dispatch-level bench, fork 506752f3): mc hoist CONFIRMED -7.5% (1.795->1.66ms lm_head m=4, 3 runs, controls +/-3%); u32 mv variants (dot + lane-wise) BOTH FALSIFIED (-4-5%) — float4 is scalar-per-lane, dot() serializes chains, u16 mask-multiply schedules best. New canonical kernel table (M4 Max): q4K mv 1.20ms/119GB/s, q4K mc(m4) 1.66ms, q8 mv 1.48/183, q6K 1.52/135, dense bf16 ~1.58/320. OLD 99/262/358 table DEPRECATED (lmbrrr harness had ~1.6ms/call floor — measured q4k==bf16, useless for kernel deltas; use fork 'metal_benchmarks qmv').

MSL SPEC REVIEW (opus, full spec vs kernel): kernel is LATENCY/OCCUPANCY-BOUND (22% of 546GB/s peak on zero-reuse traffic), not int-pipe-bound. RULED OUT with spec citations: extract_bits (needs 8 scalar calls vs 2 SWAR ops), unpack_unorm builtins (normalize — wrong op), deinterleave (bit-interleave only), simd_shuffle row co-op (no shared operand; y already register-resident across rows), threadgroup y staging (y is L2-resident 2-4KB; weights have zero reuse), dual-issue tuning (not exposed, irrelevant in this regime). LIVE LEVERS: (T1a) simdgroup_half8x8 for mc at m>=8 — requires NC 4->8 batching + tile marshalling, amortizes dequant across 8 cols onto the matrix pipe; (T1b) mv occupancy attack — function-constant nb (full unroll; ib-loop runs ONCE at k=1024), [[max_total_threads_per_threadgroup]], float4 y-loads (32 scalar loads/thread/ib today, 32B-aligned so legal); (T2) uint2 weight loads for cross-lane coalescing (likely the falsified load-shape change — check notes first); (STRUCTURAL CEILING) 144-byte AoS superblock straddles cache lines irregularly — density-preserving plane-split SoA repack is the biggest lever but is a weight-format change (new ticket territory, touches quantizer + all consumers).
