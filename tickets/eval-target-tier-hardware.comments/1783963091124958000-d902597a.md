---
id: 1783963091124958000-d902597a
at: 1783963091
---
STEP 3 COMPLETE — M3 spec economics refit (2026-07-13; artifacts committed f940a64): STS fitted from 746 in-loop samples (per-position accepts 77-85%, sane Platt params); cost model from realized round walls via the new evals/refit_cost_model.py: draft 14.25ms, greedy-in-spec-loop 6.35ms, verify[2..12] = 9.8..15.2ms.\n\nTHE FINDING (decode-only metric, so honest for the first time): on the M3, DSpark speculation is NET-NEGATIVE vs device-chain greedy on EVERY class — best case math 148-156 tok/s at tau 4.2-4.6 vs the greedy chain's 182. THE WHY, decomposed: (a) draft costs 14.25ms = 2.24 greedy-loop steps on the M3 (the drafter propose path is host/dispatch-heavy and the M3's slower host inflates it; on the M4 the same ratio was ~1.4x); (b) the spec loop's per-round sync makes even its OWN greedy floor 6.35ms/token = 157 tok/s — WORSE than the async device-chain greedy at 5.5ms/token = 182. Math at 155.6 actually beats the spec-loop's internal floor (+24% vs 157 — the drafter works!) but loses to the chain. The comparison target was always the chain; the old prefill-inclusive suite metric obscured this.\n\nIMPLICATIONS: (1) the M4 standings claim 'speculation wins' MUST be re-arbitrated with the decode-only metric on the M3-workflow... i.e. next quiet M4 opportunity or inferred from raw walls — the M4's faster host shrinks both taxes, so spec may still win there, but that is now an open question, not a fact; (2) the async lanes are the unlock for spec on down-tier hardware: dspark-ondevice-chunk-assembly (kill the per-round sync) + the drafter-propose cost (cut-drafter-propose-cost was 'done' at M4 economics — its M3 cost says the propose path deserves a dispatch census); (3) the scheduler NOW has truthful costs and will correctly skip drafting where it loses — per-class suite behavior under the refit model becomes the honest spec standings.
