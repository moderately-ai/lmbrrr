---
id: 1784307958453352000-a3f21c90
by: "claude"
at: 1784307958
---
TASK-0 COMPLETE (2026-07-17). Three parts, each verified by reading the source (not the ticket summary):

**1. MiniCPM-lane fixes were already landed** (bd2fbb0: flush-aligned steady window [HIGH-1], eos_overshoot_forwards [HIGH-2a], decode-only suite rate + file loads out of the wall [MED], warm baseline [MED], jitter percentiles + provenance; 46094/46690f0: suite divergence detector dead-code fix; 1f0ed66: baseline_decode_tokens_per_second). Verified in generate.rs (steady_window_tokens/elapsed with unit tests), run_bench.rs (v2 fields replaced v1 in place per the no-transitional-duplicates rule), run_spec_suite.py (interleaved arms, NaN-guarded stdev).

**2. LANE RECONCILIATION — the ~+6% chain-inflation bias NEVER applied to the 19.17 gguf standing.** The gguf lane (gguf_run.rs) has its own simple wall-clock ruler; full-file audit found only small, mostly ANTI-spec biases, fixed in 02e12c8: (a) plain decode encoded a dangling tail forward whose unsynchronized GPU time leaked into decode_seconds (~1/N pro-baseline) — now never encoded; (b) spec counted truncation-discarded tokens' verify cost silently — now reported as discarded_tokens; (c) overhead_seconds double-counted rollback — now subtracts it; (d) provenance (git rev, candle pin, loadavg, pmset therm, ru_maxrss) + per-round wall/accepted vectors + eos_reached/decode_forwards stamped on both gguf reports.

**3. Fresh quiet rotated v2 BASELINE (M3, 91% idle, prose prompt, 128 tok, 3 reps rotated P/E/M — MEASURED):** plain decode 14.40/14.42/14.43 (median 14.42; steady 14.36); spec exact 14.65/14.69/14.67 (median 14.67, accept 2.342, ids BYTE-MATCH plain every rep = golden gate); spec margin-3.0 19.18/19.18/19.17 (median **19.18**, accept 3.448, discarded 2). Spread <0.3%. **The v1 standings stand under the v2 ruler** (19.17 -> 19.18). Note: plain-decode arm runs WITHOUT the mm2d env — LMBRRR_MM2D_PLANAR=1 at m=1 forces the planar kernel and craters decode to 5.9 tok/s (mechanism confirmed by intervention: same binary, env off -> 14.64; ids byte-match across routes). Never share the spec env with the plain arm.

**NEW MECHANISTIC ANCHOR (per-round vectors, margin arm — MEASURED):** round wall is FLAT at ~229 ms regardless of accepted count (0..4): verify 192 ms (84%), propose 30 ms (13%), rollback 3-6 ms, overhead ~4 ms. Throughput identity closes: (accept+1)/round_wall = 4.448/0.2299 = 19.3 ~= measured 19.18. And **18/29 rounds saturate the width-4 cap** (exact arm: 12/38) — the acceptance run-length distribution is cap-truncated, so width-7 headroom is larger than this ticket family's propose-bound estimate (propose is 13% of the round, not the binding term). Fed to drafter-width7-retrain-bonsai.

M3 provenance note: lmbrrr_git_rev reads "-dirty" because the M3 keeps local pyproject/uv.lock edits (the load-bearing debuggable-python gpucapture setup) — Rust sources clean.
