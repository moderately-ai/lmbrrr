---
id: 1784212938000000000-2f4e2026
by: "claude"
at: 1784212938
---
TOOLCHAIN GATE CLEARED — 2-bit `matmul2d` compiles on the M3 (2026-07-16). The item-5 blocker (weight-bound verify GEMM needs `tensor_ops::matmul2d` with a `uint2b_format` B operand, which is Metal 4.1, absent from Xcode 26.6's `32023.883`) was NOT a dead end — it was a toolchain-version gap. The earlier "blocked" conclusion was correct *on Xcode 26.6* but stale: reconfirming against the newest toolchain flipped it.

Installed **Xcode 27.0 beta 3** (build `27A5218g`, from the developer-downloads web page — xcodes CLI Apple-ID auth was flaky, the visual .xip route worked) + its Metal Toolchain component (`27A5218h`, compiler **`metalfe-32023.918.1`**, target `air64-apple-darwin27.0.0`; the Metal Toolchain is a separate `xcodebuild -downloadComponent MetalToolchain` download, decoupled from Xcode since 26). Three checks, all green on the same M3 hardware that failed them on 26.6: `-std=metal4.1` is a valid language mode; `uint2b_format` compiles; and — decisively — `tensor_ops::matmul2d` **accepts `uint2b_format` as its B operand** (a minimal kernel mirroring `mm2d_q4k.metal` with a 2-bit B tensor compiled AND linked to an 11 KB `.metallib`; the MPP whitelist would `static_assert`-fail otherwise). No M5 / Neural-Accelerator hardware needed.

Meta-lesson recorded in the research doc + metal_notes §14: a toolchain-capability "wall" is a dated fact tied to the installed compiler build, not a permanent limit — when a feature is spec-confirmed but compile-blocked, re-test on the next Xcode beta before treating it as impossible. The one-line re-test is `xcrun metal -std=metal4.1 -c` on a `uint2b_format` matmul2d kernel.

Now building the real `mm2d_q2_0` kernel (mirror the q4_K stack: `q2_0_mm2d_planes` already written → `mm2d_q2_0.metal` with the ternary fold `d·(P − rowsum)` → `mm2d_q2_0.metallib` (`-std=metal4.1`) → `Source::Mm2dQ2_0` + loader + `call_quantized_matmul_mm2d_q2_0` → route verify m∈2..8). Projected ~28–31 tok/s (~2×). See docs/research/dspark-verify-weightbound-gemm.md.
