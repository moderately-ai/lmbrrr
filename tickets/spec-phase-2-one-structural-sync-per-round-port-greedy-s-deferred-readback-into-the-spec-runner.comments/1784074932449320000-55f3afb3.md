---
id: 1784074932449320000-55f3afb3
at: 1784074932
---
ONE STRUCTURAL SYNC PER ROUND — SHIPPED, GATE BYTE-IDENTICAL (2026-07-15 early; receipts /tmp/mtp13-*, /tmp/suite-mtpos.log): the MTP round now assembles the verify chunk ON DEVICE (anchor is a device slice of the previous verdict; drafted ids never leave the GPU pre-verify) and makes exactly one small host readback per round (targets + drafted ids packed into one u32 buffer). The two instrumentation synchronize()s are opt-in via LMBRRR_SPEC_FENCED_TIMING=1 (report carries "fenced_timing"; per-bucket decompositions need the flag). Text byte-identical to the previous build, deterministic across runs. Dev arm: round 13.58 -> 12.66 ms, 169.2 -> 181.6 tok/s.

SUITE (mtpd3os, r1 acceptance): 6-class MEAN 181.0 = 91.6 pct of greedy 197.6. math 225.8 mean (226.0/218.4/233.1 — +14 pct OVER GREEDY as a class), coding 196.5 (wash with greedy: q124 197.9, q125 199.1), translation 185.1 (q164 229.9), qa 172.3 (q326 216.7), writing 158.7, summarization 147.7. Evening mean arc: 137.5 -> 145.2 -> 170.4 -> 181.0.

REMAINING BEFORE THE SHIP-BAR A/B: (1) r2 checkpoint (step ~10k/50k, holdout 0.7620 already above r1 final) -> re-arbitrate; (2) weak-class greedy handoff (hysteresis port) so writing/summarization/qa get the 197.6 floor -> mean crosses; (3) optional scraps: SharedEvent listener on the verdict readback, draft chain dispatch collapse, fused m-row verify argmax, speculative cross-round overlap (async round loop — now the biggest single remaining idea at ~2.9ms draft overlap).
