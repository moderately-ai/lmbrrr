---
id: 1784048488659266000-46ff3758
at: 1784048488
---
The spec round loop predates the greedy lane's async machinery: 4+ pipeline drains per round (per-draft-step syncs, blocking verify argmax readback, rollback bookkeeping) = 4.2ms/round measured. The algorithm requires exactly ONE sync per round (the verify verdict gates the commit). Port the deferred-readback pattern; batch rollback/truncate decisions; assemble catch-up input without intermediate materialization. INCLUDES spec-verify fused argmax: extend the q4k head-argmax kernel to m rows (MV_ARGMAX_ROWS_PER_TG machinery exists) so verify never materializes m x 248k logits — per-row argmax lands in u32[m], one readback. Target: host/sync 4.2 -> <=1ms/round. Gate: committed ids byte-identical; divergence oracle unchanged.
