---
id: 1783761116047697000-52d4e586
by: "claude"
at: 1783761116
---
CLOSED (2026-07-11, lmbrrr HEAD). Item 2 (device-resident sampled token) LANDED: greedy loop keeps the argmax id on device, feeds the next forward directly, reads back in batches of 8 (EOS overrun <= 7 discarded forwards). Kills the per-token 4-byte from_slice upload AND the per-token host sync - host now runs ahead of the GPU. Measured +4-5.5% every greedy lane: full-text 223.8->235.0 (NEW RECORD), q4k-mlp-q8 200.0->211.4, bf16 147.3->153.4. Output bit-identical (verbatim text match vs pre-change), tests + tree-check green. Item 1 (two-stage argmax kernel) NOT PURSUED, with reason: with the per-token sync gone, the single-threadgroup 248k scan (~10-16us of GPU timeline) sits below the end-to-end measurement floor (~85us/token at 2% noise); a composed two-stage costs ~5 extra dispatch launches (~15us) - expected wash; only an Instruments capture could attribute it, and the ceiling is ~0.3%. Non-greedy path's 1MB/token host logits copy remains flagged for anyone benchmarking temperature > 0.
