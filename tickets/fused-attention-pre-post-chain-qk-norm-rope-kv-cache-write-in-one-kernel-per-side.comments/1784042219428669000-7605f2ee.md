---
id: 1784042219428669000-7605f2ee
at: 1784042219
---
RECEIPTS (2026-07-14, M4 Max one-token gputrace decode-step-100 + code read). Trace: 178 dispatches/token, 2 encoders, 12 pipeline states; per-kernel replay costs rmsnorm_bf16 10.04% (180 SIMD groups), rope_partial_bf16 5.78% (168), copy2d_bf16 2.87% (136), copy_bf16_strided 0.40%. Code: standalone rmsnorm = q_norm/k_norm x6 attn layers + first-input + final norm (~14 dispatches, qwen35.rs:658); DeltaNet o-norms already fused into gated_delta_v2_epilogue (norm_weight_f32 params, qwen35.rs:1146+). copy2d = KVCache::append slice_set kx6+vx6 (qwen35.rs:426-440); copy_strided = k.contiguous() feeding it. INFLATION CAVEAT (load-bearing): 10.04%/14 dispatches = ~34us per tiny norm under profiling replay vs single-digit-us untraced boundary cost from the diag dependent-chain probe -> replay exaggerates tiny-dispatch share. Honest M3 prize estimate ~3-6% wall, NOT the 18.7% trace-flattered sum. DESIGN: per attention layer, one kernel for q-side (head-norm + partial rope on q) and one for k/v-side (head-norm + partial rope on k + direct write of k,v into cache slots at offset — kills slice_set copy2d AND the contiguous copy). Deletes ~38 of 178 dispatches. Gates: bitwise vs unfused path (norm+rope are exact-arith reorder-free if kept in f32 accum like fused_norm), full battery, rotated A/B on M3 AFTER the macOS-27 re-baseline. Secondary priced item: folding epilogue into gated_delta_v2_decode main kernel (removes 18 dispatches; grid mismatch is the reason for the split — price before building).
