---
id: 1784045516241460000-2f2ac527
at: 1784045516
---
SHIPPED (2026-07-14, pin ce19a417). Implementation: fork kernels attn_q_prep_bf16 / attn_kv_prep_bf16 (dedicated metal_src/attn_prep.metal + kernels/attn_prep.rs), lmbrrr src/fused_attn.rs wrapper, TruncatableKvCache::reserve/advance fused-append protocol, FullAttention::forward fused branch (default on, LMBRRR_FUSED_ATTN_PREP=0 opt-out; tree-verify rope_positions path stays unfused). Replaces ~10 dispatches/attn-layer (2 narrow copies + 2 rmsnorms + 2 ropes + v copy + 2 slice_sets + contiguous) with 2. BIT-IDENTITY achieved (better than the margin-class fallback): required mirroring the production rms_norm's real semantics — rsqrt(fast_divide(sum,d)+eps) and BF16-precision output scaling (scale rounded to bf16 BEFORE x*scale*alpha), discovered after the naive f32-scaling copy diverged +-1 ULP in both math modes; reduction tree pairing (shared s=64..; simd shuffle 16..1) pairs identically to a plain shared tree so no template extraction needed. Gates: fork test attn_prep_matches_unfused_chain (kernel-level byte identity incl. cache-slot placement + untouched-row check), lmbrrr e2e fused==unfused==fc3e0f7c golden (256 tok), extended battery green, goldens re-blessed at ce19a417 byte-identical to ancestors, M3 battery green ON MACOS 27 (goldens matched — no Metal-compiler bit drift from the OS upgrade). A/B (M3, macOS 27, rotated F-U-U-F, long profile, 3 iters each): fused 197.6 median (196.9-198.1) vs unfused 193.5 (192.0-195.2) = +2.12%, non-overlapping. NEW GREEDY FLOOR 197.6 tok/s (macOS 27 baseline; unfused arm doubles as the OS re-baseline: 193.5 vs 191.3 on macOS 26). Remaining priced item on this lane: fold gated_delta_v2_epilogue into the main decode kernel (18 dispatches).
