---
id: 1783945199262285000-c33fd11b
at: 1783945199
---
EXECUTOR PROCEDURE (self-contained; assumes zero prior context).

REPOS: lmbrrr = ~/workspace/github.com/moderately-ai/lmbrrr. Kernel fork = ~/workspace/github.com/huggingface/candle, branch `lmbrrr`, push remote `tomsanbear`. Workflow for ANY fork change: edit fork -> commit -> `git push tomsanbear lmbrrr` -> copy the new sha into the four `rev = "..."` pins in lmbrrr/Cargo.toml -> `cargo build --release` in lmbrrr. TRAP: `cargo nextest run` rebuilds without the metal feature and CLOBBERS target/release/lmbrrr — always re-run `cargo build --release` after nextest, before benching. TRAP: artifacts under lmbrrr/target/ (manifest, drafter, cost models, rankings) are destroyed by `cargo clean` — never run it.

ORDERING: run eval-apples-to-apples-qmv FIRST if not done — it establishes whether llama.cpp's geometry actually beats ours on this machine at our shapes, which decides how much effort this ticket deserves.

VARIANTS TO EVALUATE (all bit-identical per row by construction — same fma order within a row):
- rt2: already implemented — `kernel_mul_mv_q4_K_bf16_bf16_rt2` via MV_Q4K_ROWTILE in candle-metal-kernels/src/metal_src/quantized.metal, host fn `call_quantized_matmul_mv_q4k_bf16_rowtile(2, ...)` in src/kernels/quantized.rs. Within-run micro result: +10% on lm_head; needs production arbitration.
- nr2sg2 (NEW, the llama.cpp geometry): N_DST=2 rows per simdgroup, 2 simdgroups per TG (64 threads). This is exactly llama.cpp's N_R0_Q4_K=2 / N_SG_Q4_K=2 (their PR #20399 found N_R0=4 spills registers). Implement as a new template param on the mv impl (copy the MV_Q4K_NSG pattern which already threads NSG through `first_row = (r0 * NSG + sgitg) * N_DST`; add N_DST as a second param, instantiate <NSG=2, N_DST=2>). Host: TG size (32*NSG,1,1), grid width divide(n, NSG*N_DST).
- nr2sg2 + rt2 combined only if both win independently.

PRODUCTION WIRING (env-selected, one binary, no rebuild between arms): in the fork, candle-core/src/quantized/metal.rs fwd_mv, inside the q4_K + bf16-dst branch, read `LMBRRR_Q4K_MV_VARIANT` (values: unset/baseline | rt2 | nr2sg2 | nr2sg2rt2) once via std::sync::OnceLock<String>, route to the matching call_* fn. Unset MUST be byte-for-byte the current dispatch (zero risk to the deployed path).

GATES (in order; abort on any failure):
1. Bench-task bitwise gate: `cd ~/workspace/github.com/huggingface/candle/candle-metal-kernels && cargo build --release --example metal_benchmarks && ./target/release/examples/metal_benchmarks nsg-sweep` — every variant row must print bitwise=OK vs baseline on all 5 shapes.
2. lmbrrr: `cargo nextest run` (expect 59 passed) then REBUILD `cargo build --release`.
3. Stub oracle: `./target/release/lmbrrr dspark-run --quantized-manifest target/minicpm-v46-q4k-full-text/manifest.json --quantize-lm-head q4k --prompt "Explain how tides work." --max-new-tokens 128` -> JSON must show invariance_oracle_passed=true and max deviation <= 0.75.
4. Text-identical per variant: run `./target/release/lmbrrr run --quantized-manifest target/minicpm-v46-q4k-full-text/manifest.json --quantize-lm-head q4k --prompt "Explain how tides work." --max-new-tokens 256` with LMBRRR_Q4K_MV_VARIANT unset vs each variant; generated text must be BYTE-IDENTICAL (variants are bit-preserving; any diff = implementation bug, stop).

MEASUREMENT (per eval-protocol-ambient-control: quiet machine, session control at start+end, discard first post-build run as shader-compile transient):
- Arms = env values on the ONE binary. Palindrome rotation per profile: base, rt2, nr2sg2, nr2sg2, rt2, base (x2 if time allows). Command per run: `./target/release/lmbrrr bench --quantized-manifest target/minicpm-v46-q4k-full-text/manifest.json --quantize-lm-head q4k --warmup 1 --iterations 3 --profile medium` and again with `--profile long`. Metric: median steady_state_tokens_per_second per arm.
- DECISION: ship the best variant iff >= +2% over baseline with non-overlapping run ranges on BOTH profiles. Ship = make it the default dispatch (keep env override for rollback), bump pin, then run the post-ship obligations: in-loop cost refit (`python3 evals/run_spec_suite.py --arm round4=target/dspark-drafter-round4 --split calibration --per-class 3 --reps 1 --gamma 6 --tag refit-q4k-r3`) and a validation standings row (`--split validation --per-class 3 --reps 3`). If no variant clears the bar: record the numbers here, close as falsified, and note it in docs/research/1000-toks-campaign.md falsification ledger.

CONTEXT RECEIPTS (why these variants): production trace shows kernel_mul_mv_q4_K_bf16_bf16 at 74.6% of decode GPU time, ~196 GB/s vs ~245 theoretical; llama.cpp/MLX inner loops are byte-equivalent ALU-wise (verified by source read) — the residual gap is short-K amortization (our K=1024 vs their benched K=4096) + register pressure at N_DST=4. Micro-bench drifts +/-35% across sessions; only within-session ratios and production A/B count.
