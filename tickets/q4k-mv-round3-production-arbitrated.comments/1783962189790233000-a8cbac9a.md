---
id: 1783962189790233000-a8cbac9a
at: 1783962189
---
SHIPPED (2026-07-13, fork ce8c3df3, lmbrrr pin bumped): nr2sg2 (llama.cpp's N_R0=2/N_SG=2 geometry) is now the DEFAULT q4_K bf16 decode dispatch; LMBRRR_Q4K_MV_VARIANT=baseline restores the historical geometry, nr2sg1 also available. EVIDENCE (all on the M3 referee, quiet, plugged): kernel-level +30% on the head (101.5 -> 131.5 GB/s, 68% -> 88% of roof, within 6% of llama.cpp's 139.5; all arms BITWISE-identical); production palindrome A/B 6 runs/arm: medium 182.1 vs 178.2 (+2.2%), long 181.2 vs 177.6 (+2.0%) — clears the +2% bar on both profiles. MECHANISM (settled): rows-per-simdgroup 4->2 halves live accumulator state -> more resident simdgroups hide DRAM latency; the second simdgroup (sg2 vs sg1) adds nothing on M3; the f32-y dtype effect collapsed to ~1.4% after adding vectorized ushort4 bf16 y-loads (also shipped — scalar bfloat loads were the issue-rate cost, not conversion ALU). WHY e2e gain < kernel gain: the M3's host encode shadows part of the GPU saving (0.32ms/token head saving -> ~0.11ms realized). REMAINING: M4 production A/B in a quiet window (expected LARGER conversion: bigger head share, host encode a smaller fraction; the loaded-M4 micro suggested up to 2x kernel-level); M4 in-loop cost refit after (also owed post-wipe); then close. rt/nsg variant lanes on this ticket are dead (superseded by geometry).
