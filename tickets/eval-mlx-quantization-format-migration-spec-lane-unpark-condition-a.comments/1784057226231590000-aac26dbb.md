---
id: 1784057226231590000-aac26dbb
at: 1784057226
---
Filed from the wide-kernel A2A verdict (2026-07-14): MLX's affine group-64 format is MEASURABLY batch-friendlier on the M3 than ggml q4_K — their m=2 lm_head matmul costs ~1.0x m=1 (1297us absolute) vs our best wide-kernel 1.44-1.53x (1616us); three falsified variants bracket the residual gap as format-structural (flat scales + single-plane packing vs hierarchical 6-bit sub-scales + dual-plane nibbles). SCOPE (eval only, quality-gated): (1) quantize the model to MLX-style affine g64 (4.5 bits/w, same footprint) and measure quality vs the q4_K deployment (ppl battery + KLD + top1 + text-agreement, thresholds per eval-quality-reference-battery); (2) port/adapt an affine-g64 GEMV+wide kernel pair to the fork (MLX quantized.h is the reference; simpler dequant than q4_K); (3) re-measure the m-sweep + verify intercept. UNPARKS the spec phase-2 tickets if chunk2 <= ~1.2x decode at equal quality. Priced honestly: this is a format migration (loader, pack, kernels, goldens all re-baselined) — only worth it if the spec lane is the product priority; p3 until the user weighs in.
