---
id: 1783942647281499000-9a00154d
at: 1783942647
---
ROUND 2 EXECUTED AND RESOLVED (2026-07-13): all four vectors dead, two by measurement, two by analysis. The re-open was legitimate (the dimensions were genuinely untested) and the closure is now airtight.

V1 THREADGROUP GEOMETRY — MEASURED FLAT (fork d994d9a1). Built NSG-templated kernel (bit-identical per row, bitwise gate passed on all shapes) + nsg-sweep bench task. lm_head: nsg=1 1.179ms/121 GB/s -> nsg=4 1.121ms/127 GB/s (+5%, under the 1.15x kill gate); nsg=8 worse; ALL body shapes flat. The trace's 54.6% launch limiter is CONCURRENT with the wall, not causal. Also learned: isolated serialized dispatches put body shapes (n<=8192) at a 0.10-0.38ms latency floor (12-13 GB/s) — per-dispatch drain/ramp dominates small shapes, i.e. the comb, not the kernel.

V2 MAGIC-NUMBER DEQUANT — ANALYTICALLY DEAD, with the bit math: (a) the q4_K kernel's in-place mask trick (nibbles used at value nib*{1,256,16,4096}, scales deferred to the rescale) already avoids all shifts, so f32-magic (OR into 2^23 mantissa) merely swaps CVT->OR at equal op count; (b) deferring the magic-base subtraction into the sumy correction — the only way to save the op — CANCELS CATASTROPHICALLY: accumulating y*(2^23+val) loses ~11 bits of every product (val/2^23 ~ 2^-11 relative), so per-element subtract is mandatory -> 4 ops/element, WORSE than the current 3; (c) half2-packed magic overflows half range through the in-place bases (y*(1024+...) sums past 65504) and bf16->half y conversion is range-unsafe. The 3-ops-per-element floor of q4_K's packing is real.

V3 SHAPE SPECIALIZATION — analytically ~nil: k=1024 -> nb=4 -> the block loop is already ONE iteration per thread; nothing to unroll, index math amortizes over ~100 ops. V4 SPLIT-K small-n — the isolated bench shows those dispatches at the drain/ramp latency floor; cutting kernel exec 2-3x moves nothing e2e (matches the original demotion).

FINAL STATE OF THE FLOOR QUESTION: q4_K mv is closed at FOUR levels now (micro-falsifications, buffer counters, line profile, and this geometry/arithmetic round). 121-132 GB/s is the intrinsic rate of 3-ALU-ops-per-half-byte at m=1 occupancy. The floor moves only via fewer weights (32k head — measured, rejected on quality), fewer dispatch drains (fusion — shipping), or the host path (deferred readback — next). Closing.
