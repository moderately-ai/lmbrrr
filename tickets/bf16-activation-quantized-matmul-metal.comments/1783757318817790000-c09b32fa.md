---
id: 1783757318817790000-c09b32fa
by: "claude"
at: 1783757318
---
BF16-SRC1 LANDED, TICKET COMPLETE (2026-07-11, fork e02eeacb, lmbrrr b7393bc). Item (1) done: q8_0/q4_K/q6_K mv + mc kernels templated on src1 type with bf16 entry points; MixedLinear passes BF16 directly on Metal for those dtypes (bf16_direct flag; output F32->BF16 hop remains since kernels write F32). +4% on every quantized lane: greedy 189.3->196.9, fixed-g4 128.1->133.3, sched math 141.3->146.9, aggregate 686/862 (N=8/32), verify l=1 4.80ms / l=2 8.51ms. Cost model updated in place (draft 4.6ms, verify table 4.8/8.5/.../19.6). Width-4 break-even now 3.11 tokens/round vs greedy-q4k 196.9 (was 4.07 at ticket start). ACCEPTANCE DISPOSITION: bf16 mv variants DONE; batched single-dispatch GEMV DONE (earlier); force_f32_input dropped DONE; bf16 TILE mm NOT built - fwd casts internally for m>12 (prefill-only shape, one cast amortized over >=16 rows; revisit only if profiling shows it matters). The 250 quantized forwards/s stage gate is NOT met (197 f/s; roofline says ~1.2ms/forward vs measured 4.8ms) - that residual is dispatch overhead, which is single-command-buffer-decode-forward's scope, not kernel work; gate transfers there. OPERATIONAL TRAP recorded: cargo nextest run --release rebuilds the binary WITHOUT the apple-optimized feature and silently produces a CPU-fallback binary ("SDPA has no cpu impl") - always rebuild with --features apple-optimized after running tests, and treat first-run spec numbers after a new kernel binary as shader-cache-compile transients (observed 86 vs 147 tok/s bistability, settles by run 3).
