---
id: 1784068324206256000-6f7e90c0
at: 1784068324
---
WALL-2 FIRST RESULTS (2026-07-14 late night, M3, tides/384, head-only mm2d route throughout; receipts /tmp/mtp4-*, /tmp/mtp5-*).

2b CHAINED ZERO-SYNC DRAFTING — SHIPPED, GATE PASSED: device argmax (same kernel as argmax_token, readback deferred) feeds the next chain step via the embedding lookup; drafted ids return in ONE readback per round; catch-up argmax also stays on device. Text + accepted-histogram BYTE-IDENTICAL to the synchronous path at d3 and d5. Win small alone (draft d5 9.98 -> 9.07ms) because the per-step cost was the FULL-VOCAB head read, which led directly to:

2c FR-SPEC DRAFT-HEAD SLICE — SHIPPED (--mtp-draft-vocab): separate sliced+quantized draft head (target verifies full-vocab, losslessness structural); slice-local argmax remapped to global ids ON DEVICE (index_select), so the zero-sync chain is preserved. RESULT: draft d5 9.07 -> 3.76ms, d3 5.81 -> 3.35ms. d3+slice = 124.3 tok/s (from 94.6 at the session start). CAVEAT: the ranking artifact never existed on any machine — tonight ran a PLACEHOLDER (BPE id order, /tmp/frspec-placeholder-32k.json on the M3); perf numbers are ranking-independent (cost is only N), acceptance is provisional (p1 0.61-0.70 vs 0.72 full; tau single-prompt variance d3 3.28 / d5 3.17). The REAL corpus ranking is owned by eval-frspec-longtail-audit and is now a prerequisite for re-arbitration.

STANDING ARITHMETIC (all measured terms): d3+slice round = verify 11.14 + draft 3.35 + other 3.99 = 18.5ms at tau 3.28. Remaining levers: (a) 2a one-sync — "other" ~4.0-4.85ms/round (includes the verify argmax_tokens readback, chunk assembly, snapshot/rollback host work, and possibly top_k_values reading back full verify logits for divergence diagnostics — QUANTIFY that harness tax first, it may be a free win in both arms); (b) all-mm2d flat verify at deep chunks (d5 head-only verify 16.5 vs all-mm2d ~12.5) + the geometry table (~10ms). Composed: d5 at tau 3.55 with real ranking -> 211-248 tok/s vs greedy 197.6. NEXT SESSION: (1) quantify+gate the top_k_values harness tax, (2) 2a one-sync implementation, (3) per-shape mm2d geometry table + split-K, (4) real FR-Spec ranking, then 6-class suite re-arbitration per drafter-checkpoint-ab-protocol.
