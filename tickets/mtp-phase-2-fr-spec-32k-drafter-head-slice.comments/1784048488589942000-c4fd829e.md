---
id: 1784048488589942000-c4fd829e
at: 1784048488
---
Slice the shared lm_head to the FR-Spec 32k high-frequency vocab for DRAFTING only (the DSpark drafter lane's fr-spec vocab artifact is the source list). Draft argmax runs over 32k rows (~18MB) instead of 248k (~143MB) — cuts the dominant draft-step cost ~8x (1.4ms -> ~0.2ms). LOSSLESSNESS PRESERVED BY CONSTRUCTION: the target verifies over the full vocab; slicing only affects which tokens can be PROPOSED (acceptance), not correctness. Measure: distilled-head holdout top-1 under 32k restriction offline (vs 0.7595 unrestricted), then on-device p1 (expect small drop from 0.724; fold in eval-frspec-longtail-audit's rare-token classes). This is the DSpark-paper drafter design, not an approximation.
