---
id: 1783975896425581000-0ae665cf
at: 1783975896
---
Priority p2 -> p1 and the probe question is now half-answered in-situ: the drafter's fenced Markov chain (12 strictly-serial dispatches) costs 3.4 ms against ~78us/step isolated kernel exec -> ~0.24-0.28 ms per serialized dispatch boundary on the M3 — two orders of magnitude over the 3us threshold in this ticket's verdict gate. What the synthetic probe must still decompose: how much of that 0.28 ms is (a) pipeline drain at the auto-barrier, (b) host encode, (c) amortized CPB-100 commit share (cpb-ladder A/B on spec-loop-economics-recovery covers (c) in parallel). Run the probe as designed (1-TG chained loop vs N real dispatches, N in 8/32/128/340) on the M3; its verdict routes drafter-chain-persistent-kernel (gated dependent) and the metal-icb-decode-replay unpark decision — note ICB removes encode, NOT drains, so only the (b) share counts toward its case.
