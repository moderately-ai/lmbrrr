---
id: 1784317200000000000-d4b91e55
by: "claude"
at: 1784317200
---
SPIKE RUN + VERDICT (2026-07-17; kernel `kernel_ternary_bitplane_qmv` at candle 9d514e7c, bench `gguf bench-gemv` Q2_0 BITPLANE section; M3, 17408x5120, 2.125 bpw planes, int4 offset-8 activations, exact vs its int4 reference rel 0.0000 at every m — MEASURED):

m=1: 0.177 ms (133.7 GB/s) | m=2: 0.181 (130.5) | m=4: 0.309 (76.6) | m=8: 0.586 (40.4). Memory-bound at m<=2, ALU-degrading beyond (int-op throughput ~950 Gop/s at m=8 — v1 is not even ALU-saturated; per-m activation-plane loads + the shift-add chain gate first).

**TWO-TIER REFUTED — by arithmetic against a measured fact, not by the spike's speed:** exact mm2d verify is FLAT in m (0.55 ms, m=1..8), so filter + exact-on-survivors >= exact-alone ALWAYS; a cheap first pass cannot pay when the exact pass does not shrink with surviving rows. As a REPLACEMENT (approximate verify) v1 loses at m=8 (0.586 vs 0.552) before the int4 accuracy cost; a v2 (tg-staged activation planes, lane-split m) might reach ~0.4 ms = 1.4x approximate verify, but that is exactness-breaking (C4-class quality trade) — out of scope for the quality-preserving campaign. Do not reopen for the exact pipeline.

**KEPT FINDING (mechanism, new):** at m=1-2 the bitplane read sustains 133.7 GB/s vs the exhausted mv GEMV's 106 on identical bytes — proof (with Q4K mv's 142 as the platform roof) that the Q2_0 mv is ~25% INSTRUCTION-limited by its 2-bit unpack, not bandwidth-limited. No exact-path exploitation found (exact ternary dot from sign planes with float activations needs per-weight select/fma = mc-class cost), but this bounds what any future m=1 decode-kernel idea chases: ~106 -> ~135-142 GB/s (+25-30% plain decode) IF an exact decode kernel can cut per-byte decode instructions. Recorded as the successor question; kernel + bench stay in-tree as the measurement harness.
