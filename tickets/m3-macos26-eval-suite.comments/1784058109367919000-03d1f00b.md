---
id: 1784058109367919000-03d1f00b
at: 1784058109
---
UNBLOCKED — MSL 4.1 unpack COMPILES on macOS 27 (2026-07-14, M3 on 27.0 build 26A5378n + Xcode 26.6). Step-1 gate PASSED after a four-layer diagnostic, recorded so nobody re-treads it: (1) Xcode 26.6 ships WITHOUT the Metal Toolchain — install via 'xcodebuild -downloadComponent MetalToolchain' (688MB) or every xcrun metal call errors; (2) RED HERRING: the offline toolchain (17F109, metal 32023.883) still has NO -std=metal4.1 flag — it caps at metal4.0 and 'unpack' is undeclared there; (3) MSL 4.1 lives in the RUNTIME compiler only, and the SDK has no named enum case — use MTLLanguageVersion(rawValue: 262145) ((4<<16)|1) on MTLCompileOptions; raw 262146 (4.2) rejected, so 4.1 is the ceiling; (4) the ticket's original probe source was WRONG PER SPEC: unpack<T>(w[tid]) on a device pointer fails with 'no matching constructor' — spec p508: construct the thread-space packed_numeric_type from its storage_type word first (uint for 8x4b), i.e. 'packed_numeric_type<uint4b_format, 8> p(w[tid]); vec<half,8> v = unpack<half>(p);'. Fixed probe compiles AND creates pipelines for BOTH half and bfloat targets (bfloat direct = our pipeline dtype, confirming the earlier spec note). Probe at m3:/tmp/probe4.swift. PRODUCTION IMPLICATION: the fork's get_compile_options never sets languageVersion (defaults below 4.1), so any unpack kernel needs a capability-gated languageVersion=262145 override in kernel.rs — OS-gated at runtime since macOS <27 rejects it. NEXT (steps 2-3 merged): empirical lowering check via gpudebug per-dispatch instruction counts (masked-fma baseline vs unpack variant on identical data — beats static disassembly, which is unavailable offline anyway) + GB/s micro-bench on the lm_head shape; remember the q4_K nibble-order caveat (elements i,i+1,i+8,i+9 per u16 — unpack assumes linear order, so the bench must repack or the variant must permute).
