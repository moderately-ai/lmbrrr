---
id: 1783947195310193000-1229cb50
at: 1783947195
---
STATUS 2026-07-13 — BOX ACCESSED, packed_numeric PARTIALLY BLOCKED, baseline cross-check RUNNING. Access: ssh -o IdentitiesOnly=yes -i ~/.ssh/id_ed25519_thomas_work_laptop_m4_pro tsanterre@100.66.57.91 (Tailscale node thomass-macbook-pro-2, 1h sleep timer). Box: M3 Pro, 14-core GPU, 18GB RAM (maxWS 13.6GB), macOS 26.5.2, Rust 1.97, NO Xcode (CLT only — no offline metal compiler; xcrun metal unavailable). PROBE RESULTS (runtime compiler via MTLDevice.makeLibrary, Swift probes at /tmp/probe*.swift on the box): (a) runtime accepts up to -std=metal4.0; 'metal4.1' REJECTED as invalid and MTLLanguageVersion 4.1 absent -> MSL 4.1 is not in macOS 26.5.2's runtime at all, so even offline-compiled 4.1 metallibs would not load; (b) under MSL 4.0 the header metal_packed_numeric EXISTS and packed_numeric_type<uint4b_format, 8> COMPILES (sizeof works), but unpack<T,Format,N> is undeclared — spec-confirmed (MSL spec 2026-06-04, p81-82): extract/unpack/pack are 'Metal 4.1 supports...' additions; (c) softwareupdate offers no newer macOS to this box yet (only CLT 26.5/26.6). CONCLUSION: the unpack primitive needs a macOS release that ships MSL 4.1 (presumably 26.6+). REVISED PLAN: (1) baseline cross-check (step 4 of the body) UNBLOCKED and running now — fork cloned+built at ~/lmbrrr-work/candle, nsg-sweep 2x launched; (2) unpack micro-bench stays blocked; re-probe with /tmp/probe2 after any macOS update on the box (30-second check); (3) the box meanwhile serves eval-target-tier-hardware as the mid-tier point. One more spec detail for the eventual bench: unpack also supports T=bfloat directly (float/half/bfloat all legal targets), so the q4_K variant can unpack straight to bf16 — better than the half assumption in the body.
