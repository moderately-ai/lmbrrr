---
id: 1784061798484763000-08dda410
at: 1784061798
---
VERDICT 2026-07-14 — packed_numeric line CLOSED (regression, not a win). Steps 2-3 executed on the M3 (macOS 27.0/26A5378n, Xcode 26.6, fork rev 9232f7ab, lmbrrr 28599f0).

IMPLEMENTATION: kernel_mul_mv_q4_K_bf16_bf16_unpk in fork candle-metal-kernels/src/metal_src/quantized_unpk.metal — own Source compiled at MTLLanguageVersion raw 262145 (only the unpk source; the rest of the fleet keeps the default compiler), routed by LMBRRR_Q4K_MV_VARIANT=unpk with a warn-once fallback to the nr2sg2 geo on library-load failure (macOS <27 proven on the M4: falls back cleanly). Geometry mirrors the shipped nr2sg2 kernel exactly; the inner loop is 2x N=16 unpacks + 32 fmas per row-block with zero integer fixup ops (the 1/256 and 1/16 corrections vanish because unpack yields true values).

TWO SPEC TRAPS RECORDED: (a) the packed_numeric_type constructor takes storage_type (packed_vec<uchar,N/2>), NOT an integer scalar — passing a uint compiles but scalar-converts and broadcasts one byte (probe: 0x87654321 -> [1,2,1,2,...]); construct via as_type<P::storage_type>(word). (b) N=16 unpack of a 64-bit word is legal (Table 2.19 caps uint4b at N=16), lane order is little-endian nibbles with true integer values (probe: 0xFEDC...3210 -> [0..15]) — the q4_K interleave (even lanes low plane -> yl[b], odd lanes high plane -> yl[b+8]) maps directly, NO repack needed (the body's repack caveat was unnecessary).

CORRECTNESS: candle-core test_matmul_mv_unpk_accuracy PASSES on the M3 running the real kernel — mean_rel 1.63e-3, max_rel 4.11e-3, signed bias 1.69e-4 = the bf16-activation floor, same signature as the shipped kernel's wide-accuracy test.

PERFORMANCE (the decision measurement): quant-matmul-bench, lm_head m=1 Q4K bf16_direct (248094x1024), warmup 20, iterations 200, rotated ABBA-AB within one session on a verified-quiet machine (aerial-wallpaper GPU contamination found and eliminated first — screensaver now disabled on the M3): default 1024.5 / 1025.9 / 1026.0 us; unpk 1138.3 / 1134.8 / 1134.7 us. unpk = 0.90x baseline — a 10.7 pct REGRESSION with sub-us within-arm spread. Kernel identity is established by differential: no fallback warning in the unpk arms (the router warns once on load failure), the 4.1 library compiled, and the arms separate cleanly and reproducibly — a silent fallback would produce identical times.

DECISION RULE APPLIED: 0.90x < 1.15x -> close the packed_numeric line. The shipped masked-FMA kernel stands; the hardware unpack does not beat two cheap int ops per MAC on this workload. The unpk kernel STAYS in the fork behind the env flag as the reproducible negative (same policy as the falsified wide/mc/mvbf variants).

NOT DONE (optional, decision-irrelevant): the gpudebug instruction-mix comparison of what unpack lowers to. First capture attempt produced empty traces — Metal capture records only command buffers created inside the window, candle pre-creates one, so a bounded 3-forward capture recorded nothing; fix = synchronize() right after capture start to force rotation (committed to the bench capture hook). The profiling attempts also wedged the machine-wide GPU counter-session singleton twice (stale session, then kill -9 orphaning the lock; lessons persisted to the metal-gputrace-cli skill). If the lowering answer is ever wanted it is a 10-minute task once a real trace exists; it cannot change this verdict.

FOLLOW-UP FILED: matmul2d tensor-ops (MSL spec Table 7.3) support bfloat x uint4b_format -> float IN HARDWARE on OS 26.4+ — irrelevant to the m=1 GEMV but a genuine candidate for the m>=2 wide/verify lane; filed as its own exploratory ticket. Step 4 (baseline cross-check) was completed earlier; steps 1-3 are now done -> ticket complete.
