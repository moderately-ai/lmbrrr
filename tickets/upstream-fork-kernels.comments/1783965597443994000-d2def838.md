---
id: 1783965597443994000-d2def838
at: 1783965597
---
REPURPOSED AS THE PR QUEUE (2026-07-13): first concrete item is the SharedEvent PR (user-sanctioned exception to the upstreaming deferral): candle-metal-kernels shared_event.rs + CommandBuffer::encode_signal_event + Commands::signal_event + MetalDevice {new_shared_event, signal_event, flush} — write against upstream's Mutex encoder seams (NOT our RefCell swap), route through end_encoding (fence contract), CI = fmt + clippy -D warnings + metal tests on Apple runners, style 'metal: <summary>'. DOCS CONTRACT that must ship in the PR (learned the hard way, receipts on greedy-host-path-deferred-readback): the signal guarantees ORDER not MAPPING — host reads after a signal are valid only for caller-allocated shared-storage buffers; op outputs from the private pool are never contractually CPU-readable (their mapping is allocation-size luck). Also include the flush-before-blocking-wait requirement (uncommitted-tail-signal deadlock). Soak: async readback shipped as default 2026-07-13; open the PR after a few days of local mileage.
