---
id: 1783910492943588000-1db85a57
at: 1783910492
---
PROTOTYPE LANDED + MEASURED (lmbrrr a2730b6): --lag-schedule (default OFF). Implementation: propose_device keeps tokens+packed confidences on device; verify chunk assembled device-side (cat anchor + token chain, narrow to width); proposal readback rides the verify-targets drain — 2 drains/drafted round -> 1. Width for round r from round r-1's vector; first drafted round + --tree stay synchronous. Flag-off path verified byte-identical (propose refactor equivalence). A/B VERDICT (calibration split, 2 arms x 3 reps rotated): (1) the drain kill is REAL — drafted-round residuals off +0.09 -> lag -0.34 median (l=1 -0.37, l=4 -0.30), ~0.4-0.5ms per drafted round. Honest correction to the ticket's evidence: measured saving is BELOW the ~1-2ms OS-wait estimate — same lesson as wave-2, host-side cost estimates run high. (2) End-to-end WASH: +0.3% over the 12 non-divergent questions, -0.6% raw; weak classes +0.2-2.6%, but width staleness taxes the strong classes exactly where drafting pays (math q403, SAME committed text: width-3 rounds 16 -> 4, skips 35 -> 50, -7.5% — the offline sim's 2% mean regret underestimated the hysteresis feedback loop, its known blind spot). DECISION: default stays off; flag ships. REMAINING UNLOCK PATHS, in EV order: (a) adaptive sync — lag by default, pay the synchronous drain only when the lagged decision sits near a scheduling boundary (recovers most of q403's loss while keeping most drains killed); (b) full on-device width selection (the ticket's harder variant — admission argmax on device); (c) accept as-is only if a future stack makes drafted rounds much cheaper relative to the ~0.5ms drain. The instrumentation (proposal_confidences) and the device-proposal plumbing are the reusable assets either way.
