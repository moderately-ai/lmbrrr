---
id: 1783963091161426000-d2a4f61a
at: 1783963091
---
REPRICED UP (2026-07-13, M3 refit finding): the spec loop's per-round sync gives it a 6.35ms/token greedy floor on the M3 vs the device-chain's 5.5ms — the loop overhead alone (~0.85ms/token, 15%) eats more than speculation's best-class yield down-tier. Killing the round-boundary sync (this ticket + the SharedEvent pattern from greedy-host-path-deferred-readback) is now the gating item for spec being net-positive on base-tier hardware at all, not an optimization. The refit cost model (draft 14.25ms on M3 vs ~5 on M4) also says the DRAFTER PROPOSE path is host-dominated — its dispatch census belongs in this lane's scope.
