---
id: 1783967472070850000-356f2af8
at: 1783967472
---
ADAPTIVE-SYNC PARKED (2026-07-13, sim-design validation): the width-agreement oracle EV computes to 0.688 x 0.45ms = ~0.31 ms/round = +1.1-1.2% tok/s — an UPPER BOUND (the sim structurally omits the hysteresis feedback that turned a +2% offline estimate into a measured -7.5% on math), and a practical margin trigger captures only a fraction. Above the prototype gate, below the +1.5% ship bar: not worth hot-path code while order-of-magnitude larger levers are open. IF REVIVED: (a) the per-round data needed is NOT currently reconstructable from mixed-run reports (three per-round lists with three different membership sets, no round index) — the one-field fix is promoting proposal_confidences entries from {logits, width} to {logits, width, accepted}; (b) corpus must be pure --schedule (no tree/pld/recycle) for alignment; (c) the boundary-distance trigger is the relative gap between best and runner-up Appendix-A f-values evaluated on the STALE vector (bias-free). SCOPE REDIRECT: this ticket's live value is the device-side assembly machinery serving spec-loop-economics-recovery — a device-resident accept-mask consumed by next-round chunk assembly (the vLLM #29134 'host counts optional' recipe), not lag scheduling.
