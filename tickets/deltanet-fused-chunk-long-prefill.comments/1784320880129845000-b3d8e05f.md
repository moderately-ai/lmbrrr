---
id: 1784320880129845000-b3d8e05f
by: "claude"
at: 1784320880
---
CORRECTION to the prior comment's "streaming refuted" + the PRECISE mechanism (2026-07-17, ping-pong intervention + re-capture). The prior comment blamed streaming's L1 thrash on the in-place read-write S; that was WRONG, and the fix I built (ping-pong: separate read-only/write-only S buffers, candle 134c4856) DISPROVED it: l1_eviction 0.55 -> 0.56 (UNCHANGED), wall 14.75 -> 15.07 ms/seq (unchanged). Intervention moved the variable, the number didn't budge -> not the cause.

**REAL mechanism (measured): the 64KB/head recurrent state S exceeds M3 L1 (~32KB/core), and the SINGLE-DISPATCH design re-reads S every tile in a warm, contested L1 -> thrash.** Counters (stream vs chunk, both ~8% occupancy): l1_eviction 0.56 vs 0.01; gpu_bandwidth 6% vs 13% (r2.2/w3.9 vs r6.4/w6.9). The chunk-loop's 20 SEPARATE dispatches each start with a COLD L1 and stream S once cleanly (13% BW, 0.01 evict); the streaming kernel holds S live across 20 tiles in one dispatch and re-fetches it as each tile's proj stream evicts it (6% BW, 0.56 evict). Ping-pong doesn't matter because the thrash is S-size-vs-L1, not the access pattern. Both kernels are LATENCY-bound at 8% occupancy (xctrace: 100% GPU-busy at Max clocks, gaps ~1ms/13s -> busy STALLING; alu 6%, everything low, manager_target 100% -> register/tg-mem capped).

**RECONCILES with the hardware-scaling intuition (which was right, opposite sign):** streaming is the more SCALABLE design (one dispatch, no compile-time l cap) and WOULD WIN on hardware where L1 >= S (S stays cached across tiles, no re-fetch). It loses on M3 SPECIFICALLY because 64KB > M3's L1 — a cache-size regime mismatch, not an architecture failure. The fix that would make it win on M3 — split S across threadgroups so each dv-slice fits L1 — breaks the group-RMSNorm reduction over dv and the ks0 reduction over dk (not a small change).

VERDICT: ship the CAP=12 chunk-loop on M3 (1.27x prefill, quality-preserving). Keep gated_delta_prefill (ping-pong version — byte-correct, no slower, architecturally cleaner) + bench-deltanet in-tree, flag off, as future-hardware infra (re-test when L1 >= state). **The real M3 DeltaNet lever is OCCUPANCY: both the prefill AND the HOT VERIFY chunk kernel run at ~8% occupancy, latency-bound. Raising it (reduce the 4x[TILE*128] f32 threadgroup stages ~24KB -> bf16 or fewer arrays -> more threadgroups/core) would hide the serial recurrence latency and speeds the spec-round DeltaNet (~27% of the verify step), not just prefill.** Spawn a child ticket. METHOD WIN: wall-clock+algebra mis-diagnosed this THREE times (dispatch-bound, then S-traffic-bound, then in-place-L1); only the gpudebug counters (occupancy 8%, BW 2-13%, evict 0.56-vs-0.01, manager 100%) revealed latency/occupancy-bound + the S>L1 cache regime. The trace tools earned their keep.
