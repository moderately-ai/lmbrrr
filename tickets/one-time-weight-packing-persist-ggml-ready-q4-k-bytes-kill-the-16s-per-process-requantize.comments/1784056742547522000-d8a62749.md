---
id: 1784056742547522000-d8a62749
at: 1784056742
---
SHIPPED (2026-07-14). PackStore (src/pack.rs): GGUF sidecar (packed.gguf next to the manifest) caching the exact ggml q blocks the loader produces — same CPU quantizer as quantize_onto, so a pack hit is BITWISE identical (verified: cold == warm == blessed golden on both machines). Fingerprint = sha256(manifest) + lmbq data length + head tier + schema; mismatch/corruption -> warn, slow-load, rewrite (atomic tmp+rename). Auto-writes after the first slow load; LMBRRR_PACK=0 opt-out. Every command using load_model_with_optional_quantization benefits automatically. Also: BufReader on all 9 serde_json::from_reader(File) sites (per-byte read syscalls from the trace autopsy), and the load report now carries pack_status + quantize_seconds so the phase can never hide again. MEASURED: M4 quantize 7.65s -> 0.13s (warm total process 1.98s incl. 128 tokens); M3 7.07s -> 0.06s (warm total 1.17s incl. 64 tokens) — the <1.5s target met with margin. Battery green, goldens blessed at 6552c231 byte-identical to ancestors. GUI-TTFT blocker cleared; compounding wall-time saving on every future gate/bench/dev run.
