---
id: 1784039801758171000-7aa313f8
at: 1784039801
---
DISCOVERED via Instruments Metal System Trace on the M4 (2026-07-14, user-assisted stack sampling): a single model load runs 16+ SECONDS of single-threaded CPU work — the entire 15s capture window was load; decode never started. Autopsy from main-thread samples: (1) make_qkx1_quants / BlockQ4K::from_float dominating throughout = re-quantizing ~350MB of lmbq text tensors PLUS the 254M-value lm_head embedding to ggml q4_K at EVERY process start (the artifact stores lmbq blobs which we decode->f32->requantize; quantize_lm_head does the same to the tied embedding); (2) interleaved safetensors header parsing + unary_map bf16 CPU dtype conversions per tensor; (3) serde_json IoRead<File> byte-at-a-time reads through std::io::Bytes<File> (unbuffered from_reader anti-pattern) — 100+ms of read syscalls. NONE of this is measured by any report field (load_seconds covers only the mmap), so it has been invisible all campaign; bench loads once per process (verified run_bench.rs:253), so every gate/bench/suite/dev run paid it. FIX: (a) one-time packing pass persisting GGML-READY q4_K blocks (all requantized text tensors + the quantized head + FR-Spec-sliced drafter heads if applicable) as a versioned sidecar next to the manifest, keyed by (source hash, policy, head tier); load = mmap + upload, zero quantization; (b) BufReader/read-to-string every serde_json::from_reader(File) site; (c) add quantize_seconds/pack_load_seconds report fields so this phase can never hide again. TARGET: cold start 16s -> <1.5s. This also directly serves the GUI's TTFT (gpui tickets), the eval-distribution-cost first-launch story, and returns hours of aggregate M3/M4 wall time.
