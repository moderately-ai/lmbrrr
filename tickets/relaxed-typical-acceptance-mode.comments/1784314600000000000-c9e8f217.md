---
id: 1784314600000000000-c9e8f217
by: "claude"
at: 1784314600
---
PEAKEDNESS GATE REFUTED (2026-07-17, same-day follow-up; MEASURED, M3, v2, n=1 prompt/class): --margin-peak (margin only on rows with top_logprob >= -peak, exact elsewhere) does NOT fix the factual drift. peak 2.0 = no-op (0 flat rows on factual AND prose — sub- -2 top-logprob rows essentially never occur at draft positions; ids byte-match ungated). peak 1.0: factual PPL unchanged (+21.6% vs +21.4%) with only 14/176 rows flat — the drift lives in ORDINARY mid-peaked rows where a margin-3 draft is a different plausible continuation and the whole divergent trajectory scores worse; prose peak 1.0 actually WORSENED PPL (+8.8% vs +5.4%) — forcing exact on one row reroutes the trajectory, and teacher-forced PPL is not monotone in per-token strictness. ALSO MEASURED: factual accept at margin 3.0 is intrinsically ~2.0/round (12.9 tok/s v2, this prompt) — the margin buys factual little speed anyway. Verdict: keep --margin-peak as a knob (default off), do NOT pursue peakedness as the quality lever; the per-class margin POLICY question (A4) remains open but its mechanism is trajectory-level, not row-level. Caveat both directions: single prompt per class; trajectory effects dominate at n=1.
