---
id: 1784052208321267000-192dbe73
at: 1784052208
---
CORRECTION + FINAL A2A (2026-07-14, live m-sweep via quant-matmul-bench --token-counts on M3, /tmp/qmv-sweep.jsonl): the '+8% greedy prize / 1.36x m=1 gap' is RETRACTED — it was a replay-vs-live artifact (our 1455us figure was gpudebug replay at max perf-state; live m=1 lm_head Q4K/BF16 = 1033us, at parity with llama.cpp's 1064us live microbench — consistent with our kernel being structurally their kernel). Lesson re-learned: never compare replay numbers to wall-clock numbers (it is the skill's own caveat). WHAT SURVIVES, live-confirmed: lm_head BF16 m-ratios OURS 1.80x/2.21x/2.58x/4.19x (m=2/3/4/8) vs llama.cpp 1.59x/2.31x/3.54x/7.05x vs MLX ~1.00x/1.28x/1.63x/3.07x. We already match/beat llama.cpp at m>=3; MLX's qmv_wide design is the only stack with a real batch discount and the sole remaining target. ATTEMPT #3 SCOPE NARROWED to one kernel: q4_K wide-mv for m in [2,8]. Design (grounded in qmv_wide_impl read + our mv kernel read): 2 simdgroups/tg, k_lanes=8 -> 4 rows/simdgroup; each lane owns one 32-value q4_K sub-block per superblock (k=1024 -> 4 superblocks -> 4 sub-blocks/lane, striding 8); per sub-block: extract (scale,min) once, read 32 q bytes once as 16xu16, single-nibble-plane masked-FMA per STREAMED VECTOR (llama.cpp-style, no dequant materialization) + per-vector sumy for the algebraic dmin term; per-lane registers = 2 accumulators x vecs (vecs_per_tg<=4, M-tiles re-read weights like MLX); shuffle-ladder reduce over k_lanes. Margin-class expected (new summation order): oracle + goldens re-bless + ppl battery, NOT bitwise. Contract: lm_head m=2 <= 1.15x m=1; e2e verify chunk2 <= 1.4x decode. Routing: m in [2,8] -> wide (env LMBRRR_Q4K_WIDE=0 opt-out), m=1 and m>8 unchanged. Small-shape sweep rows are dispatch-noise-dominated (20-70us) — judge on lm_head + e2e verify table only.
