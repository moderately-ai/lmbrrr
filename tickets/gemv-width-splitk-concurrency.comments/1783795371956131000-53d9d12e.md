---
id: 1783795371956131000-53d9d12e
by: "claude"
at: 1783795371
---
Hypothesis test #2 FALSIFIED (2026-07-11): block-staged q4_K mv variant (same N_DST=4 tile, ALL weight loads vectorized - d/dmin as half2, scales as 3x u32, nibble planes as 8B uint2; ~13 scalar 2B reads -> 6 wide loads per row-block) measured FLAT end-to-end: g1 +0.5%, g2 -0.0%, g7 -0.3%. Side observation: the expression restructure produced ulp-level fast-math reassociation drift (coherent alternate greedy text - advisory class). Reverted.

Two falsifications now bracket the problem: the ~90 GB/s q4k-mv ceiling on k=1024 shapes is NOT row-tile width (test #1) and NOT memory transaction count (test #2). Remaining suspects: the nibble-unpack ALU chain (int mask + convert + fma per element - possibly issue-width bound), 32-thread threadgroup scheduling behaviour, or something in the y-gather pattern. DIRECTIVE FOR ATTEMPT #3: no more blind kernel edits - take an Xcode GPU capture / Metal performance counters profile of a decode forward first (occupancy, ALU vs LSU utilization, stall reasons on the lm_head dispatch), then design against the measured limiter. The MLX qmv structure remains the reference design if the profile points at ALU issue width (it dequantizes via wider integer ops).
