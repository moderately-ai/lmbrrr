---
id: 1784053960786496000-3b3ea445
at: 1784053960
---
WIDE KERNEL SHIPPED (2026-07-14, pin 6ad9320c). Attempt #3 succeeded after one in-flight falsification: v1 kept llama.cpp's fused mask-FMA inside the streamed-vector loop (re-paying the int unpack per vector) and measured WORSE than mc (lm_head m=2 2.25x vs 1.80x); v2 dequantizes each 32-value sub-block ONCE into float registers (MLX qmv_wide's actual amortization) with pure-FMA per vector. MICRO (M3, warm, lm_head): m=2 1.44-1.48x (mc 1.80x, llama.cpp 1.59x, MLX ~1.0x — best ggml-format stack; MLX gap remains as a tuning follow-up: k_lanes/vectorized-y variants), m=3 1.74-1.80x, m=4 2.15-2.26x, m=8 3.77-3.96x. ACCURACY: new fork test test_matmul_mv_wide_accuracy — mean rel 1.6e-3 (bf16 activation floor), signed bias ~1e-4 (guards spec-acceptance poisoning that max-error gates miss). E2E (M3, 4-prompt depth-1 A/B, wide vs LMBRRR_Q4K_WIDE=0): verify chunk2 9.41-9.56 -> 8.65-8.77ms (-8.4% every prompt); spec tok/s mean 124.7 -> 131.5 (+5.5%, wide wins all 4); acceptance parity on 3/4 prompts (tides delta = content variance from the margin-class trajectory split, ruled a non-issue by the accuracy test + multi-prompt parity). Greedy goldens BYTE-IDENTICAL through both pin bumps (m=1 path untouched); drafter golden re-blessed margin-class with oracle receipts. Depth-2/3 verify also improved (10.92->9.92, 12.54->11.81). Remaining for the spec verdict: the phase-2 harness items (FR-Spec slice, chained draft, async round loop) — verify is no longer the sole blocker but the round still spends 2.4ms drafting + 4.3ms host. Gates: fork 59 tests + accuracy test + mv_mc test (bf16-dst bitrot fixed), lmbrrr extended battery green x2, oracle dev 0.375.
