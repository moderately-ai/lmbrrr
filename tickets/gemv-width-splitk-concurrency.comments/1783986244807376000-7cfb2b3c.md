---
id: 1783986244807376000-7cfb2b3c
at: 1783986244
---
MLX split-K #3120 READ (merged 2026-03): qmm_t_splitk partitions K across the grid's z axis into an intermediate [split_k, M, N] buffer + one strided sum-reduce; split factor targets ~512 threadgroups, capped by K/group_size and divisibility, falls back to plain qmm at split_k<=1; measured +25-30% at D=2560-4096, M=12-16 on M3 Max. TWO TRANSFER CAVEATS for us: (1) our verify K dims are small (hidden 1024; q4_K superblock 256 -> split_k <= 4), so the win must come from mc-kernel occupancy at m=2-4 rather than MLX's large-K regime — the micro gate decides; (2) the cross-partition sum changes accumulation order, so this is NOT bit-preserving: gates are margin-based text identity + oracle, never bitwise (protocol already says so). Kill-early micro gate launched on the M3 (mc-micro: qmv session controls bracketing the qmm mc-vs-mm sweep at verify widths).
