---
id: 1783986504776441000-98430c46
at: 1783986504
---
MC KERNEL READ — the slope has two separable causes, each with a scoped fix. (1) BEYOND m=4: the grid launches ceil(m/NC_MV_Q4_K=4) column-groups and EACH re-reads the full weight matrix (m=12 -> 3x bytes — exactly the measured 1/3 GB/s collapse). Fix B: widen NC for q4k (4 -> 8; Q8_0/Q6K are already 8) — bitwise-preserving (per-(row,c) accumulation order unchanged, only fewer weight re-reads), sumf grows to 4x8 floats/thread; AND/OR per-shape routing of wide matrices to mm at m>=8 (measured -25-35% on dn_qkvz at m=12, but o_or_down prefers mc — routing table not blanket). (2) AT m=2-5 (the MTP verify regime): one column-group already, but the mc kernel never received the nr2sg2 geometry that bought the mv kernel +30% — it runs ~half the mv's efficiency (51.5 vs 98.3 GB/s on the head at m=4). Fix A: port the nr2sg2 row/simdgroup geometry into an mc variant — NOT bitwise (geometry changes accumulation order), gates are margin-based text identity + oracle per protocol, benched at pipeline depth 1. Fix A is the one that moves verify(2) toward the 6.5ms break-even; Fix B moves the DSpark gamma-6 regime. Build order: B first (small, bitwise-safe, immediate gamma-6 win), then A (the tuning round).
