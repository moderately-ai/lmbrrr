---
id: 1783854412422702000-71391d16
by: "claude"
at: 1783854412
---
Hypothesis test #3 FALSIFIED (2026-07-12): half-precision inner accumulators (nibbles shifted to 0..15 to avoid half overflow, end-scales absorbed, f32 combine per block) measured g1 4.74 -> 4.91 ms (+3.5%), g2/g7 slightly worse. Combined with tests #1 (row tile) and #2 (load shape), the limiter is now pinned: the q4_K mv kernel is INTEGER-UNPACK-PIPE-BOUND - the AND/shift/convert chain (~2/3 of per-element ops) runs on integer/conversion units that half-rate FMA does not accelerate. Any win must ELIMINATE per-element integer work, not cheapen the FMA. The one remaining credible direction is a simdgroup_matrix redesign: dequantize each block once into half tiles (amortizing unpack over the tile) and replace scalar FMAs with simdgroup matrix ops - a substantial kernel project, right-sized for a dedicated session, with bounded upside ~1 ms/token on the lm_head (99 -> toward 262+ GB/s effective). Until then the q4k mv stands as-is; the practical mitigations already shipped elsewhere (FR-Spec cut the DRAFTER's head cost -35%; the target head cost is the residual).
