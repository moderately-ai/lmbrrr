---
id: 1783987953647249000-2bbb5f13
at: 1783987953
---
DOOR-A SIZING (MLX qmm read, quantized.h @ main): their ~3x comes from dequant-to-threadgroup-tiles (QuantizedBlockLoader) + steel BlockMMA (simdgroup 8x8 matrices) — the llama.cpp mul_mm architecture. KEY REDUCTION FOR US: the fork already ships MLX's steel MMA machinery for dense gemm (kernels/mlx_gemm.rs + metal source), so the port is (1) a q4_K/q8_0 GGML-BLOCK-LAYOUT tile dequant loader (~150 lines per format — MLX's loader assumes their affine layout, ours must unpack ggml superblocks into the tile), (2) instantiating the existing BlockMMA at small-M tiles (their qmm_t handles M<32 with masked stores), (3) routing m=2..13 to it. NOT bit-preserving (tile accumulation order) — margin gates + suite text battery + pipeline-depth-1 bench. Cross-reference the closed simdgroup-matrix K1 ticket's closure reasons before starting (that was m=1 mv where MMA had no reuse to exploit; at m>=2 the x-tile reuse is exactly what MMA is for — different regime, closure does not carry over). Estimated arc: 2-3 sessions with gates. This ticket now IS the door-A arc.
