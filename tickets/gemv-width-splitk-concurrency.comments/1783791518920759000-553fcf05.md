---
id: 1783791518920759000-553fcf05
by: "claude"
at: 1783791518
---
REPRIORITIZED p3->p1 (user directive 2026-07-11): "if there are known performance issues we should address them before judging if a technique works or doesn't work." Add explicit fourth item, likely first to execute: the VERIFY-CHUNK INTERCEPT. Measured verify cost at l=2 is 1.77x a decode step; roofline says ~1.05x (weight reads dominate, extra rows nearly free). Same family as the aggregate batch-scale gap (860 vs 1530 at N=32): small-l batched qmatmul does not scale. This intercept is the denominator in every speculation-economics verdict - it sets recycling's depth-1 break-even at ~77% acceptance (vs ~50% at roofline cost), priced ungated PLD out, and made the two-branch tree "break-even at best". Those verdicts are all PROVISIONAL until this is fixed and the cost model re-measured (remeasure-spec-round-cost-model after landing). Diagnosis first: l-sweep of verify cost {1,2,4,8,12} on the current binary, then read the fork's quantized matmul Metal path (mv-per-row dispatch suspected).
