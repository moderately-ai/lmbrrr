---
id: 1784048425813153000-a3feb482
at: 1784048425
---
UNBLOCKED (2026-07-14): the attempt-#3 prerequisite (per-dispatch GPU counter profile) is now executable programmatically — macOS 27 gpucapture/gpudebug on the M3 give per-command occupancy/limiter/stall counters headless (see m3:~/gpu-runs/run-001 for the working flow). NEW MOTIVATION: this ticket is now THE spec-lane linchpin. Distilled-MTP sweep (mtp ticket, 2026-07-14) shows acceptance solved (p1 0.72) but verify chunk2 = 9.45ms = 1.87x decode kills the economics; contract target: chunk2 <= 1.4x decode, chunk4 <= 2.0x. EXECUTION ORDER (no blind edits, per the standing directive): (1) gpudebug counter profile of the lm_head mv dispatch at m=1 AND mc dispatches at m=2/4 inside a bounded verify-chunk capture; (2) apples-to-apples llama.cpp/MLX q4k matmul at our shapes x m in {1,2,4,8} on the M3 — attainable-target table + reference geometry; (3) implement against the measured limiter: option B column-parallel mc (simdgroup-per-column, TG=NCx32, block staged once — design already on this ticket) or MLX qmv port, per what the profile says; fix the latent OOB guard (vocab 248094 % 4) while in there. Gates: parity/battery, verify l-sweep {1,2,4,8,12} re-measure, cost-model refit + drafter-golden re-bless same-run.
