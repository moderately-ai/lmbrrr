---
id: 1783954882233514000-b6f76522
at: 1783954882
---
SAME-MACHINE DATA POINT (2026-07-13, M3): llama.cpp's ext (column-parallel) kernel at n=4 on 8192x1024 ran 4.33x the n=1 time — SLOWER than linear scaling, i.e. column parallelism does not amortize weight reads at cache-resident body shapes (the weights are already in SLC; there is no DRAM traffic to share). Implication: the mc port's verify-width value concentrates ENTIRELY on head-sized rows (143MB, genuinely DRAM-bound) and the verify-table slope should be re-modelled as head-dominated before building. This shrinks the expected win vs the original ticket estimate; the kill-early micro gate stands.
