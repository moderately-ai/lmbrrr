---
id: 1783793820678542000-cd5618c4
by: "claude"
at: 1783793820
---
Intercept re-attribution strengthens this ticket (2026-07-11): after the DeltaNet v2 chunk kernels landed, the l=1->2 verify step moved only 3.65->3.44 ms — DeltaNet owns almost none of it. The step decomposes across the l>=2 mode switches: quantized mv->mc (186 linears; micro-bench shows lm_head q4k/bf16 at n=2 costing 1.91x n=1 DESPITE the mc kernel — the mc variants appear to scale per-thread-arithmetic-linearly in m rather than amortizing the weight read), SDPA vector->full (6 layers), and residual dense matvec->mm switches. Dense-only sweep shows +7.65 ms at l=2 with near-free rows after (0.3 ms/row) — pure mode-switch cost. Diagnosis entry point: profile/optimize the mc kernels' m-scaling first (grid height=ceil(m/NC) with per-thread m-loop), then SDPA full for l<=12.
