---
id: 1783986422473447000-f702a7fd
at: 1783986422
---
MICRO GATE: GO (M3, session controls valid within 1%). The verify slope's kernel mechanism is measured: q4k head runs 98.3 GB/s at m=1 (mv) but 51.5 at m=4 (mc), and the small verify-shape matrices collapse to 12-20 GB/s at m=8/12 — mc time grows near-linearly in m (weights re-streamed per row, no cross-column amortization), which reconstructs verify(4)=12.83ms as ~420MB at ~33 GB/s effective. The mm variant is flat in m (lm_head 4.70ms at m=8 AND m=12) but pays BM=32 tile quantization at small N (15 GB/s on mlp_fused). HEADROOM at the m=2-7 verify regime: ~1.9x on the head, 4-6x on small-N shapes — far past the 1.3x kill bar. Prize restated: flat-in-m kernels put verify(2) near the m=1 floor (~6.5ms) = spec beats greedy at current MTP acceptance. CHEAP CONFIG WIN also measured: routing m>=8 to mm for WIDE matrices (dn_qkvz q4k 0.599->0.453ms, q8 0.841->0.605 at m=12) — per-shape routing table, but o_or_down prefers mc (0.059 vs 0.072). NEXT: read our mc kernel geometry, design the m-amortized variant (process all m columns per weight load), pipeline-depth-1 bench per the new instrument rule.
