---
id: 1783988969029356000-d412a9bb
at: 1783988969
---
DOOR-A DEFLATED BY HARDWARE NORMALIZATION (before building, per protocol): MLX's 2.9 TFLOPS qmm number is from an M3 MAX (~2x our M3 Pro referee's compute peak) — normalized, MLX-class would land ~1.4 TF effective on our box ~= the 1.3 we already get. Our mul_mm IS llama.cpp's simdgroup-MMA kernel already; its small-m cost is dominated by fundamental 32-column tile padding (2.7x dead compute at m=12), not a bad inner loop. Realistic ceiling for the m=2-5 verify increment is ~2x (mc scalar-dot -> tuned/MMA hybrid), which puts verify(2) at ~7.6ms -> MTP depth-1 ~168 tok/s at tau 1.53 — STILL UNDER the 182.6 greedy floor. CONCLUSION: door A alone cannot cross the line; MTP distillation (tau 1.53 -> ~1.9) is the NECESSARY condition, door A the amplifier. Ticket parked pending the distillation decision; do not start the tile-loader port on the current economics.
