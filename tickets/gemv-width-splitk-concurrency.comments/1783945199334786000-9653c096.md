---
id: 1783945199334786000-9653c096
at: 1783945199
---
EXECUTOR PROCEDURE for the remaining live item on this ticket: mc (multi-column / verify-width) column-parallel kernel, the llama.cpp `mul_mv_ext` pattern. The width-fusion and barrier items earlier in this ticket are DONE/superseded (see dossier docs/research/metal-decode-utilization.md); split-K was falsified in q4k-mv-rewrite-round2. What remains is verify-path throughput: at verify width w in 2..7 (spec decode), the current mc kernel processes columns with poor per-column parallelism; llama.cpp's `kernel_mul_mv_ext_q4x4_f32_r1_N` tiles r1ptg columns per threadgroup with per-column register accumulators (sumf[r1ptg]) and reports ~1.5-2x at m=2..8.

WHY IT MATTERS BEYOND VERIFY: a faster verify[w] curve re-opens TWO closed economics: tree speculation (tree-speculation-over-dspark, killed at current verify slopes) and recycle top-k width (verify-logit recycling break-even). After shipping, BOTH break-evens must be recomputed.

IMPLEMENT (fork, candle-metal-kernels): port the ext structure into quantized.metal as `kernel_mul_mv_q4_K_bf16_bf16_mc_rN` for r1ptg in {2,3,4,5}: outer structure identical to the existing mv impl, but each thread loads r1ptg activation values (one per column) and keeps sumf[r1ptg] accumulators; columns share every weight load (the whole point — weight traffic amortized across the verify width). Host: extend call_quantized_matmul_mv_mc in src/kernels/quantized.rs to pick rN by ne11 (w+1), keep the existing path as fallback for w outside 2..5. Reference source: llama.cpp ggml/src/ggml-metal/ggml-metal.metal, search `mul_mv_ext_q4`.

GATE 1 (micro, kill-early): add an `mc-sweep` task to candle-metal-kernels/examples/metal_benchmarks.rs (clone nsg-sweep harness) benching m in {2,3,4,5,7} on the verify shapes (dn_qkvz 8192x1024, mlp_gate_up 7168x1024, mlp_down 1024x3584, lm_head 248094x1024) with a bitwise gate: new kernel output must be bit-identical per (row, column) to the existing mc kernel (same fma order per column — keep the inner block loop untouched). KILL CRITERION: < 1.3x within-session at m=4 on the body shapes -> record + close this item (weight amortization should give ~m-fold ceiling; if it doesn't materialize at m=4 the port isn't capturing it).
GATES 2-4 (production): nextest 59 -> rebuild -> stub oracle (dspark-run, invariance true, dev <= 0.75) -> `./target/release/lmbrrr tree-check --quantized-manifest artifacts/minicpm-v46-q4k-full-text/manifest.json --quantize-lm-head q4k` -> spec text-identical: `dspark-run --drafter artifacts/dspark-drafter-round4 ...` committed_text byte-equal to pre-change binary on the tides prompt.

MEASUREMENT: verify table first — `./target/release/lmbrrr bench` won't show it (greedy); use the profile-verify path: run_spec_suite calibration pass (`python3 evals/run_spec_suite.py --arm round4=artifacts/dspark-drafter-round4 --split calibration --per-class 3 --reps 1 --gamma 6 --tag mc-refit`) and compare the fitted verify[w] slope in the refit cost model vs artifacts/spec-round-cost-model-fusion.json. Then full A/B: validation suite (`--split validation --per-class 3 --reps 3 --tag mc-ab`) vs the deployed binary, per-class means + overall mean tokens/s, rotated arms (suite handles arm rotation; run controls first per memory protocol; DIVERGENT-CONTENT classes excluded from means as usual).
DECISION: overall validation mean >= +2% -> ship (default on), bump pin, refit cost model in-loop, THEN re-run tree break-even (tree-check economics with new verify slopes; reopen tree-speculation-over-dspark if EV flips) and recycle top-k width sweep. < +2% but verify[w] slope measurably flatter -> keep behind env flag and reopen the tree EV computation anyway (the slope, not the mean, is what tree needs). Flat slope -> falsified, record + close.
