---
id: 1783987864905330000-b78202b3
at: 1783987864
---
NC=8 VERDICT (M3, goldens byte-identical as predicted): KEPT but the expected win FALSIFIED — m=8 mc -9%, m=12 -1% (M3-to-M3; my M4 'confirmation' compared across machines and was meaningless — logged as a protocol violation). THE SLOPE IS COMPUTE/ISSUE-BOUND, not weight-re-read-bound: time grows ~linearly in m while weight bytes are constant (that IS why GB/s-of-weights falls — wrong denominator, same error class as the dispatch pricing). FLOPS check: our mc/mm run ~1.3 TFLOPS effective at verify shapes vs MLX's qmm ~2.9 TFLOPS on the same class (their PR numbers) and ~7 peak — ~20% ALU efficiency from the scalar unpack+dot inner loop. Verify table at NC=8 (synchronized medians): chunk2 8.9ms, +~1.5ms/row to chunk12 26.4ms. CONSEQUENCE: occupancy/split-K class fixes CANNOT flatten a compute-bound slope — the remaining lever is a simdgroup-tile (MMA) inner loop for the m=2..13 regime, i.e. the K1-keystone class of work applied to mc/mm (the m=1 K1 was closed; m>1 is unexplored and MLX demonstrates ~2-3x). That is a fresh multi-session kernel arc; the alternative door for spec viability is MTP tau via distillation (user-gated). Fix A (nr2sg2 mc port) is DOWNGRADED: under a compute-bound model its expected value at m=2-5 is modest — fold into the simdgroup arc decision rather than building standalone. DSpark standings under NC=8: mean ~119 (flat, within noise).
