---
id: 1783807739260530000-4fa1aec6
by: "claude"
at: 1783807739
---
LIMITER ISOLATED from existing data — no new instrumentation needed (2026-07-11). Effective bandwidth on the lm_head shape (248k x 1024, n=1) tracks unpack complexity, not bytes: dense bf16/f16 354-358 GB/s (roofline), Q8_0 262, Q6K 127, Q4K 99. The q4_K mv kernel is ALU/ISSUE-BOUND on its nibble mask/shift/scale-decode chain. This also explains falsified tests #1/#2 (row tile and load shape do not touch the unpack chain).

Tested the tempting shortcut — flip lm_head to Q8_0 (isolated kernel 1.031 vs 1.449 ms): END-TO-END NEGATIVE, -1% to -6% across all six held-out classes; the 270 MB read competes with the rest of the in-stream forward in ways the synced micro-bench hides. Interesting side-signal worth its own note: q8 head IMPROVES tau (coding 1.75 -> 1.92, math 1.97 -> 2.03) — head fidelity shifts near-tie argmaxes toward the greedy baseline. q4k head remains the deployment config.

ATTEMPT #3 DESIGN CONSEQUENCE: the win must cheapen the unpack arithmetic itself in kernel_mul_mv_q4_K — candidates: byte-wide LUT dequant, wider integer ops processing two nibble pairs per instruction (llama.cpp's newer arm/metal paths), or fp16 accumulation of the inner products. Target: close part of the 99 -> 262 GB/s gap to Q8_0's unpack cost. The old direction (grid/tile geometry) is dead.
