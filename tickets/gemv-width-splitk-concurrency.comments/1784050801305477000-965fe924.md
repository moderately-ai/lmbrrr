---
id: 1784050801305477000-965fe924
at: 1784050801
---
PHASE 0 COMPLETE (2026-07-14, M3 Pro macOS 27; artifacts m3:~/gpu-runs/run-002). Per-dispatch counters (gpudebug, MTP round-40 capture): (1) m=1 mv lm_head dispatch: 3.26B instructions, 61.4% INTEGER/conditional vs 34.6% float — dequant-unpack ISSUE-BOUND, ~2 int instrs per MAC; (2) m=2 mc lm_head dispatch: 2.39B instrs for 2 columns (unpack DOES amortize — fewer instrs than mv for 1 column) but HALF the threads (1.98M vs 3.97M) each with a ~50% longer serial chain — OCCUPANCY/LATENCY-bound, confirming the serial-column-loop diagnosis. Round profile: mc kernel = 77.4% of round GPU time (69 dispatches); 6 CBs/round (sync receipt for spec-round-async-loop). A2A AT OUR SHAPES (llama.cpp bf2c86d test-backend-ops + MLX 0.29.3, same machine): llama.cpp m=1 lm_head 1064us (134GB/s, 90% roof) vs OURS 1455us — 1.36x gap = ~+8% GREEDY prize in the m=1 unpack path. Batch ratios at k=1024: llama.cpp m=2 1.59-1.83x (their k=14336 1.38x does NOT transfer — granularity); MLX m=2 lm_head ~1.00x (FREE: 1297 vs 1315us), m=3 1.28x, m=4 1.63x, m=8 3.07x — the batch discount EXISTS at our shapes and MLX's design achieves it. REVISED TARGETS: (a) m=1 lm_head <= 1.1x llama.cpp (greedy +~8%); (b) chunk2 <= 1.15x decode (MLX-proven) -> spec round ~6.6ms at tau 1.71 -> ~250-260 tok/s vs improved greedy ~210. ATTEMPT-#3 DESIGN REFERENCE: MLX quantized.h qmv/qmm_n structure (rows amortized per weight block, wide-int unpack) — read in full before writing MSL. The two failure modes need different fixes and one kernel family: wide-int unpack helps m=1; row-amortized processing helps m>=2.
