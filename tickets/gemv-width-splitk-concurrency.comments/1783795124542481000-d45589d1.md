---
id: 1783795124542481000-d45589d1
by: "claude"
at: 1783795124
---
Hypothesis test #1 FALSIFIED (2026-07-11): a q4_K small-k mv variant with N_DST 4->16 rows/simdgroup + 8-byte vectorized nibble-plane loads compiled, passed parity (greedy text identical, 49/49 tests), but REGRESSED end-to-end: verify-table g1 4.74 -> 4.97 ms (+4.9%), g2/g7 flat. The serial 16-row inner loop lengthens the per-thread dependency chain more than the y-stage amortization pays back; micro-bench had suggested -7..-20% on lm_head (misleading, as usual). Reverted; not committed. Surviving observations for attempt #2: (a) dense bf16 matvec hits ~343 GB/s on the SAME shape/grid style, so the gap is q4k-unpack-specific - likely the per-row 2-byte scale/q transaction pattern and unpack ALU chain, not row count; (b) design candidates: stage the whole 144B block via uint4 16B loads once per (thread,block) and unpack from registers, or adopt the MLX qmv structure (8 simdgroups/TG, vectorized u32 loads, k-loop); (c) latent OOB found: base q4_K mv (and q4k mc) write dst rows unguarded - vocab 248094 % 4 != 0 writes 2 rows past the buffer every lm_head call (hidden by page slack); fix the guard whenever this kernel is next touched.
