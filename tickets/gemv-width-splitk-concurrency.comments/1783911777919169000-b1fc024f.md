---
id: 1783911777919169000-b1fc024f
at: 1783911777
---
RE-SCOPED (2026-07-12 post-wave census + fusion prior-art). Most of this ticket is SUPERSEDED: width-fusion shipped (K2); the +8-12% claim predates the falsification ledger; barrier-minimal encoding is falsified (barrier-probe 0.85-1.0, and MLX ConcurrentContext confirmed by the upstream agent to help only INDEPENDENT groups, not our serial decode chain); the 'profile before attempt #3' demand was met (the utilization dossier ruled mv issue-rate-bound, now three-level confirmed — see the closed simdgroup-matrix ticket). ONE SURVIVING ITEM, well-specified now: verify-chunk mc COLUMN-PARALLELISM. Our q4_K mc runs the NC-column loop SERIALLY per thread (quantized.metal ~8233), so m=2 costs ~1.9x m=1 — the verify-chunk intercept that prices every speculation verdict (tree break-even, recycling depth, PLD). FIX (both llama.cpp and MLX converge on it): dequant each weight chunk ONCE, hold a register array sumf[NC], inner 'for c in 0..NC: sumf[c] += dot(w_chunk, y[c])'. REFERENCE: llama.cpp kernel_mul_mv_ext_q4x4_f32_impl (ggml-metal.metal:3836) — already a q4_K kernel with our simdgroup-per-N-rows layout, float4x4 chunks matching the 256-block, r1ptg (columns/tg) a COMPILE-TIME template so we emit one specialization per verify width 2..8. Two cautions from their code: reduce with a partial simd_shuffle_down ladder over K-lanes ONLY (not simd_sum — a simdgroup now spans multiple output rows, simd_sum cross-contaminates); crossover to tile mm above width ~8. MLX analogue: qmv_wide (quantized.h:981); MLX #1503 measured ~5x on batched-KV. EV GATE per protocol: qmm micro-bench m-sweep {2,4,6} before/after, then verify_table + in-loop refit; only ships if it moves the verify intercept enough to re-open tree/recycling economics (re-check those after). Split-K for skinny dense shapes: separate, EV-probe on the qmv n-sweep first, likely not worth it. This ticket = the mc-column-parallelism item only now; renaming scope accordingly.
