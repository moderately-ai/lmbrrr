---
id: 1783907032254766000-6ffc2ce0
at: 1783907032
---
R1+R2+R3 LANDED as one slice (lmbrrr HEAD, fork 8b54c6ff). R1: static name tables for the binary ops (12 ops x 6 dtypes x 9 variants via concat! macro, format! fallback for exotic dtypes) and the mlx dense-GEMV tile combos (60 statics); call_binary_contiguous/strided signatures take impl Into<KernelName> so static callers are allocation-free end-to-end; also removed the stale unused binary::contiguous import. R2: load_pipeline_with_constants and load_library take a shared READ lock on cache hits, write lock only on miss (double-checked re-probe). R3 DEVIATION, with reasoning: the ticket's OnceLock-per-op call-site handles are multi-device-INCORRECT (pipelines are device-owned; statics would leak one device's pipelines into another's dispatches). Correct equivalent shipped instead: a zero-dep multiply-xor (Fx) hasher on the pipelines/libraries maps — kills the SipHash per-probe setup that dominated the short-key hit path; the residual per-hit cost is read-lock + probe + retain, which is inherent to shared Retained objects. GATES: nextest 59/59, stub oracle 0.5/0.75 text-identical, drafter smoke byte-identical vs the wave-1 binary on the same bundle (an initial mismatch traced to the wave-1 cost-model refit having changed the bundle between references — not a kernel effect). Next: R5 alone (EncoderState lock collapse) with oracle + tree-check + barrier-probe.
