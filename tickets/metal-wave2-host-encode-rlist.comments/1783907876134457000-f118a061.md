---
id: 1783907876134457000-f118a061
at: 1783907876
---
R5 LANDED (lmbrrr 14b9f3f, fork 4a3285fb) — EncoderState Mutex -> RefCell. Audit result backing it: every EncoderState access is serialized by the outer Commands state mutex (dispatch access borrows via CommandsGuard; end_encoding runs under commit/blit paths holding the same lock), and a standalone ComputeCommandEncoder is !Send/!Sync (objc2 Retained), so cross-thread movement exists only through Commands' unsafe Send/Sync — the very mutex the RefCell relies on. Tracking logic untouched; reentrancy stays checked. GATES (sync-machinery battery): nextest 59/59; stub oracle 0.5/0.75 text-identical; tree-check identical deltas; drafter smoke byte-identical + identical stats; barrier-probe 0.84-1.03 inside the recorded envelope. WAVE-2 PACKAGE MEASUREMENT (rotated ABBAAB, quiet-ish rig): steady bench greedy WASH (+0.3%/-0.3% medium/long) — host encode is in the GPU shadow, as Amdahl predicts; decode_model host wall improved +5.5% (medium) / +7.9% (long), ~0.52 -> 0.47 ms/token ≈ 110ns/dispatch — an order below the dossier's -1.0-1.6us/op estimate (worth recording: the anatomy's msg_send costs dominate and are untouched). In-loop calibration: residuals unchanged vs wave1 table (l=4 -0.13, no-draft 1.27 vs 1.30; identical sample counts = byte-identical content) — NO cost-model refit needed. Value: banked host headroom (draft loop + future floor drops) + upstream-worthy patches. Ticket complete.
