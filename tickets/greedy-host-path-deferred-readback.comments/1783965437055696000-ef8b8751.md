---
id: 1783965437055696000-ef8b8751
at: 1783965437
---
SHIPPED after one real bug (2026-07-13, full story for the record + upstream PR contract). V1 held the argmax tensors and read their buffer contents() directly — WRONG: candle op outputs allocate from the PRIVATE-storage pool (BufferBuilder::with_size_for -> new_buffer -> StorageModePrivate on macOS), and MTLBuffer.contents() for private storage is only accidentally non-null: small allocations carry an undocumented CPU mapping (which is why 256-token bench runs read correct ids and matched goldens!), while a recycled 32KB pool bucket returned null — surfaced only in dspark-run because the drafter load warmed the pool into handing the baseline's 4-byte argmax a 32KB bucket. Debug discriminator: warmup passed / baseline failed / bench-8 passed / stub passed -> pool-state dependence, then the error payload (U32 [1] on a 32768-byte buffer) named the mechanism.\n\nV2 (shipped): committed ids are slice_set-copied on-device into a caller-owned SHARED-storage ring (new_buffer_builder().with_size() = shared pool) ordered before each per-token signal; the host reads only the ring. One extra tiny dispatch per token (~0.4% cost vs v1). FINAL M3 A/B (palindrome, 6 runs/arm/profile, non-overlapping ranges): medium 182.6 vs 178.9 (+2.1%), long 182.3 vs 178.1 (+2.4%); jitter p95 44.7 -> 5.9ms. Gate battery green INCLUDING drafter smoke. Default on Metal; LMBRRR_ASYNC_READBACK=0 keeps the flush path (which remains the non-Metal fallback). ALSO FIXED en route: blocking waits must flush() first (uncommitted-tail-signal deadlock; medium profile survived only because 100 tokens x 340 dispatches is an exact CPB-100 multiple).\n\nUPSTREAM PR CONTRACT (write this into the PR docs): the SharedEvent signal guarantees ORDER, not MAPPING — host reads after a signal are valid only for buffers the CALLER allocated as shared storage; op outputs from the private pool are never CPU-readable contractually. Expose that as documentation on the host-view API rather than a runtime check. Remaining item on this ticket: the upstream PR itself (shared_event.rs + signal_event + flush; write against upstream's Mutex encoder seams) after a soak period. READBACK_EVERY sweep: superseded (the flag was never worth tuning once async shipped).
