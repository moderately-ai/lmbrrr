---
id: 1783960256963801000-58cd6154
at: 1783960256
---
ORDER v6 (2026-07-13 post-referee-session; supersedes v5). CLOSED TODAY: eval-gate-battery-script (shipped, goldens blessed both machines), eval-apples-to-apples-qmv + eval-e2e-stack-tax (delivered: we lead e2e 1.31x/1.73x; head-kernel gap real at 1.4x; full tables + WHYs on the tickets). ROUTING RESOLVED: the qmv verdict landed on 'kernel gap' — llama.cpp wins at the SAME format, so NO format-migration ticket is filed (that branch is dead; MLX's format win was matched by llama.cpp's kernel win at our format).\n\nNEW #1 — q4k-mv-round3 (IN PROGRESS): nr0=2 x nsg=2 port + f32-y discriminator, measured target 140 GB/s on M3, est +10-15% e2e. Everything else shifts down one. Then: SharedEvent async readback (candle PR; drain 3x bigger on M3 — down-tier value confirmed), MTP transplant phase 1, fused argmax, drain probe, elementwise slices, mc (REPRICED DOWN: width-parallelism only pays on DRAM-bound head rows — see its comment), deltanet roofline (AMENDED: traffic model must use SLC-resident assumptions for body shapes, and the llama.cpp GDN comparison arm is now trivially runnable — llama.cpp is built on the M3).\n\nPHASE 0 LEFTOVERS still owed: quiet-M4 re-baseline + census rerun + CPB re-anchor (M4 was loaded all day); M4 STS/cost refit post-recovery (drafter runs on identity STS since the wipe — refit BEFORE any suite-based verdicts); bench_session.sh wrapper. PHASE 1 LEFTOVERS running now on M3: 10-min sustained run + calibration refit.\n\nSTANDING PROTOCOL AMENDMENT (recorded on eval-protocol-ambient-control): micro-harness rows for shapes smaller than ~SLC size are DEPRECATED as kernel evidence — per-eval sync overhead (~150us) floors them and chained reruns measure cache, not DRAM. Kernel claims are valid only on DRAM-bound shapes (head-sized) or in production A/B.
