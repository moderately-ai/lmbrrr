---
id: 1784067417231284000-e8f002be
at: 1784067417
---
IN-LOOP INTEGRATION + FIRST PRODUCTION A/B (2026-07-14 evening, M3, MTP distilled drafter, tides/384, palindrome x2, deterministic within arms).

SHIPPED TO THE FORK (pins 00f836ab -> 3e5cd88f -> 43b559d1): prebuilt-metallib loader (newLibraryWithData via dispatch2; pre-26.4 load fails clean -> route fallback, verified on the M4), kernel_mul_mm2d_q4k_bf16 (exact fold, bf16 dst, real-N/real-m guards), CPU repack q4k_mm2d_planes in candle-core, e2e accuracy test (M3: mean_rel 1.13e-3, max 5.4e-3, bias 5.6e-4 — fp16 dsc/dmm adds ~3x the GEMV path bias, under gate; flip planes to f32 if tau ever shows poisoning). LMBRRR side: src/mm2d.rs wrapper + MixedLinear route (LMBRRR_MM2D, default OFF), eager plane build at load, LMBRRR_MM2D_MIN_N per-shape threshold.

TWO BUGS FOUND AND FIXED BY THE FIRST A/B (receipts in /tmp/mtp-*.json on the M3): (1) RACE — the rowsums scratch registered as encoder INPUT, so the concurrent encoder inserted no barrier before the matmul read it (non-deterministic verify 30-54ms, deterministic wrong dmin at m=4 from zero pages). Fix: Output registration; then the whole second dispatch was DELETED (row sums now in threadgroup memory inside the matmul — the A tile is SLC-resident anyway; one dispatch per linear). (2) TIMING CONTAMINATION — lazy repack ran inside the first verify forward; now eager at load.

CLEAN SWEEP (fused kernel, race-free, deterministic a/b): verify/round OFF (wide) 8.64/9.92/11.84 ms at chunk 2/3/4 vs ON (mm2d all-linears) 12.37/12.32/12.71 — FLAT IN m AS DESIGNED, crossover ~m=4. tok/s: off 124.9/112.5/94.6 (d1/d2/d3), on 102.5/97.8/93.0. Divergences: tie-flip class only; on-arms draft slightly better (d3 tau 3.11 vs 3.07, p1 0.59 vs 0.57).

DECOMPOSITION VIA MIN_N (head-only arm, d3): verify 11.18 vs wide 11.84 -> mm2d HEAD saves ~0.7ms/round at m=4 (98.0 vs 94.6 tok/s, +3.6% — the first spec arm to beat wide since the park); mm2d BODY costs ~1.5ms latency (12.71 - 11.18). ROOT CAUSE of the body gap vs the isolated probe (~7ms predicted, 12.3 observed): per-dispatch LATENCY — body shapes get 16-56 TGs with a serial K-loop; the probe pipelined 200 identical dispatches, a dependent layer chain cannot. NEXT KERNEL ITERATION: split-K (or execution_simdgroup 32-wide tiles) for n < ~4k shapes to multiply TG count; head routing is already shippable as a per-shape table.

STANDING: best spec arm today = d3 head-only mm2d at 98.0 tok/s vs greedy 197.6. Remaining wall-1 work: body split-K. Then walls 2-3 per the program (draft 6.2ms synchronous chain -> ~0.8 chained zero-sync; host ~4.0 -> ~0.5 one-sync).
