---
id: 1784064786043103000-fdeeb370
at: 1784064786
---
EXACT-FOLD PROTOTYPE LANDS (2026-07-14 evening, M3, quiet): mm2d_q4kf — exact q4_K semantics (per-32 sub-block d*sc and dmin*m folded per-element on the C tile via cooperative_tensor get_multidimensional_index; dmin term from precomputed per-slice A row-sums) — NUMERIC PASS max rel 4.3e-7 vs f32 reference. Perf, M=8 tile, GPU-timestamped, 3 rounds each:
- lm_head K=1024 N=248064: 1584us (100.2 GB/s) — vs wide-v2 m=2 1616us, m=1 GEMV 1025us. Fold overhead vs raw-nibble ceiling (1280us) = 1.23x. EIGHT exact rows cheaper than today's two.
- mlp up/gate K=1024 N=3584: 34us (67 GB/s); attn-q-ish K=1024 N=2048: 23us (56 GB/s); mlp down K=3584 N=1024: 61us (37.8 GB/s); out-proj-ish K=2048 N=1024: 36us (36.8 GB/s). Small-N shapes under-occupy (N/64 = 16-56 TGs on 14 cores) — split-K is the standard fix, not yet applied.
- Net: EVERY shape ~1.5-1.8x its m=1 cost, FLAT in m for 1..8 -> whole-stack verify[k<=8] ~= 1.6x decode pre-tuning, vs today's chunk2 1.72x / chunk4 2.5x. Phase-1 bar effectively met at the prototype level; split-K + wider tiles + static_slice are pure upside.
Probe artifacts: m3:~/gpu-runs/run-006/{mm2dq.metal,probe11.swift}. PRODUCTION INTEGRATION STARTS: fork Source (offline metal4.0 compile path needed — include MPP framework header at build time; check candle build.rs/xcrun availability on both machines: M4 macOS 15 CANNOT compile Metal-4 tensor ops -> runtime-OS-gated routing with wide-v2 fallback, same pattern as unpk), pack.rs repack (nibbles->[K,N] linear + dsc/dmm fp16 planes + N-pad to 64), per-shape routing table (head first; keep wide-v2 where it wins at m=2), accuracy tests, verify_ms_by_chunk_len refit.
