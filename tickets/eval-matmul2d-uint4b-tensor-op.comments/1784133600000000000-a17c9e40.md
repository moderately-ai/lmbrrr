---
id: 1784133600000000000-a17c9e40
at: 1784133600
---
BODY-LINEAR SPLIT-K RETUNE — CLOSED (falsified, corrected mechanism). Tested `LMBRRR_MM2D_SPLIT_TGS=256` (qkvz/gate_up 2-way split) vs 128 (unsplit) under the r1 canonical arm. Two experiments:

1. **In-loop suite** (rotated per-question paired A/B, 6 classes x 3 q x 2 reps): MEAN **-2.9%**, every class flat-to-negative (math -3.6, summ -5.5, writing -2.8; coding -0.0). Many questions DIV with tau drops (qa 1.69->1.54, summ 1.90->1.55, writing 1.95->1.61). Same direction as the earlier split-384 falsification.

2. **Kernel isolation** (greedy target decode, NO drafter, no tau; `decode_model_tokens_per_second` = pure forward-pass, overhead-decoupled): MEAN **-0.1%**, every class within noise (math +0.6, summ -1.2, others -0.0). **The split-K kernel is speed-NEUTRAL.**

CORRECTION to the earlier framing (which blamed "reduction overhead makes the split slower"): the kernel is not slower, it is **neutral** — and it's neutral because occupancy is already saturated. qkvz (n=8192 -> 128 threadgroups) and gate_up (n=7168 -> 112 TGs) dispatch far more threadgroups than the M3 Pro's ~18 GPU cores at 128, so splitting K to 256 TGs adds a reduction pass for ZERO parallelism gain. No bandwidth headroom to reclaim here.

CONSEQUENCE for the arm-dependence question (the plan's "retest split-384 under r3/r4 arm"): **moot.** The in-loop -2.9% is ENTIRELY the tau-perturbation component (split-K changes the target's accumulation order -> flips its argmax at near-ties -> diverges the committed trajectory), which IS drafter-arm-dependent. But since the underlying kernel upside is exactly zero, the best case for split-256 under ANY arm (even a perfect drafter with no near-tie flips) is break-even, NEVER a win. So keep-128 is settled **arm-independently**; no future-arm retest can find a prize that the kernel doesn't offer.

Caveat: isolation is m=1 (greedy); the verify pass is m~=4 (chunk). The occupancy argument (>=112 TGs saturates ~18 cores at any small m) predicts neutral at m=4 too, but only m=1 was measured. `LMBRRR_MM2D_SPLIT_TGS` stays at 128. The remaining B1 tuning-tail items (K=64-slice paired scale loads, head-tile 128, chunk-aware body routing) are separate and untouched by this.
