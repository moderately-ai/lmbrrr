---
id: 1784067630748678000-13ee2a25
at: 1784067630
---
PHASE-1 GATE RESOLVED — CROSSING PROVEN, WALLS 2-3 NOW BINDING (2026-07-14 night, M3, receipts /tmp/mtp3-* + probe12).

Depth extension (head-only mm2d route, tides/384): d4 tau 3.21, d5 tau 3.55, d6 tau 3.28 (single-prompt noise; d5 hist [14,15,10,4,5,3]). tau DOES grow past d3 — flat verify amortizes over deeper chunks. CROSSING ARITHMETIC at measured tau 3.55 (d5): with TODAY'S all-mm2d flat verify (12.3ms, no further kernel work) + phase-2 targets (chained draft ~0.8ms, one-sync host ~0.5ms): round 13.6ms -> 261 tok/s = +32% over greedy 197.6. With per-shape-geometry verify (~10ms, see below): ~314 = +59%. The 1.3x verify bar is NOT required for the crossing — flat-to-m=8 at ~2x decode suffices at depth 5+. Today's actual blockers, measured at d5: draft 9.98ms (5 synchronous chained steps) + other/host 4.84ms = 47 pct of the 31.3ms round. THE PHASE-2 TICKETS (mtp-phase-2-chained-depth-k, spec-phase-2-one-structural-sync) ARE NOW THE CRITICAL PATH — promote to p1 and execute.

VERIFY TUNING REMAINDER (optional upside, probe12 latency table): narrow 32x1sg tiles win mlp_up (49.1 vs 135.7us) and head (1490 vs 1583); lose qkv (30.6 vs 23.4) and oproj (53.6 vs 38.3); wash on long-K mlp_down (94us both, 37GB/s — needs split-K). Best-of-geometry linear sum ~7.6ms + ~2.5ms non-linear ~= 10ms verify. Work items: per-shape geometry table in the call wrapper (tile 32 vs 64 by shape), split-K variant for K>=2048 N<=1024 shapes. Filed as the tuning tail — NOT blocking phase 2.

CAVEATS BANKED: fp16 dsc/dmm bias 5.6e-4 (3x GEMV path) — watch tau in the re-arbitration, planes go f32 if poisoning appears; single-prompt tau values need the 6-class suite before any ship decision; mm2d planes add ~11 pct resident weight bytes for routed tensors (both layouts resident).
