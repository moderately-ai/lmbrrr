---
id: 1784068584058048000-a04a2131
at: 1784068584
---
CORRECTION (2026-07-14, same night — my accounting error, caught by bucket reconciliation): every tau I reported in tonight's comments was INFLATED BY +1 — the report field mean_accepted_length already includes the bonus token, and my extraction added 1 again. TRUE committed-tokens-per-round: d1 1.64, d2 1.96, d3 2.07-2.28, d5 2.55 (full-vocab chain) / 2.17 (placeholder slice). All MEASURED tok/s stand (they never used tau); the PROJECTIONS were wrong.

HONEST RE-DERIVED ARITHMETIC: latest arm (d3, slice, chained, margin-oracle gated off) = verify 11.28 + draft 5.22 + other 0.51 = 17.0ms/round at tau 2.28 -> 135.1 tok/s MEASURED (session started at 94.6; greedy 197.6). Composed best case with all current levers (all-mm2d geometry-tuned verify ~10ms, draft ~3.8, other ~0.5): d5 at tau 2.55 -> ~5.6ms/token ~= 178; at verify 9.5 -> ~break-even. THE CROSSING NOW REQUIRES THE ACCEPTANCE LEVER: tau_commit >= ~2.9-3.1 at d5-6, i.e. p1 toward 0.8+ — the concrete candidates are (a) the REAL FR-Spec corpus ranking (placeholder cost ~0.1 p1 tonight), (b) MTP distillation r2 at the 400k fakequant-matched corpus (r1 was 40k/2-epoch and moved p1 0.54->0.724; USER-GATED Modal spend), (c) wall-3 scheduler wiring (confidence-pruned suffixes raise effective acceptance per verify row). Also note the margin-oracle finding: the per-round full-logits top-k readback was ~3.5-4.3ms/round of pure diagnostics tax in EVERY prior MTP measurement (now opt-in via --mtp-margin-oracle); historical MTP round decompositions carried it in the "other" bucket.

The "wall-2 opens" comment's 261-314 projections and the phase-1 "crossing proven" claim are RETRACTED in their tau-3.5 form; the flat-verify amortization logic survives but lands at break-even-to-modest-gain at today's acceptance. Walls status: 1 = kernel done, geometry tuning open; 2 = effectively done (draft 3.35-3.8ms chained+sliced, other 0.51ms); 3 (acceptance/scheduling) = now the binding wall.
