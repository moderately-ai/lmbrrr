---
id: 1784064562565504000-e46bd257
at: 1784064562
---
CEILING PROVEN 2026-07-14 (M3, macOS 27, quiet, load 1.3): matmul2d bfloat x uint4b_format on the lm_head shape (K=1024, N=248064) does the FULL M=8 TILE in 1280.4 us (3 rounds: 1286.1/1280.4/1280.4; 200 dispatches/round, GPU timestamps), 99.2 GB/s weights-only effective. Same-machine anchors: our q4_K bf16 m=1 GEMV 1025 us; wide-v2 m=2 1616 us; MLX m=2 1297 us. So 8 verify rows through the head = 1.25x ONE greedy head read, flat in m for m in 1..8 (HW floor is M=8 per the framework static_assert "M must be a multiple of 8 or 16" — sub-8 chunks ride a padded tile at identical cost). Phase-1 bar (verify[2] <= 1.3x, verify[4] <= 1.5x) is DEMOLISHED at the head shape by a NAIVE kernel (64-wide bounds-checked tiles, no static_slice, no tuning). Numeric gate: bitwise-exact vs CPU f32 reference (max abs err 0.0) on M=8/K=256/N=128.

FOUR TOOLING TRAPS RECORDED: (1) the MSL spec PDF example passes K=0 to matmul2d_descriptor — WRONG, hangs the GPU (K-tile of zero never progresses; watchdogged CB never completes). Dynamic K is static_cast<int>(dynamic_extent) per the MPP header comments. (2) MTLComputeCommandEncoder has NO setTensor — construct tensors IN-SHADER from raw device pointers via tensor_inline (dextents<int,2>, dim0 = innermost); this also matches candle's buffer-binding model. (3) A uint4b tensor's data_handle_type is device uchar* (not uint4b_format*). (4) extents<int,2> is a STATIC extent of size 2 — dynamic 2D is dextents<int,2>. Offline toolchain: -std=metal4.0 accepts everything (SDK 26.4 rows in Table 7.3); no runtime-4.1 needed for tensor ops. Probes + metallib: m3:~/gpu-runs/run-006/{mm2d.metal,probe9.swift,probe10.swift}.

PRODUCTION DESIGN (exact q4_K, no quality change): cooperative_tensor exposes get_multidimensional_index -> exact scale folding at C-TILE granularity. Repack at load (pack.rs sidecar, one-time): nibble plane transposed to [K,N] linear little-endian order (N padded to /64), plus fp16 planes dsc[N, K/32] = d*sc_j and dmm[N, K/32] = dmin*m_j (+11% bytes vs q4_K: 160B vs 144B per 256 values). Kernel per TG (64-wide N tile): loop 32 K-slices of 32; matmul2d(multiply) into coop tensor P; elementwise C[i] += dsc[n(i),j] * P[i] via get_multidimensional_index; dmin term folded as C[i] -= dmm[n(i),j] * rowsumA[m(i),j] with per-slice A row-sums precomputed (tiny). Expected: 1280 us ceiling x K-slicing loss x fold overhead ~= 1.4-1.8x m=1 GEMV for 8 rows — still >2x better than wide-v2 economics at m>=4, flat to m=8. Fold-cost fallback if K=32 slicing loses too much ceiling: K=256 slices with per-256 folding on a REQUANTIZED shared-scale format (quality-gated, only if needed).

NEXT: production kernel in the fork (new Source, offline-compilable metal4.0), repack in pack.rs, route in fwd via env (LMBRRR_Q4K_WIDE_VARIANT=mm2d), accuracy test vs dequant reference, then verify_ms_by_chunk_len refit + spec re-arbitration per the program.
