---
id: 1783971392731257000-03668ad1
at: 1783971392
---
FUSED-CHAIN IN-LOOP RESULT (2026-07-14 M3 eval job) — ATTRIBUTION FALSIFIED, honestly recorded. Gates green; integration byte-identical; the ISOLATED chain runs 78us/step. BUT the in-loop refit says draft = 15.37ms (was 14.25 — within session noise, i.e. UNCHANGED) and the gamma slope is unchanged (1.14 vs 1.17 ms/gamma). CONCLUSION: the serial-Markov-dispatch hypothesis for the 14-15ms was WRONG — those dispatches executed in the shadow of something bigger. PRIME SUSPECT (was classified (b) in the round-anatomy report all along): the PROPOSE READBACK DRAIN — propose_inner's packed readback pays a full pipeline flush per round; the refit's draft bucket = round wall minus verify table, which contains exactly that drain + backbone GPU + host encode. The already-built --lag-schedule path defers this readback into the verify drain (2 drains -> 1). The fused kernel's value is real but masked until the drain goes; testing NOW with a lag-vs-default validation A/B + a LOOP_TIMING decomposition under lag. Validation standings under fused+refit (2 reps, all classes): 83-157 tok/s, everything still below the 182 chain floor — unchanged from pre-fused, as expected given draft cost didn't move. LESSON for the ledger: gamma-slope attribution conflates all gamma-scaled costs (features, heads, queue depth), not just the chain; direct decomposition (sync ladder or the timestamp meter) should have preceded the kernel. The kernel keeps: it is correct, cheap to keep, and becomes load-bearing the moment the drain is deferred.
