---
id: 1783971798959322000-1d183722
at: 1783971798
---
PROPOSE DECOMPOSED — DIRECT MEASUREMENT (2026-07-14, propose-ladder instrument, fenced segments, M3, gamma 6, 16 rounds): backbone+heads 6.6-10.7 ms (THE MASS), chain ~3.4 ms in-loop (0.47 isolated; delta = per-step feature cats + fence tax), pack ~0.2, readback ~0.15. BOTH prior hypotheses dead: not the Markov chain (kernel shipped, bucket unmoved), not the readback drain (0.15 ms fenced). The drafter BACKBONE is dispatch-execution-bound: ~45 tiny sequential dispatches x ~0.15 ms M3 per-dispatch floor — the same disease as the chain, three times the dispatch count. LESSON (bold, for the ledger): run the fenced ladder BEFORE building kernels; two attributions were inferred from slopes/anatomy and both failed; the 30-minute instrument settled it in one run.\n\nNEW WORK ITEM — DRAFTER BACKBONE DISPATCH DIET (replaces the census item): the drafter uses raw candle ops and never received the target model's fusion treatment. Apply shipped machinery: (a) fused rms_norm_add for both residual+norm pairs per layer (src/fused_norm.rs); (b) candle_nn::ops::swiglu for the MLP (3 dispatches -> 1); (c) QKV width-fusion at load (3 gemvs -> 1, the load_linear_fused pattern); (d) fold q/k norm + rope if cheap (rope_partial exists). Estimate ~20 -> ~12 dispatches/layer, backbone ~7 -> ~4 ms; with the fused chain + lag-deferred readback, propose ~5-6 ms => math projects 205+ vs the 182 floor. Then re-ladder, refit, validation standings.
