---
id: 1784079006395998000-62765dd3
at: 1784079006
---
FUSED M-ROW VERIFY ARGMAX — BUILT, GATED BYTE-IDENTICAL, FALSIFIED IN-LOOP (2026-07-15; /tmp/mtp19-*): kernel pair kernel_mul_mm2d_q4k_argmax + reduce (bf16-rounded compares, lowest-index ties — text byte-identical to head+fast_argmax); dev arm 11.71 -> 11.82 ms/round. The m x 248k logits write it eliminates is only ~2MB (~0.02ms) and the fast_argmax pass 0.13ms, while the in-kernel threadgroup argmax epilogue costs ~0.25ms — the "never materialize the logits" intuition priced the wrong term. Default OPT-IN (LMBRRR_FUSED_VERIFY_ARGMAX=1); machinery kept (deep-chunk memory-pressure scenarios + the plumbing now exists: forward_verify_ids_and_hidden with margin-oracle fallback). Falsification ledger entry: the fused-argmax prize on the VERIFY side was already captured by the one-sync packed readback; the remaining term was epilogue-bound, not bandwidth-bound.
