---
id: 1783981303315895000-9f31fff8
at: 1783981303
---
CPB-LADDER VERDICT (M3, 8 runs x 32 fenced samples, arms alternated): CPB commit hypothesis FALSIFIED — low mode identical at 100 vs 4096 (~6.7ms), high mode ~0.25ms WORSE at 4096 (10.3-10.5 vs 10.1-10.2), and the high/low pattern repeats at the SAME round positions across all 8 runs: deterministic per round, not commit noise. ROOT CAUSE of the bimodality: verify-drain contamination of the instrument. Greedy acceptance is deterministic on a fixed prompt, so the same rounds follow wide verify chunks; under async readback the verify GPU work is still in flight when propose starts and the ladder's FIRST fence absorbs it (+3.4ms ~= a wide verify chunk). Production overlap is a feature; only the fenced attribution mis-reads it. TRUE fenced backbone: ~6.7ms tight. DEPTH MODEL REVISED: per-stage cost is heterogeneous — the chain's 0.28ms/stage is real but specific to its 1-TG occupancy-collapse dispatches (chain rock-steady 3.43ms across 176 samples); the diet's removed add+norm stages moved the low mode by ZERO (6.72 pre vs 6.70 post), so small elementwise stages are cheap. Uniform 0.28ms/stage is falsified. ROUTING: (1) drafter-chain-persistent-kernel PROMOTED to next build — the only lever the data validates directly (3.43ms of measured 1-TG serialization -> 1 dispatch); (2) drafter-rope-in-place ON HOLD — rope stages are the cheap kind, its predicted win collapsed with the uniform model; (3) drain probe scope sharpened: boundary cost as a function of kernel WIDTH/occupancy; (4) the remaining 6.7ms backbone gets a gputrace attribution by kernel before any further backbone work. Instrument fix owed: the propose ladder should synchronize BEFORE starting its first segment timer so prior in-flight work cannot contaminate the backbone bucket.
