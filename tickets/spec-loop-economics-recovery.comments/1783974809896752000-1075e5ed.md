---
id: 1783974809896752000-1075e5ed
at: 1783974809
---
diet-eval verdict (M3, detached): NEGATIVE on e2e — and it corrects a stale baseline. gamma-6 in-loop propose: 10.24 ms post-diet vs 10.14 pre-diet (fused-eval log) = no change within noise; validation standings statistically identical across all 18 prompts (mean ~117 vs ~119, inside the run-to-run band). The 15.37 ms figure quoted earlier PREDATES the fused-chain integration: the fused Markov chain had in fact already cut the bucket 15.37 -> ~10.1 (the earlier 'bucket unmoved' note was measured before that ship and never re-checked against fused-eval). Post-diet fenced ladder: backbone+heads 6.7-10.1 ms (bimodal), chain 3.4, pack 0.23, readback 0.16 — fenced sum exceeds the unfenced 10.24 total, so the eliminated dispatches were already overlapped in-loop; the diet removed encode work the pipeline was hiding. Diet stays shipped (bit-identical, fewer dispatches, zero regression) but the dispatch-count lever is EXHAUSTED. Remaining mass: the always-full block-size-12 backbone compute, the 32k q8 heads, and the chain's fence-visible 3.4 ms. Economics: gamma-1 propose 4.53 ms ~= 0.8 greedy steps for tau 1.25 — no scheduling trick closes that; the drafter forward is structurally too close to a target step. Next levers by expected value: MTP transplant (drafts from verify-pass hiddens, amortizes the drafter backbone entirely) > chain handoff for parked classes > lag-path readback. Post-refit gate battery on M3 pending (cost_model.json changed under the drafter dir).
