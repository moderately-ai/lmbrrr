---
id: 1783967472035533000-1ac2c264
at: 1783967472
---
BACKOFF A/B VERDICT (2026-07-13, M3, validation split, per-class 3 x reps 2, arms rotated; raw table in ~/lmbrrr-work/backoff-ab.log on the M3): weak classes recover as predicted — summarization +5-13%, qa +2-12%, writing +11-17%, coding +8-10%, translation flat-to-+5 — but MATH REGRESSES: q405 clean -7% with tau 2.67->1.41, q404 unstable (one rep collapsed, +/-40). MECHANISM (strike arithmetic): at draft=14.25ms a width-3 round resets the counter only on FULL acceptance ((a+1)*6.35 must exceed ~27ms), so borderline-strong classes strike constantly and backoff then locks them out of drafting. DECISION: default reverted to OFF (flag retained, mechanism documented on it); policy re-tune happens AFTER the two big levers change the strike economics. The levers, from the round-anatomy validation: (1) CHAIN HANDOFF — parked classes exit to the async chain (182 tok/s) instead of in-loop greedy (135-142 realized); (2) FUSED MARKOV CHAIN in propose — gamma serial iterations of ~5-6 tiny kernels are the top hypothesis for the 14.25ms; confirm via gamma-sweep of draft_seconds under LMBRRR_LOOP_TIMING (backbone is gamma-invariant by construction, any slope isolates the chain), then one persistent-threadgroup kernel. Propose at ~7ms flips math above the 182 floor (205-225 projected) and roughly halves the strike threshold — at which point backoff (or no hysteresis at all) gets re-evaluated.
