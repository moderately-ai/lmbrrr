---
id: 1784071212231180000-a0da361b
at: 1784071212
---
TIERED ROUTING SHIPPED (2026-07-14 late; lmbrrr pin unchanged, routing-only): head-class (n >= 100k) tensors ride mm2d at any m in [2,8]; BODY tensors only at m >= LMBRRR_MM2D_BODY_MIN_M (default 5, the wide-slope crossover). Tides/384 A/B: d5 verify 16.72 -> 13.38 ms/round (110.4 vs 106.1 tok/s). d6 adds nothing (13.86, draft +0.55). NOTE: d5-tiered trajectory shifted (commit/rd 2.30 vs 2.57 — margin-class body mm2d numerics flip ties on single prompts); the suite arbitrates any default change. d3 unaffected (chunk 4 < body threshold).

DISTILL-R2 PREP (user-approved run, efficiency pass first per user): mtp_distill.py rewritten to length-bucketed batching (right-pad + causal + loss mask; per-sequence mean loss and effective batch 16 preserve r1 dynamics). SMOKE b1 (2000x1ep, batch-1 reference): GPU util 24.7 pct mean / 52 peak, 9.0/79.7 GiB, ~10k tok/s, holdout 0.3829 -> 0.7159 — confirms the batch-1 headroom hypothesis. Batched smoke (b8) running; r2 launches at the smoke-validated config (equivalence gate: final holdout within ~0.02 of b1, then 400k x 2ep detached, 23h timeout).
