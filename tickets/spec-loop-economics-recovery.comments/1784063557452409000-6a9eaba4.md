---
id: 1784063557452409000-6a9eaba4
at: 1784063557
---
SPEC-RECOVERY PROGRAM APPROVED (2026-07-14, user-directed re-plan; plan file rippling-wobbling-dijkstra). Directive: push spec past greedy (197.6 M3 decode-only), no excuses. Three walls, all engineering: (1) verify slope — q4_K m in [2,8] compute-bound ~20% ALU, verify(2)=1.72x decode vs MLX-proven 1.02x at m=2 same machine; (2) dispatch/host floor — drafter backbone 6.7ms fenced on ~1-2ms compute (45 dispatches x 0.15ms launch floor), 14-15ms realized (drain tax), MTP host 4.2ms vs 1 needed sync, 94% of drafter bytes are 248k-vocab heads; (3) tau — MTP path bypasses all of src/spec/ (no confidence/width scheduling/STS/hysteresis).

PHASES: 1) verify at memory-bound — arm 1a q4_K-native matmul2d tensor-op (bfloat x uint4b HW, MSL Table 7.3), arm 1b affine-g64 format migration (USER-AUTHORIZED auto-proceed if 1a misses), 1c quality battery; bar verify[2] <= 1.3x (stretch 1.2), verify[4] <= 1.5x (stretch 1.35), m=1 parity. 2) one-sync round (fused m-row verify argmax) + chained zero-sync drafting + vocab-head diet (32k slice BOTH lanes + q4k head quant) + drafter-backbone ICB/persistent collapse (never falsified for the drafter). 3) wire scheduler into MTP path (pseudo-confidences + Platt + schedule_prefix_width), refit cost model, gamma re-sweep, dual-vehicle re-arbitration; user-gated distill-r2/Markov-for-MTP only if acceptance binds. Pushed arithmetic: MTP d3 ~7.2ms round at tau 2.32 = ~322 tok/s; DSpark gamma4 math ~10.5ms at tau 3.76 = ~278. Ship bar unchanged: suite mean >= +2%, no class regressing > 2%; skip-hysteresis keeps 197.6 as per-class floor. M3 ONLY for all measurement (user rule).

Paper-fidelity resolution (dspark-2607.05147v1 read end-to-end + implementation audit): round-4 drafter IS the faithful DSpark (2-layer target-hidden-conditioned backbone = paper's optimal depth, Markov head, confidence head, Appendix-A-faithful scheduler); MTP transplant = the paper's superseded-baseline shape; both ride the same walls at batch-1, suite arbitrates per class.
