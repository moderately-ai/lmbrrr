---
id: 1783968841679734000-641a6ab5
at: 1783968841
---
FUSED MARKOV CHAIN — KERNEL SHIPPED TO FORK (9ed88b18..HEAD), BENCH GREEN ON BOTH MACHINES. Correctness: all four variants (q8_0/bf16 w2 x remap/identity) match a same-accumulation-order CPU reference BITWISE — tokens AND per-step feature embeddings — via the new markov-chain bench task (metal_benchmarks; the gate is exact because each vocab row's dot accumulates sequentially in one thread, so the CPU reference reproduces it; only legacy-qmv comparisons carry ulp-tie caveats, which lmbrrr's parity mode will count). TIMING (M3, quiet, load 1.8): gamma=6 chain 467.8us = 78us/step vs the legacy serial chain's ~1170us/step — 15x. Projected propose: 14.25ms - 7.0ms(chain) + 0.47ms = ~7.7ms, at the threshold the round-anatomy arithmetic needs (math -> 205-225 tok/s vs the 182 chain floor). Implementation notes: 2 dispatches/step (grid-wide fused gather+gemv+add+partial-argmax at 64 TGs x 256, then 1-TG reduce+remap+chain-feed+feature-row stash); review fixes worth remembering — threadgroup arrays must be declared in the kernel-qualified function (not template helpers), constexpr constant for array bounds, ulong index widening for the 248k-row table. REMAINING: lmbrrr integration — wire into propose_tensors (fast path when !branch/!diagnostics; MixedLinear needs a qtensor accessor + fork QTensor storage accessor for the q8_0 buffer), LMBRRR_FUSED_MARKOV=verify parity mode counting token diffs vs legacy, then gamma-sweep re-measure + suite tau comparison on the M3, then re-evaluate hysteresis/backoff and the chain handoff under the new economics.
