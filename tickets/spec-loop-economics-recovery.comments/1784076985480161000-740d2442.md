---
id: 1784076985480161000-740d2442
at: 1784076985
---
Q8_0 ANOMALY RESOLVED — NO KERNEL BUG (2026-07-15; new fork test test_matmul_q8_0_bf16_accuracy, M3): q8_0 bf16 paths pass at the bf16 floor (mean_rel 1.5-1.8e-3, m=1 and m=3, k=2048/3584) — same accuracy class as every q4k path. MECHANISM of the acceptance damage: the 20M drafter head is high-entropy (p1 0.68 by construction), so a large share of its argmaxes are near-ties where ~2e-3 relative perturbation flips the drafted token; per-linear bisection damage scales with K (absolute error), and the q4k-vs-q8 "inversion" (0.54 vs 0.27) was single-prompt trajectory variance — the recurring trap, now bitten on p1 itself. CONCLUSIONS: (1) post-hoc quantization of a small distilled drafter head is intrinsically acceptance-hostile — only quantization-AWARE distillation (train the head against its own quantized weights, r3 candidate) can bank the 1.4ms; (2) --mtp-quantize stays off; (3) the q8_0 kernel suite now has standing accuracy coverage it previously lacked; (4) margin-class p1/tau comparisons MUST use the suite, never one prompt — add to the measurement-protocol notes.
