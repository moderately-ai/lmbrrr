---
id: 1784086500000000000-9e41c7d3
at: 1784086500
---
R1-VS-R2 ARBITRATION COMPLETE (M3, canonical arm, per-class 3, reps 3, rotated; artifacts/suite-r1r2*-*.json). VERDICT: CANONICAL ARM STAYS R1 AT D3. (1) d3 head-to-head: mean is a wash (baseline-relative 153.1% vs 153.3%; raw tok/s ~+0.5% r2) but MATH REGRESSES -2.6% relative (139.6->135.9%; q405 comparable-text 215.2->204.7, tau 2.76->2.65) — fails the no-class-regressing->2% switch bar; r2 gains coding (+5.0pt, q124/q126) and writing (+2.1pt) but both classes carry cross-arm tie-flip flags (detector now real). (2) Depth re-sweep: d4 math r1 224.2 vs d3 223.5 (wash; q404 hits 244.1 tok/s tau 3.34 — best single-question number of the campaign), d4 worse for r2 math (214.9 vs 218.1) and worse for BOTH arms on writing (r1 151.5 vs 161.2, r2 156.5 vs 165.4). d3 stays. (3) LESSON: +1.64pt holdout top-1 (0.7759 vs 0.7595, 2x corpus) did NOT lift in-loop tau — acceptance is run-length-bound, not top-1-bound; more same-recipe data is a dead lever. (4) IMPLICATION: the training-side lever is now QAT-r3 (train the head against its own q4k-quantized weights; banks the measured ~1.4ms/round draft-cost win, needs only acceptance PARITY at q4k, not a gain) — proposal to present with these numbers, Modal spend = user gate. Handoff/summarization retune not triggered (conditional on r2 winning). NOTE for table-readers: the report's baseline_* fields include prefill — usable only as an arm-relative comparator, not as the decode-only greedy floor; the ship-bar-vs-greedy table still requires dedicated greedy arm runs.
