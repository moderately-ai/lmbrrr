---
id: 1783967954118888000-13bb7d51
at: 1783967954
---
FUSED MARKOV CHAIN — IMPLEMENTATION SPEC (validated against code 2026-07-13; supersedes sketches). LEGACY SEMANTICS to reproduce (dspark.rs:517-542): per step k: prev_emb = markov_w1[prev_global_id] (markov_w1 [vocab_full, 256] bf16, global-id indexed); bias = markov_w2(prev_emb) (MixedLinear: q8_0 QMatMul under --drafter-quantize q8-0 [draft_vocab 32768 x 256], bf16-direct dst; or dense bf16); step = base_logits[0,k] + bias (bf16); sub = argmax(f32(step)) over draft vocab (FIRST index wins ties); token = draft_vocab_ids[sub] (or identity); prev = token. Side products: prev_emb per step feeds features[k] = cat(h_k, bf16(prev_emb)) for the confidence head; 'corrected' step logits are consumed ONLY by branch (tree) and diagnostics paths (verified: pack_proposal ignores them, propose_inner materializes only under diagnostics).\n\nKERNEL DESIGN (fork, new metal_src/dspark.metal + Source::Dspark registration in source.rs/kernel.rs): per step TWO dispatches. (A) markov_step_partial_{q8,bf16}: NTG~64 TGs x 256 threads; TG loads prev_emb row (w1[prev[k-1] global] — reads the u32 token buffer written by the previous step's B; step 0 pre-seeded with the anchor) into threadgroup memory; each thread strides rows of its vocab slice: dot(w2_row, prev_emb) with f32 accumulation -> bf16 round (mirrors qmv bf16-dst) -> + base_logits[k,row] (bf16 add via f32, round) -> f32 compare value; per-TG argmax with FIRST-index tie rule -> partials[(val,idx)] per TG. (B) markov_step_reduce: 1 TG reduces partials (first-index tie), remaps via draft_vocab_ids (or identity), writes tokens_global[k] (u32) + copies w1[global] row into prev_embs[k*256..] (bf16) + writes the next prev-token slot. gamma steps = 2*gamma dispatches (12 at gamma6) replacing ~30 serial tiny kernels. Host fn call_markov_chain in kernels/dspark.rs, one encoder.\n\nBITWISE HONESTY: the gemv reduction order cannot match candle's qmv exactly -> logits differ by ulps -> argmax tie-flips possible but rare. Draft tokens NEVER affect committed text (exact verify) — gates are therefore: (1) built-in parity mode LMBRRR_FUSED_MARKOV=verify runs BOTH paths per round and asserts token equality (count+log any diffs; expect ~0); (2) invariance oracle; (3) suite tau comparison fused-vs-legacy (acceptance must match within noise). Rollout: env default legacy -> parity soak -> flip default, legacy stays for branch/diagnostics paths (which the fast path does not serve — NOT a transitional duplicate).\n\nPLUMBING NOTES: q8_0 buffer access — QMatMul enum is public; QTensor Metal storage exposes buffer() via candle::quantized::metal (add a small accessor in lmbrrr or fork if the path is private — check QTensor::storage visibility first). base_logits must be contiguous bf16 [gamma, Vd]; draft_vocab_ids U32 [Vd] device tensor already exists. Outputs: tokens_global u32 [gamma] + prev_embs bf16 [gamma,256] as caller-allocated SHARED-pool-safe tensors via the fused_norm.rs from_storage pattern (op-output privacy does not matter here — both are consumed on device; the token readback continues through pack_proposal unchanged). Features rebuilt from prev_embs; corrected left empty in the fast path. EXPECTED: ~1.17ms/step serial chain -> ~0.1-0.2ms/step wide = propose 14.25 -> ~8-9ms, then the lag path (readback) takes it to ~7.
