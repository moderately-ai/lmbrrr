---
id: 1784072613031617000-1c7864dc
at: 1784072613
---
SPLIT-K SHIPPED AND ARBITRATED IN-LOOP (2026-07-14 night; fork pin ffbf4ca7, all three kernel-geometry accuracy tests PASS on M3):
- Kernels: kernel_mul_mm2d_q4k_bf16_splitk (K/32 slices partitioned across grid-y, f32 partials, per-split TG-local rowsums — the fold is linear over slices so partials are exact) + kernel_mm2d_q4k_splitk_reduce. Partials register as encoder Output (the rowsums-race lesson applied). n_splits = clamp(128/(n_pad/64), 1, K/32).
- IN-LOOP A/B (tides d5, warm): verify 13.36 -> 10.98 ms/round; verify slope now near-flat 10.3 -> 11.4 across chunks 4->7 (was +1.5ms/row). First-run shader-compile transient observed (12.59) — warm arms are the truth, per the standing trap.
- SUITE (validation 6-class, 3q x 2 reps, split-K + body at all m): mtpd3sk MEAN 145.2 tok/s (was 137.5), math 183.5 mean / peaks 189-191, translation q164 190.9 — best classes at 96-97 pct of greedy 197.6. d5 loses the suite mean to d3 (draft cost still beats its tau gain; dev-prompt d5 wins were trajectory luck).
- DEFAULTS FLIPPED (sub-knobs only; master LMBRRR_MM2D stays opt-in until the ship-gate battery): SPLITK on, BODY_MIN_M 2.
Session arc on the verify wall: chunk-4 verify 12.5 (session start, wide) -> 11.14 (mm2d head) -> 10.31 (split-K all-body); chunk-6: ~16.5 -> 10.98. REMAINING verify headroom: fenced non-linear decomposition (deltanet/attention m-scaling), fused m-row verify argmax (~0.3ms), verify-side dispatch count. REMAINING draft: chain-step dispatch collapse (ticket 2d scope, ~1-1.5ms at d3). r2 distillation running (ap-P5FfeztMflt7TR0iHmgQNY).
