---
id: 1784079300291702000-60ee8dae
at: 1784079300
---
FULL-HARVEST LEDGER UPDATE (2026-07-15, pre-r2-landing):
- A1 copy attribution: labeled encoder walk (run-009/010, session procedure clean this time — stale session terminated before profiling): the copy_bf16_strided mass sits INSIDE the verify encoder adjacent to gated_delta prep/core = the per-layer verify-capture window builds (qwen35 conv_full transpose+contiguous+cat x18/round) + assorted small copies; NO single dominant producer. Cheap fix (lazy conv_full built only on rollback) saves only the full-accept fraction ~0.2ms — filed as a minor follow-up, not built. NOTE: the gpudebug CLI does not surface debug-group labels in performance lists even on a metal-debug-labels build; encoder-scoped command lists are the attribution ceiling.
- B4 chain dispatch inventory: verify encoder = 45 commands for the m=4 forward; chain-step encoders similar density. ICB collapse for the MTP step remains the one large unbuilt item (design on metal-icb-decode-replay).
- D9 adaptive depth: BUILT (LMBRRR_MTP_ADAPTIVE_DEPTH, prev_accepted+1 clamped to --mtp-depth cap). Dev: round 11.7 -> 9.4ms but commit/rd 2.15 -> 1.71 (under-drafts after single misses) = wash. Rides the r2 suite as an arm; a smarter policy (decay toward cap, not reset) is the obvious iteration if the suite shows promise.
- D8 SharedEvent verdict listener: measured host residual is 0.4-0.5ms and the wait is data-dependent (bookkeeping needs the verdict) — CLOSED as no-gain per the plan's own criterion.
- E11 QAT-r3 DESIGN (prepared, code deferred until r2 arbitration says acceptance still binds): torch-side approximate q4_K fakequant (per-32 asymmetric 4-bit under per-256 6-bit two-level scales, ~40 lines) applied to the head weights each forward (straight-through), rest of mtp_distill.py unchanged; banks the measured 1.4ms/round without the post-hoc acceptance loss. Runs at the r2 recipe (400k x 2ep batched).
- G14/G15 (pack-cached mm2d planes; layout unification): open, post-landing.
