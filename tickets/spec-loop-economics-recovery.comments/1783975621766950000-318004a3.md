---
id: 1783975621766950000-318004a3
at: 1783975621
---
Diet post-mortem — WHY the null result (mechanism, mostly from our own dossier): (1) The '~45 dispatches x 0.15ms floor' premise divided segment wall by dispatch count — an average, not a marginal cost. Dossier section 1 already measured per-kernel cost at 5-20us; removing ~14 dispatches = 0.07-0.3ms, invisible. (2) The wall is SERIAL DEPTH x barrier drain + commit boundaries: auto-barriers fire on true hazards only but each drains the pipeline (barrier-probe: 0.85-1.00 interleave, no overlap survives a barrier). In-situ per-serialized-stage cost from the fenced chain segment: 3.4ms / 12 strictly-serial dispatches ~= 0.28ms/stage on M3; backbone ~30 stages ~= 8ms — reconstructs the measurement from depth alone. (3) qkv/gate-up width fusion scored ZERO by construction: q/k/v write disjoint buffers from one input (no hazard -> no barrier -> already concurrent) and the fused gemv reads the same total weight bytes. Predictable before building. (4) The add+norm/swiglu depth fusions (~5 serial stages, ~0.5-1.4ms expected) are real but hidden inside the +-3.4ms ladder bimodality, consistent with the 1-3ms CPB-100 commit floor landing mid-segment (non-monotonic samples rule out ctx growth). PENDING VERIFICATION: cpb-ladder A/B (CANDLE_METAL_COMPUTE_PER_BUFFER 100 vs 4096, 15 samples/arm) — if the 10.1 mode collapses, commits own the variance and the depth model is calibrated. PREDICTED NEXT LEVER from the model: drafter rope-in-place (Rotary::apply is the naive 5-dispatch neg/cat/mul/mul/add form x2 apps x2 layers, ~20 mostly-serial dispatches; the target model got the wave-1 in-place kernel, the drafter never did). Depth model predicts ~1.5-2.5ms; per-kernel-us model predicts ~0.3ms — the CPB experiment calibrates which, BEFORE any port.
