---
id: 1784070267345595000-cd9da9a0
at: 1784070267
---
6-CLASS SUITE RE-ARBITRATION (2026-07-14 night, M3, validation split, 3q x 2 reps, per-run SD < 2.3 — receipts artifacts/suite-mtpwall3-*, /tmp/suite-mtpwall3.log). Arms: distilled MTP + real-FR-Spec 32k slice + chained drafting + mm2d head route + margin-oracle off, depths 3 and 5.

MTPD3 class means: math ~173 (peak q404 178.7), coding ~155, translation ~140, qa ~128, summarization ~114, writing ~114 -> OVERALL MEAN ~137.5 tok/s vs greedy 197.6 (70 pct; best class 91 pct). Suite tau is MUCH healthier than the tides prompt suggested: math 2.83-2.96 committed/round, coding 2.5-2.8 (tides was 2.09) — the single-prompt pessimism was real.

MTPD5 loses to d3 on EVERY question (e.g. math 404: 142.4 vs 178.7) — at head-only mm2d routing the wide-kernel body slope (+1.5ms/row) eats the tau gain at chunk 6. Deeper depths only pay off after all-mm2d-at-deep-chunks routing (per-chunk-size table) or body split-K, both in-loop-gated per the t32 lesson.

CROSSING ARITHMETIC AT SUITE TAU: d3 round today ~15.0ms at math tau 2.9 -> 5.17ms/token ~= 193 ~ greedy; the remaining identified scraps (fused m-row verify argmax ~1ms, catch-up cost, all-mm2d routing) put math class OVER the line at current acceptance. The MEAN crossing still needs acceptance: at p1 -> 0.8+ (distill-r2 at the 400k corpus; r1 was 40k/2ep -> 0.724) projected mean tau ~2.6-2.9 -> mean ~165-190 + scraps -> at/over greedy. DISTILL-R2 IS NOW A WELL-FOUNDED ASK: the harness around it is no longer the bottleneck, and every projected term is suite-measured.
