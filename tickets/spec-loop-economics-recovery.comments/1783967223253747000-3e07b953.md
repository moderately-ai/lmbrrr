---
id: 1783967223253747000-3e07b953
at: 1783967223
---
ROUND-ANATOMY VALIDATION (2026-07-13, full-code read; full report in session log) — REPRICES THE PLAN. Ground truth: a default drafted round has EXACTLY TWO host-blocking readbacks: (A) propose's packed readback (dspark.rs:632-635, ~14.25ms incl. the drafter GPU drain) and (B) the verify argmax_tokens readback (commands/dspark.rs:823-854, ~verify_ms[w+1]). Everything else (snapshot, rollback_to_prefix, append_context) is enqueue-only. (A) is ALREADY SOLVED behind --lag-schedule (propose_device defers the readback into verify — 2 drains -> 1).\n\nNEW ITEM RANKING from the arithmetic (tok/s = tau*1000/round_wall; floor 182):\n1. CHAIN HANDOFF FOR PARKED CLASSES (biggest, previously unplanned): in-loop greedy realizes verify_ms[1]+0.7-1.1ms bare host cost = 7.05-7.6ms/token (135-142 tok/s) — parked weak classes pay ~25-35% vs the async chain's 5.5ms/182. Skip mode should run bounded CHAIN segments (factor the async device-chain core out of generate_tokens into a continue-from-current-state helper; the spec loop already owns compatible KV/GDN state) and return for probes. Recovers summarization/translation/qa/coding to ~182.\n2. FUSED MARKOV CHAIN in propose (top hypothesis for the 14.25ms): backbone is ONE gamma-invariant block forward (~1-2ms-class); the gamma SEQUENTIAL Markov steps (dspark.rs:522-542: index_select -> rank-256 gemv over the 32k draft vocab -> add -> F32 cast -> argmax -> remap, ~5-6 tiny kernels each, data-dependent chain) are launch-latency bound — ~30 serial dispatches for ~8M MACs of real work. CONFIRM with the gamma sweep (LMBRRR_LOOP_TIMING=1 draft_seconds at gamma 1/3/6 — backbone gamma-invariant by construction, any slope isolates the chain), then fuse the whole chain into ONE kernel (per-step: gather row, gemv, add base logits, argmax over draft vocab — all fits one threadgroup-persistent dispatch iterating gamma times). Propose 14.25 -> ~5-7ms flips MATH above the floor (205-225 tok/s).\n3. Drain-tail (SharedEvent flush+poll on the verify readback) + lag/adaptive-sync: second-order (+4-5% on drafted classes; the 0.85ms/round bucket).\n4. Optimistic next-draft: feasible — unwind cost is ONLY the drafter ctx tail truncate (prefix rows provably rollback-invariant per the code's own comments) + discarded compute; the hard part is the anchor (bonus = a TARGET argmax, host u32 today) needing a device-tensor embed path. Park behind 1-3.\nCORRECTIONS TO PRIOR COMMENTS: rollback is NOT a sync in the default path (readvance legacy env only); greedy-in-loop floor is 7.05-7.6ms realized, not 6.35 (kernel-only) — weak-class exit value is LARGER than stated. Also note accept_margin adds 2 extra readbacks (typical-acceptance mode only).
