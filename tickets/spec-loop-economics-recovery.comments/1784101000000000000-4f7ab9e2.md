---
id: 1784101000000000000-4f7ab9e2
at: 1784101000
---
FRESH ROUND PROFILE UNDER THE CURRENT STACK (dspark-round-8.gputrace, canonical arm + lazy capture + mtp_fc_prep; gpudebug serial profile, session artifacts /tmp/shaders-011.json + /tmp/cmds-011.json, 293 dispatches / 3 CBs / 4 encoders in the captured window — draft-chain kernels absent from the window, so this is the verify+catch-up side). Per-shader: kernel_mul_mm2d_q4k_bf16 59.1% x40, splitk body 15.3% x39 + reduce 1.1%, gated_delta_v2 core 6.4% + prep 3.4% + epilogue 1.2%, steel_attention 3.4% x5, fast_argmax_bf16 2.9% x1, rmsnorm_add 2.2%, cast_bf16_f32 1.8% x14, copy_bf16_strided 1.2% x20 (down from 4.8% — the lazy capture harvested it). DECODE of the x40 plain-kernel count: 1 dispatch = the lm_head at 24.4% (bandwidth floor, ~146MB q4k read); the other 39 are BODY linears that the split-K router leaves UNSPLIT because their TG count already meets the 128 target by integer division — qkvz n=8192 (n_pad/64 = 128 -> n_splits = 1) and gate_up n=7168 (112 -> 1) — at ~1% each, TOGETHER ~35% of the window in the 47 GB/s class (the known unsplit-gate_up receipt, now quantified as the dominant remaining inefficiency). RANKED LIST (post-r3, all margin-class): (1) split/retune the unsplit body class ~35%; (2) GDN core+prep 10.9%; (3) fused verify argmax retest 2.9%. Nothing non-margin-class of size remains — the margin-class retune wave IS the perf frontier, and it re-opens under whichever arm the r3 arbitration ships. gpudebug procedure now recorded: open session (gpudebug -t trace -o dir), poll status until replayer ready, "profile run --exec serial", read /performance/shaders and /performance/commands, terminate.
