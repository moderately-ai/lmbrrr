---
id: 1784075717236222000-5429e520
at: 1784075717
---
R2 LANDING RUNBOOK (execute verbatim when mtp-distill-r2 prints FINAL/DONE; the 10-min cron carries the download/scp steps):
1. DOWNLOAD: uv run modal volume get lmbrrr-dspark runs/mtp-distill-r2/mtp.safetensors artifacts/mtp-distill-r2/mtp.safetensors --force (same for metrics.json); scp both to m3:~/lmbrrr-work/lmbrrr/artifacts/mtp-distill-r2/.
2. RE-ARBITRATION SUITE (M3, quiet-machine checklist first): cd ~/lmbrrr-work/lmbrrr && LMBRRR_MM2D=1 python3 evals/run_spec_suite.py --arm "r1=artifacts/mtp-distill-r1/mtp.safetensors:--mtp-depth 3 --mtp-draft-vocab 32768" --arm "r2=artifacts/mtp-distill-r2/mtp.safetensors:--mtp-depth 3 --mtp-draft-vocab 32768" --split validation --per-class 3 --reps 2 --max-new-tokens 256 --tag r2arb
3. DEPTH RE-SWEEP with r2 (tau moved, the depth optimum may too): dev-prompt d3/d4/d5 warm pairs, then suite the winner if it is not d3.
4. SHIP-BAR TABLE: python3 /tmp/shipbar.py adapted to the r2arb tag (per-question spec vs same-run greedy). Bar: mean >= +2 pct over greedy, no class regressing > 2 pct (drafter-checkpoint-ab-protocol).
5. IF THE BAR IS SHORT: retune the handoff on the two weak classes only (skip_after 2, probe_backoff_cap 32 arms — suite, classes summarization,writing per-class 3 reps 2), and pick up the two flagged kernel scraps (fused m-row verify argmax ~0.3ms; draft-chain ICB collapse ~1ms — ticket metal-icb-decode-replay carries the drafter-scoped design).
6. EITHER WAY: record standings on this ticket + campaign log; if the bar clears, file the ship ticket (default flip LMBRRR_MM2D=1 + drafter-mtp productization) with the gate battery + goldens plan — master default flips ONLY through the full gate battery per the standing rules.
CONTEXT SNAPSHOT for whoever executes: current best = mtpd3h2 config (d3, 32k slice, mm2d+splitk, fused recon, one-sync, zero-accept handoff) at 93.9 pct of same-run greedy mean; math +14.0, translation +2.6; r1 p1 0.724; r2 holdout 0.7691 at step 16.6k of 49.9k.
