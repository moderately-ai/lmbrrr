---
id: 1784125447987738000-12318b2d
at: 1784125447
---
STRATEGIC SYNTHESIS (2026-07-15, post-r3): where the crossing actually stands and what the binding lever is now.

STATE. Kernel-side bit-exact inventory is EXHAUSTED (canonical floor 225.4 -> 230.5 tok/s bit-identically this session: lazy conv capture +2.16%, cast-fold +0.21%; fused mtp pre-fc = wash/dispatch-count-thesis-falsified; plane cache QoL). Head decision made: r1-bf16 canonical; r2 (wash) and r3-QAT (near-miss, +1.0% mean but translation -2.3% on q164) both dispositioned. Decode-only mean sits at 97.6-98.2% of greedy — math 116%+, but writing/summ/qa (80-95%) hold the mean UNDER greedy. So the mean crossing is NOT achieved and the binding wall is ACCEPTANCE on the weak classes.

DIAGNOSIS (new, load-bearing). The acceptance wall is NOT a data-volume or quantization problem — r1->r2->r3 pushed holdout top-1 monotonically and it kept not moving in-loop tau (r2 +1.64pt -> ZERO tau). Root cause candidate: the MTP distillation OBJECTIVE is teacher-forced single-token cross-entropy, which optimizes a p1-flavoured proxy, NOT the accepted-run length tau. Since acceptance is multiplicative (E[tau] = 1 + p1 + p1*p2 + ...), p1 is the leading factor of tau but holdout top-1 is a single-position, teacher-forced stand-in that decouples from the run under the head's own drifting drafts. The q164 failure is the concrete instance: a tau-collapse (3.00->2.25) the depth-4 probe could NOT rescue (leading-factor collapse; adding tail terms can't fix a broken prefix) -> training-rooted, not schedule-fixable.

OPEN QUESTION THAT GATES EVERYTHING (not yet examined). Are the weak classes (writing/summ/qa) even COVERED by regen-r4-400k? The corpus was built to match the target's TOKEN-frequency distribution (it fed the FR-Spec ranking), NOT to be task-balanced -- its class mix is whatever the round-4 prompt pool was. Two competing explanations for weak-class acceptance, with very different fixes: (a) COVERAGE GAP -> rebalance corpus (cheap, high-certainty); (b) INTRINSIC ENTROPY (writing/summ are open-ended, low-margin, the target itself branches more) -> partly fundamental, objective work has a ceiling, levers shift to relaxed/typical acceptance + tree hedging. Distinguishable with cheap analysis, no training. A run-aware objective trained on a corpus that doesn't cover writing won't fix writing, so this diagnostic must run FIRST.

FORWARD SHAPE (being planned; tiers): T0 corpus-coverage + intrinsic-entropy diagnostic (gates the rest, no spend); T1 the acceptance lever (corpus rebalance if coverage-thin, and/or run-aware/on-policy MTP distillation if objective-bound); T2 QAT-r4 cheap speed shot (r2-init + assistant-span mask + 2ep, orthogonal to acceptance, banks the ~1.4ms if it clears the q164 -2% bar); T3 kernel margin-class wave (~40% of verify window: unsplit body linears ~35%, GDN core/prep, verify-argmax) -- gated behind the shipped head arm, margin-class re-arbitrated per arm.

MEASUREMENT-PROTOCOL PROMOTION: holdout top-1 is a KNOWN-WEAK proxy for in-loop tau (bitten twice now: r2, r3). Training-side gains must be confirmed on the suite's tau, never taken from the holdout proxy.
