---
id: 1784078571044869000-a2ab4a17
at: 1784078571
---
LAST-ROW DRAFT HEAD SHIPPED — BYTE-IDENTICAL (2026-07-15; /tmp/mtp18-*): mtp_step no longer runs the draft head over every row; mtp_draft_next runs it on ONLY the consumed row (chain step / catch-up last row). The catch-up's m-row head pass was ~75% wasted work. Dev arm: 182.5 -> 183.4 tok/s (+0.1ms/round — the sliced 32k head was already on the cheaper wide route at m<5, so the win is smaller than the mm2d-routing estimate). Text and per-round histogram identical. Also: split-target 384 falsified by suite (see prior comment) — the labeled-capture attribution procedure (metal-debug-labels build + serial profile) is now the standing instrument for per-dispatch work.
