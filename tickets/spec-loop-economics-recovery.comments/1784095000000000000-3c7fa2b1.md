---
id: 1784095000000000000-3c7fa2b1
at: 1784095000
---
LAZY ROLLBACK CONV-WINDOW CAPTURE SHIPPED (lmbrrr 03ce603): +2.16% ROTATED A/B (225.4 -> 230.2 tok/s, math q404 canonical arm, spreads +-0.3, byte-identical text + acceptance histograms across all arms/reps). The verify-capture path was eagerly materializing cat(conv_state, transpose(qkvz[..conv_dim])) per GDN layer per round — 18 strided copies (~0.9MB) + 18 cats — while rollback reconstruction only ever reads the last ksz=4 columns at the rollback prefix. ConvCapture::Lazy defers the transpose+cat to the rollback and narrows to exactly the needed columns; the tensor and tree paths keep the assembled Full variant (their windows exist anyway). Variant-parity regression test extends reconstruct_capture_state_matches_sequential_recurrence. This validates the re-ranked thesis: memory-traffic elimination pays where dispatch-count elimination (mtp_fc_prep, same day) was a wash. NOTE: run-007 profile (the trace behind the A1/A2 items) predates the reconstruction fusion by 15 minutes — its bmul_f32 17.3% headline was the old reconstruction chain, already harvested; remaining trace-mining should re-capture under the current stack. Also: QAT-r3 in flight (ap-KFj7ETPwBuGa2j4Aa0gCyD, INIT BASELINE 0.7503 = r1-under-fakequant, confirming the q4k drop; ~41k tok/s, ETA ~90min).
