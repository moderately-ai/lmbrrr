---
id: 1783985829043792000-360c492a
at: 1783985829
---
INSTRUMENT TRILEMMA RESOLVED + LANE VERDICT (vec-eval, M3). Three 'draft cost' numbers coexist: fenced ladder ~10ms (embeds a 1-3ms command-buffer commit floor per fence), LMBRRR_LOOP_TIMING gsweep ~9.9ms (same class), and PRODUCTION realized default_draft_ms ~15.5ms (refit artifacts; includes async queue waits — attribution across buckets is murky by design). The number that matters: production draft has been FLAT across every drafter kernel generation (fused-chain 14.25 / diet 15.7 / single-dispatch+vector-loads 15.48) and validation standings sat flat (~117 mean) through a 2x isolated-exec change. My earlier 'stale 15.37 baseline' correction was itself wrong — 15.37 was the production metric and it never moved. VERDICT: the drafter-side kernel micro-optimization lane is EXHAUSTED — closing rope-in-place and block-megakernel; the chain kernel ships as bitwise-neutral (correct, fewer dispatches, vector loads). No gputrace needed: even perfect backbone attribution cannot save block-propose economics on the M3. SPEC VIABILITY now routes through (a) the verify slope — gemv-width-splitk-concurrency becomes the arc's critical path (break-even: verify(2) 9.8 -> ~6.5ms + ~1ms draft at MTP tau 1.53 = ~204 tok/s > 182 greedy), and (b) MTP acceptance (distillation decision). Vector-load + single-dispatch chain kernels stay (strictly better, bitwise-verified, goldens byte-identical at pin f2c7fa82).
