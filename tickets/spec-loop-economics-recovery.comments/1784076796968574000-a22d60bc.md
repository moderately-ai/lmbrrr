---
id: 1784076796968574000-a22d60bc
at: 1784076796
---
MTP-HEAD QUANTIZATION EVAL (2026-07-15; receipts /tmp/mtp15-*): --mtp-quantize shipped THROUGH THE PACK (checkpoint-keyed mtp:{sha8}:{tier}:* entries inside the one sidecar; pack round-trip verified bitwise-equivalent — q4k runs a/b identical). VERDICTS on the tides dev arm (d3, slice, skip5):
- q4k: round 12.66 -> 11.25ms (-1.4, bigger than the bandwidth estimate) BUT p1 0.68 -> 0.54, commit/rd 2.30 -> 2.03 -> tok/s WASH (181.6 vs 180.3). The head was distilled in bf16; post-hoc 4-bit on a 20M-param drafter costs real acceptance. Could be revisited by quantization-aware distillation (r3 idea), not post-hoc.
- q8_0: ANOMALY — p1 collapsed to 0.27, BELOW q4k despite 4x the precision. Inverted precision ordering = suspected bug in the q8_0 m-in-[2,8] inference path (the MTP catch-up runs m 2-6 through the q8_0 mc/mv kernels — a corner the tower, being full-q4k, never exercises). Fresh-quantize and pack-take runs identical, so the pack is exonerated. FILED as a bisection item: quantize one linear at a time under q8_0; exposure elsewhere is limited (tower q4k; DSpark drafter q8_0 heads use the fused markov path).
DEFAULT: --mtp-quantize stays OFF (bf16 drafter weights). The pack plumbing is kept — it also pre-caches any future tier and costs nothing when unused.
