---
id: 1784075676968076000-a8e5c07b
at: 1784075676
---
SHIP-BAR TABLE — same-run spec vs greedy (2026-07-15, suite mtph2, per-question greedy from each run's own baseline via greedy_step_ms_estimate; script /tmp/shipbar.py on the M3):
math 225.8 vs 198.0 = +14.0 pct (all three questions 110-117 pct); translation 190.1 vs 185.2 = +2.6 (q164 at 130.8!); coding 192.4 vs 197.8 = 97.2 pct; qa 175.5 vs 193.9 = 90.5 (q326 114.6); writing 158.7 vs 197.3 = 80.4; summarization 150.9 vs 192.4 = 78.4. MEAN 182.2 vs 194.1 = 93.9 pct. Eight of 18 questions at/above their own greedy. All at r1 acceptance (p1 0.724); r2 at step 16.6k already holds 0.7691 holdout.

SHIP BAR (mean >= +2 pct, no class regressing >2): gap = +8.6 pct mean. Expected sources: r2 tau lift (largest on the mid classes: coding/qa are 3-10 pct short), post-r2 handoff retune for writing/summarization (parked-mode itself runs ~95 pct of greedy; the deficit is pre-parking drafted rounds + probes — skip_after and probe economics retune once tau moves), remaining kernel scraps (fused m-row verify argmax ~0.3ms, draft ICB ~1ms = ~+8-10 pct round-rate combined). ASYNC ROUND SPECULATION EXPLICITLY DECLINED for now: the loop is GPU-bound, so speculative misdraft costs real GPU time — EV negative at current full-accept rates (recorded so nobody re-treads it while the bottleneck is the GPU).
