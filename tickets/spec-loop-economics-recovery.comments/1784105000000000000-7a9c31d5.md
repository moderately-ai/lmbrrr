---
id: 1784105000000000000-7a9c31d5
at: 1784105000
---
CAST-FOLD SHIPPED (fork 280905a5, lmbrrr pin bump): the round-8 profile's cast_bf16_f32 x14 (1.8% of the verify window) attributed to the rollback path — the fused reconstruction emitted BF16 state and every subsequent chunk forward cast it back to F32 per GDN layer (~2MB traffic/layer). New kernel variant gated_delta_v2_reconstruct_bf16r_f32 stores float(bfloat(acc)) — value-identical to bf16-store+cast by construction — into an F32 buffer, making the next forward's dtype guard a no-op. M3: byte gate PASSED (identical text + histograms, rotated), +0.21% (230.0 -> 230.5, three-over-three). Cumulative canonical-arm floor today: 225.4 -> 230.5 (+2.3%) from the two bit-exact memory-traffic items. STATUS OF THE KERNEL SIDE: no bit-exact items of measurable size remain. Everything left is (a) bandwidth physics (lm_head 24.4% — lossless verify must read all vocab rows), or (b) margin-class and sequenced behind the r3 arbitration: unsplit body linears ~35% (qkvz=128 TGs, gate_up=112 TGs vs the 128 target), GDN core+prep 10.9%, verify-argmax fusion 2.9%, steel-attention m=4 variant 3.4%, reconstruct-precision (drop the bf16 rounding entirely). The moment the arbitration fixes the arm, that wave re-opens with ~40% of the verify window on the table.
