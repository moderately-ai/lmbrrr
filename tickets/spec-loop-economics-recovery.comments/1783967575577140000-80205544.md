---
id: 1783967575577140000-80205544
at: 1783967575
---
GAMMA-SWEEP RESULT (2026-07-13, M3, LMBRRR_LOOP_TIMING synchronized draft walls, math-style prompt): gamma=1: 9.10 ms/propose (11 rounds, noisy — small sample, do not over-read); gamma=3: 7.11; gamma=6: 10.62 (tau 2.71). The gamma3->gamma6 slope = ~1.17 ms per Markov step confirms the serial chain at ~5ms of the gamma-6 cost; the gamma-invariant base is ~6-7ms (backbone forward + heads + the propose readback drain). TWO-PRONGED PROPOSE ATTACK, both now sized: (1) FUSED MARKOV CHAIN kernel (one persistent-threadgroup dispatch iterating gamma steps: gather -> rank-256 gemv over the 32k draft vocab -> add base logits -> argmax) buys ~4-5ms; (2) the LAG PATH (propose_device — already built) removes the readback drain from the base. Combined target: propose ~6-8ms, which per the round-anatomy arithmetic flips math to 205-225 tok/s vs the 182 chain floor. IMPLEMENTATION ORDER for the next block: fused chain kernel first (self-contained fork kernel + bitwise gate vs the sequential chain), then chain-handoff for parked classes, then re-evaluate hysteresis/backoff under the new economics.
