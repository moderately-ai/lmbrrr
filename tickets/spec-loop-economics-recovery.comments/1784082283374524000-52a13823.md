---
id: 1784082283374524000-52a13823
at: 1784082283
---
FUSED MTP PRE-FC SHIPPED + DISPATCH-COUNT THESIS FALSIFIED (fork 2ba503f0, lmbrrr 8c3528c). mtp_fc_prep_bf16: 2x rmsnorm + concat + dense fc GEMV in one dispatch, bit-identical to the unfused chain at m==1 by construction (512-leaf reduction-pairing emulation + exact gemv-tile accumulation-order replication; fork test mtp_fc_prep_matches_unfused_chain enforces it). M3 byte gate PASSED: identical text/accepted_histogram/rounds/rollbacks across 8 runs (canonical arm, math q). Timing WASH: 225.6 vs 225.6 tok/s; fenced draft bucket 70.2 vs 70.1ms; engagement verified (13 fused dispatches/16 tok, M4 debug print). VERDICT: removing 3 of ~19 chain-step dispatches costs ~0 in the unfenced production loop — inter-dispatch boundaries next to real work are single-digit us, not the ~35us/dispatch from fenced probes; fencing manufactures the gap it measures. Kernel stays default-on (LMBRRR_FUSED_MTP_FC=0 opt-out): bit-identical, fewer dispatches, parity technique proven. IMPLICATION for the remaining ranked list: fusion/collapse items pay only via removed GPU work or memory traffic; launch-count items (persistent kernel, megakernel, ICB) need unfenced gap evidence first. ALSO: run_spec_suite.py divergence detector was dead code (read nonexistent committed_text; field is text) — fixed; all prior "no divergent content" suite conclusions were vacuous.
