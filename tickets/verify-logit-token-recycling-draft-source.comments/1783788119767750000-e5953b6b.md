---
id: 1783788119767750000-e5953b6b
by: "claude"
at: 1783788119
---
CARVED OFF ngram-draft-source-mux (2026-07-11). The prompt-lookup half shipped (--pld, gated); this is the second draft-free source from the frontier survey: bank the top-k candidates from every verify pass logits (which we already compute and discard) in a token->next-candidates adjacency table, and when the current token has banked candidates and no n-gram match fires, chain them as a draft. Targets the NON-quoting case (free-form prose) where prompt-lookup finds nothing and the drafter is weakest. Design notes from the PLD investigation that carry over: (1) keep proposed span SHORT (accepted runs are short; wide = rejected-tail verify waste); (2) GATE to scheduler-skip rounds like PLD, or better, mux by expected value against the drafter (recycling EV from candidate rank/frequency vs drafter confidence-head EV) - unconditional firing preempts strong drafter rounds and loses; (3) exact-argmax verify keeps it lossless. Measure rotated same-session under quiet load; compose after PLD in the round mux (PLD match > recycling > drafter > greedy, each EV-gated).
