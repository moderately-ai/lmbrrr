---
id: 1783788877837595000-6b91e5e5
by: "claude"
at: 1783788877
---
IMPLEMENTED (commit above), measurement pending quiet machine. Design departs from the reference (arXiv 2408.08696) deliberately: no 80-node draft tree (their main lever; structurally expensive on our linear-attention target) -> short margin-gated CHAINS instead, so expected gain is modest and concentrated on skip rounds with near-deterministic continuations. Viability math recorded in the ticket and code: depth-1 break-even acceptance ~77% at measured verify costs, hence hard margin gating (default 6.0 logits, tunable). Harvest is a provably-exact two-stage top-k (chunk-maxima bound), ~20KB readbacks per verify, tie-parity with candle argmax under test. Bench matrix when load clears: {ctl, pld, pld+recycle} x {summary, tides, math, code} x 3 reps rotated + margin-threshold sweep {4, 6, 9} on the winning class; then re-run with the round-2 drafter (gate interaction). If recycling adds nothing over PLD alone at any margin, record the negative and disable by default - the harvest path stays (feeds future MTP/EV work).
